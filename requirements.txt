# TopOptPilot Python Dependencies
# AI Scientist Layer
pi-agent[openai]==0.1.0 # Safe-mode Python fallback; official Pi RPC is installed by npm
openai>=1.0.0          # OpenAI-compatible fallback transport
pydantic>=2.0.0        # Data modeling & validation
pyyaml>=6.0            # Config & method cards

# MCP integration uses the bundled official MathWorks server over JSON-RPC stdio.

# Knowledge & Evidence
pymupdf>=1.24.0        # PDF text extraction
pymupdf4llm>=0.1.0     # PDF to markdown for LLM ingestion
arxiv>=2.0.0           # ArXiv API wrapper

# Experiment Management
jsonschema>=4.20       # Task/Result schema validation

# Visualization & Reporting
matplotlib>=3.8        # Convergence curves, metrics plots
numpy>=1.26            # Numerical arrays
scipy>=1.11            # Sparse FEM and numerical solvers
pandas>=2.0            # Workspace convergence tables

# Local authenticated desktop sidecar API
fastapi>=0.110,<0.116
uvicorn>=0.29
websockets>=12,<16

# CLI & Utilities
click>=8.1             # CLI interface
rich>=13.0             # Terminal output formatting
python-dotenv>=1.0     # Environment variable management
