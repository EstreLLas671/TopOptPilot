# TopOptPilot 实施计划

> **V6.0.0 当前状态**：P0 全部落地，P1 大部分落地，P2 设置页面已落地，桌面端已打包（v6.0.0 NSIS 安装包 220MB），发布门禁离线全部门通过。
> 本文档分两部分：**V5.0 已完成回溯**（旧 Phase 0–3 与 V5/V6 新架构的映射）+ **V6.0.0 后续路线**（基于 `release_audit.json` 与 `缺口分析.md`）。

---

## Part A — V5.0 已完成回溯

### Phase 0：地基加固（P0）— ✅ 全部完成

| 任务 | 计划产出 | V5 实际落地 | 验证 |
|------|----------|-------------|------|
| 0.1 LLM Client 模块 | `agent/llm/client.py` + `message_builder.py` + `response_parser.py` | ✅ 全部存在；`PiAgentClient` + Qwen 3.7 Plus + 指数退避重试 | `release_audit.json` `online_qwen` 门禁（外部凭据跳过） |
| 0.2 提示词结构化输出 | `agent/prompts/system_prompts.py` 追加 JSON Schema | ✅ 6 角色专用 system prompt + OUTPUT_FORMATS 映射 | 集成测试 T2 |
| 0.3 编排器接入 LLM | `agent/orchestrator.py` 每 phase 调用 LLM | ✅ 已导入 `PiAgentClient` / `MessageBuilder` / `ResponseParser` | `agent/orchestrator.py` LLM 路径完整 |
| 0.4 运行时异常处理 | `mcp/solver_mcp/mex_handle_manager.py` 完整实现 | ⚠️ `mex_handle_manager.py` 仍 docstring-only；**CUDA MEX 路径整体未启用**，当前求解走 Python FEM 或 MATLAB MCP | `solver/` 真实求解器已替代 |
| 0.5 NaN/Inf 检测器 | `experiments/result_manager.py` 追加检查 | ✅ orchestrator 导入 `NaNChecker`；`FailureDetector` 5 类失败检测 | 集成测试 T5 |
| 0.6 ExperimentQueue | `experiments/experiment_queue.py` 异步队列 | ✅ 实现；V5 同时有 `topoptpilot/executor/queue.py` (ProcessPoolExecutor) | `release_audit.json` `cases` 门禁（A/B/C 闭环） |
| 0.7 Pi RPC Provider 重试 | `topoptpilot/agent_runtime/pi_bridge.py` 最多 2 次 → Safe Mode | ✅ 实现；失败时 `ResearchService._safe_mode_next()` 接管 | `release_audit.json` `safe_mode` 门禁 |

**Phase 0 验收证据**：`release_audit.json` `safe_mode.pass = true`，闭环 `ESTABLISH_BASELINE → REDUCE_GRAYNESS → BUDGET_EXHAUSTED` 完整运行；FEM 残差 2.87e-13 / 3.40e-13。

---

### Phase 1：闭环稳健化（P1）— ⚠️ 旧设计大部分被 V5 新架构替代

Phase 1 原本设计"扩展状态机 + 新增 6 个 Agent 角色"，但 V5 采用了全新四层解耦架构（Pi Agent → Policy → Solver → Evaluator），原有任务被以下模块替代：

| 旧任务 | 状态 | V5 替代方案 | 替代文件 |
|--------|:----:|-----------|---------|
| 1.1 状态机扩展（3 新状态） | 🟡 | IntentCompiler 7 种意图 + FailureDetector 5 类失败 + ReviewerWorkflow 4 类触发器 | `policy/intent_compiler.py` · `evaluator/failure_detector.py` · `agent_runtime/reviewer.py` |
| 1.2 假设精炼 Agent | 🟡 | ReviewerWorkflow HYPOTHESIS_REVISION 触发器 | `agent_runtime/reviewer.py` |
| 1.3 求解器诊断路径 | 🟡 | FailureDetector INFRASTRUCTURE + ActionPolicy RETRY_OR_REVISE | `evaluator/failure_detector.py` · `policy/action_policy.py` |
| 1.4 区分性实验 Agent | ✅ | `doe_templates.discriminating_experiments()` 3 个命名模板 | `policy/doe_templates.py` |
| 1.5 RollbackManager | ❌ | **未实现**；`/rollback E01` 命令为占位 | — |
| 1.6 插件回退链 | ❌ | **未实现**；plugins/registry/ 无 fallback_chain.py | — |
| 1.7 假设模糊度检测器 | ❌ | **未实现** | — |
| 1.8 粗网格快速筛选 | ✅ | FidelityManager F0–F3 多保真分层 + `estimated_cost()` 预算校验 | `fidelity/manager.py` |
| 1.9 数值合理性校验 | ⚠️ | `policy/safety_guard.py` 参数硬约束（volfrac/rmin/beta/penal/max_iter + 允许列表） | `policy/safety_guard.py` |

**Phase 1 验收现状**：`offline_release_ready = true`，核心闭环已闭环。仅 RollbackManager / 插件回退链 / 假设模糊度检测仍未实现。

---

### Phase 2：知识库升级（P2）— ⚠️ 整体被 Research Memory L0–L3 替代

| 旧任务 | 状态 | V5 替代方案 |
|--------|:----:|-----------|
| 2.1 SQLite 数据库设计 | 🟡 | `topoptpilot/memory/research_state.py` 7 张表（research / experiments / events / decisions / proposals / memory_snapshots / agent_sessions） |
| 2.2 SQLite 访问层 | 🟡 | `ResearchStateStore` (WAL journaling, thread-safe) |
| 2.3 FAISS 向量索引 | ❌ | **未实现**（L3 语义检索） |
| 2.4 KnowledgeBase 整合 | 🟡 | `ResearchMemory.build()` L0/L1/L2/L3 四层 |
| 2.5 种子方法卡片库 | ❌ | **未实现**；`.pi/skills/` 6 个技能部分承担"专家知识"角色 |
| 2.6 知识冲突检测 | ❌ | **未实现** |

**替代思路**：V5 用"结构化实验证据层级记忆"（L0–L3）替代"论文方法卡片语义检索"。
L3 FAISS 与冲突检测仍未实现。

---

### Phase 3：演示冲刺（P3）— ⚠️ 技术路线完全改变

| 旧任务 | 状态 | V5 实际落地 |
|--------|:----:|-----------|
| 3.1 FastAPI+React 驾驶舱 | ❌ 废弃 | 改为 **Tauri 2 + React** 桌面应用（v6.0.0） |
| 3.2 预计算结果缓存 | 🟡 | `topoptpilot/executor/cache.py`（SHA256 任务键） |
| 3.3 失败→修正录像 | — | 非代码产物（不在本仓库） |
| 3.4 CPU/GPU 一致性录像 | — | 非代码产物 |
| 3.5 全新环境复现测试 | ✅ | `topoptpilot/replay.py` 离线回放；`release_audit.json` 已记录 fresh venv 验证 |
| 3.6 答辩 PPT | — | 非代码产物 |

---

### V5.1 新增模块（旧 Phase 完全未覆盖）

| 模块 | 文件 | 说明 |
|------|------|------|
| **Pi RPC 运行时** | `agent_runtime/` (5 文件) | Pi Bridge / Session / ToolGateway / EventMapper / Reviewer |
| **Policy Engine** | `policy/` (5 文件) | SafetyGuard / ActionPolicy / ApprovalPolicy / IntentCompiler / DOE Templates |
| **Research Memory** | `memory/` (4 文件) | L0–L3 记忆 + SQLite 持久化 |
| **Executor + Cache** | `executor/` (3 文件) | ProcessPoolExecutor + SHA256 缓存 |
| **Evaluator** | `evaluator/` (3 文件) | 4 项检查 + 5 类失败 + metrics 扁平化 |
| **Fidelity Manager** | `fidelity/manager.py` | F0–F3 多保真分层预算 |
| **Benchmarks** | `benchmarks/` (2 文件) | 5 方法 (Random/Grid/TPE/Rule/Pi) + 4 消融 + 13 项指标 |
| **Cases A/B/C** | `cases/` (4 文件) | 三案例闭环 + EvidenceCaseRunner |
| **Tauri 桌面** | `desktop/` | Tauri 2 + React 19 + TypeScript + i18n zh-CN/en-US + NSIS 安装包 |
| **Desktop Sidecar** | `api/desktop_sidecar.py` | PyInstaller 单文件 + 随机端口 + 一次性 token |
| **MATLAB MCP** | `mcp/matlab_mcp/` (5 文件) | 官方 MathWorks MATLAB MCP Server v0.12.0 + topopt_run_task.m |
| **求解器模块** | `求解器模块/` | MATLAB 参考实现（F3 权威资源，纳入版本管理） |
| **Release Audit** | `release_audit.py` | 8 项发布门禁 + `release_audit.json` |
| **Replay** | `replay.py` + `solver/matlab_adapter/replay.py` | 全量研究回放 + 跨后端 record-and-replay |
| **AGENTS.md** | 根目录 + `topoptpilot/AGENTS.md` | 不可变科研宪法（14 条规则） |

### V6.0.0 新增模块（V5.1 未覆盖）

| 模块 | 文件 | 说明 |
|------|------|------|
| **Settings 页面** | `desktop/src/SettingsWorkspace.tsx` + `settings.css` | 5 个 Tab 设置 UI（通用/Agent/MATLAB/默认值/诊断） |
| **Settings 数据模型** | `schemas/models.py`（AppSettings / AgentSettings / ComputeSettings / NewResearchSettings / DataSettings） | Pydantic 校验 + 深度合并 |
| **app_settings 表** | `memory/research_state.py` | 第 8 张 SQLite 表，单行 JSON UPSERT |
| **Settings API** | `api/fastapi_app.py` 8 个新端点 | GET/PATCH settings + test-agent + restart-pi + restart-matlab + diagnostics + export + clear-cache |
| **Settings 持久化** | `service/research_service.py` | get_settings / update_settings / test_agent_settings / restart_pi / diagnostics / clear_regenerable_cache |
| **MATLAB 路径安全** | `mcp/matlab_mcp/matlab_connector.py` | 校验路径在 research_root 内 |
| **Desktop bootstrap** | `desktop/src-tauri/src/lib.rs` | desktop-bootstrap.json（next_data_dir 工作区配置）+ CREATE_NO_WINDOW 标志 |
| **TypeScript 类型** | `desktop/src/types.ts` | AppSettings / SettingsDiagnostics 接口 |
| **Settings API client** | `desktop/src/api.ts` | saveSettings / testAgent / restartPi / restartMatlab / diagnostics / clearCache |

---

## Part B — V6.0.0 后续路线

### P0 — 解除 `release_ready=false`（外部凭据，1 小时）

| 任务 | 文件 | 验收 |
|------|------|------|
| 配置有效 `DASHSCOPE_API_KEY`，重跑 online_qwen 门禁 | `.env` | `release_audit.json` `online_qwen.pass = true` → `release_ready = true` |

---

### P1 — 补齐 Phase 1 中仍有价值的功能（3–5 天）

| 任务 | 文件 | 验收 |
|------|------|------|
| **RollbackManager** | 新增 `experiments/rollback_manager.py` | Workspace `/rollback E01` 实际回滚 + 重做 |
| **假设模糊度检测** | `policy/intent_compiler.py` 或 `policy/safety_guard.py` | 不可证伪意图被标记为 AMBIGUOUS 而非 INVALID |
| **插件缺失回退链** | 新增 `plugins/registry/fallback_chain.py` | ExperimentAgent 在插件缺失时自动查询替代 |

---

### P2 — Tauri 桌面 UI 完善（3–5 天）

| 任务 | 状态 | 文件 | 验收 |
|------|:----:|------|------|
| **Settings 设置页面** | ✅ V6.0.0 已完成 | `desktop/src/SettingsWorkspace.tsx` | 5 个 Tab（通用/Agent/MATLAB/默认值/诊断）+ 8 个 Settings API 端点 |
| Research Navigator（多研究切换增强） | ❌ | `desktop/src/App.tsx` | 多研究并行视图；研究间对比 |
| Experiment Canvas（交互式参数探索） | ❌ | `desktop/src/App.tsx` 新组件 | 拖拽参数滑块 + 实时 FEM 反馈 |
| Evidence Inspector（方法卡片/证据链视图） | ❌ | 新增 `desktop/src/components/EvidenceInspector.tsx` | 可视化方法卡片与证据引用 |
| Command Composer 增强（命令历史/补全） | ❌ | `desktop/src/App.tsx` composer 区 | 上下键历史 + Tab 补全 |
| WebSocket 断线重连 | ❌ | `desktop/src/api.ts` | 网络恢复后自动续流 |

---

### P3 — 知识库增强（可选，5–7 天）

| 任务 | 文件 | 验收 |
|------|------|------|
| **Paper-to-Plugin 核心** | `knowledge/paper_processor.py` + `agent/tools/paper_reader.py` 重写 | 上传 PDF → LLM 抽取公式/参数 → 自动生成方法卡片 |
| **L3 FAISS 语义检索** | 新增 `topoptpilot/memory/vector_store.py` | 相似实验检索 + 历史复现包索引 |
| **种子方法卡片库** | 新增 `knowledge/seeds/*.json` (≥5) | 首次运行自动植入 heaviside_projection / pde_filter / oc_optimizer / sensitivity_filter / mma_optimizer |
| **知识冲突检测** | 新增 `knowledge/conflict_detector.py` | 检测两篇论文对同一方法的矛盾结论 |

---

### P4 — 演示材料（非代码，2–3 天）

| 任务 | 验收 |
|------|------|
| 10 分钟 Demo 脚本（产品方案 §47 已定义） | 演示脚本文件 + 预计算结果 |
| 失败→修正录像（ScreenToGif / OBS） | mp4 / gif |
| 答辩 PPT | pptx |

---

## 验收基线

| 基线 | 当前状态 | 后续目标 |
|------|---------|---------|
| `offline_release_ready` | ✅ true | 维持 |
| `release_ready` | ❌ false (仅 online_qwen 跳过) | ✅ true（P0 解除） |
| 10 项门禁全部 pass | 9/10 | 10/10 |
| 3 案例闭环 | ✅ A/B/C | 维持 |
| 5 方法基线矩阵 | ✅ Random/Grid/TPE/Rule/Pi | 维持 |
| 桌面安装包 | ✅ 220MB NSIS (v6.0.0) | 维持 |
| FEM 残差 | ~3e-13 | 维持 |
| F3 严格性 | ✅ 禁止 Python 伪回退 | 维持 |

---

## 文件修改对照表（后续工作）

| 目标 | 文件操作 |
|------|---------|
| P0 online_qwen | 修改 `.env` |
| P1 RollbackManager | 新增 `experiments/rollback_manager.py`，更新 `topoptpilot/app/workspace/command_bar.py` `/rollback` 处理 |
| P1 模糊度检测 | 修改 `topoptpilot/policy/intent_compiler.py` 或 `policy/safety_guard.py` |
| P1 回退链 | 新增 `plugins/registry/fallback_chain.py`，更新 `plugins/registry/plugin_registry.py` |
| P2 桌面增强 | 修改 `desktop/src/App.tsx`、`desktop/src/api.ts`，新增组件文件 |
| P2 Settings | 已完成 V6.0.0（`SettingsWorkspace.tsx` + Settings API 8 端点 + `app_settings` 表） |
| P3 Paper-to-Plugin | 重写 `knowledge/paper_processor.py`、`agent/tools/paper_reader.py` |
| P3 FAISS | 新增 `topoptpilot/memory/vector_store.py`，更新 `topoptpilot/memory/research_memory.py` |
| P3 种子卡片 | 新增 `knowledge/seeds/*.json` |
| P3 冲突检测 | 新增 `knowledge/conflict_detector.py` |
| P4 Demo 材料 | 新增非代码文件 |
