# TopOptPilot 实施计划

> **V5.0 状态**：P0 全部落地，P1 的 Python 3D、DOE、因果 Skill、Reviewer、Warm Start
> 与 Baseline Dashboard 已落地；发布证据见 `V5实施与验收.md`。

> **V4.1 变更**：前端阶段调整为 Streamlit Workspace；FastAPI 仅保留测试接口。
> 下文旧 Web/TUI 版条目属于历史估算，不再执行。

> 基于当前架构评审，按优先级分阶段推进。每个阶段有明确的验收标准。

---

## 阶段总览

```
┌──────────────────────────────────────────────────────────────┐
│ Phase 0: 地基加固（P0修复）    预估：5天                     │
│   LLM适配器 · 异常处理 · 异步实验队列                        │
├──────────────────────────────────────────────────────────────┤
│ Phase 1: 闭环稳健化（P1修复）   预估：7天                     │
│   状态机扩展 · 回滚机制 · 回退链 · 快速筛选                  │
├──────────────────────────────────────────────────────────────┤
│ Phase 2: 知识库升级              预估：5天                     │
│   SQLite结构化 · FAISS语义检索 · 自进化机制                  │
├──────────────────────────────────────────────────────────────┤
│ Phase 3: 演示冲刺                预估：7天                     │
│   驾驶舱完善 · 预计算结果 · 视频录制 · 全新环境复现          │
└──────────────────────────────────────────────────────────────┘
                   总预估：24个工作日
```

---

## Phase 0：地基加固（P0 修复）

**目标**：让 Agent 真正能调 LLM、能扛运行时异常、实验队列不阻塞。

### 任务列表

| # | 任务 | 产出 | 预估 |
|---|------|------|------|
| 0.1 | **LLM Client 模块** | `agent/llm/client.py` + `message_builder.py` + `response_parser.py` | 1.5天 |
| 0.2 | **提示词结构化输出** | `prompts/system_prompts.py` 追加 JSON Schema 输出约束 | 0.5天 |
| 0.3 | **编排器接入 LLM** | `orchestrator.py` 每个 phase 调用`builder→client→parser` | 1天 |
| 0.4 | **运行时异常处理** | `mcp/solver_mcp/mex_handle_manager.py` 完整实现（崩溃重启+显存监控） | 1天 |
| 0.5 | **NaN/Inf 检测器** | `experiments/result_manager.py` 追加数值合法性检查 | 0.5天 |
| 0.6 | **ExperimentQueue** | `experiments/experiment_queue.py` 异步提交+轮询+回调 | 1.5天 |
| 0.7 | **Pi RPC Provider 重试** | `topoptpilot/agent_runtime/pi_bridge.py` 最多2次→Safe Mode | 已完成 |

### 验收标准

```
[⚠] 在线决策来自官方 Pi/Qwen；当前凭据 401 时明确进入 Rule Safe Mode
[✓] 每个 Agent 输出严格遵循 JSON Schema 约束
[✓] CUDA MEX 崩溃时自动重启 MATLAB 会话 + 日志记录
[✓] 计算结果含 NaN 时立即停止该轮 + 回滚到上一个 checkpoint
[✓] 30 组实验可异步提交，编排器不阻塞
```

### 关键文件修改

```
agent/llm/                    ← 新增目录
  ├── __init__.py
  ├── client.py               # PiAgent runtime 适配器
  ├── message_builder.py      # 提示词→messages
  └── response_parser.py      # LLM回复→Pydantic模型

agent/prompts/system_prompts.py  ← 追加 JSON Schema 输出约束
agent/orchestrator.py           ← 每个 phase 接入 LLM
experiments/experiment_queue.py ← 新增：异步实验队列
experiments/result_manager.py   ← 追加 NaN/Inf 检测
mcp/solver_mcp/mex_handle_manager.py ← 完整实现
```

---

## Phase 1：闭环稳健化（P1 修复）

**目标**：补齐三个缺失状态机状态、实现参数回滚、插件回退链、假设模糊度检测。

### 任务列表

| # | 任务 | 产出 | 预估 |
|---|------|------|------|
| 1.1 | **扩展状态机** | `state_machine.py` 新增 `HYPOTHESIS_REFINEMENT` / `SOLVER_DIAGNOSIS` / `DISCRIMINATING_EXPERIMENT` | 1天 |
| 1.2 | **假设精炼 Agent** | `agent/roles/hypothesis_refiner.py` 保留合理部分，调整权重/条件 | 1.5天 |
| 1.3 | **求解器诊断路径** | `agent/roles/solver_diagnostics.py` 诊断报告 → 修复建议 → 重做 | 1天 |
| 1.4 | **区分性实验 Agent** | `agent/roles/discriminating_experiment.py` 找到两个假设分歧最大的测试条件 | 1.5天 |
| 1.5 | **RollbackManager** | `experiments/rollback_manager.py` checkpoint 保存 → 回滚 → 寻找最近稳定点 | 1天 |
| 1.6 | **插件缺失回退链** | `plugins/registry/fallback_chain.py` 自动查询替代插件 | 0.5天 |
| 1.7 | **假设模糊度检测器** | `agent/roles/hypothesis_quality_checker.py` | 0.5天 |
| 1.8 | **粗网格快速筛选** | `experiments/quick_screener.py` 三级选择的第三级 | 1天 |
| 1.9 | **数值合理性校验** | `agent/roles/research_lead.py` 追加 E>0, nu∈(0,0.5), vf∈(0,1] | 0.5天 |

### 验收标准

```
[✓] 假设被部分否定时进入 HYPOTHESIS_REFINEMENT，而非全盘重来
[✓] GPU/CPU偏差过大时进入 SOLVER_DIAGNOSIS
[✓] 两组假设无法区分时自动设计区分性实验
[✓] 参数回滚能找到最近稳定 checkpoint 并恢复
[✓] 缺少插件时自动查询回退链，选择合法替代
[✓] "这个方法可能有效"这样的模糊假设在 REVIEW 前被打回
[✓] 完整实验矩阵前先用粗网格筛选
```

### 新增/修改文件

```
agent/state_machine.py           ← 扩展3个新状态
agent/roles/hypothesis_refiner.py        ← 新增
agent/roles/solver_diagnostics.py        ← 新增
agent/roles/discriminating_experiment.py ← 新增
agent/roles/hypothesis_quality_checker.py ← 新增
experiments/rollback_manager.py ← 新增
experiments/quick_screener.py   ← 新增
plugins/registry/fallback_chain.py ← 新增
agent/roles/research_lead.py    ← 修改
```

---

## Phase 2：知识库升级

**目标**：从 JSON + 关键词匹配升级为 SQLite + FAISS 三级架构，引入自进化机制。

### 任务列表

| # | 任务 | 产出 | 预估 |
|---|------|------|------|
| 2.1 | **SQLite 数据库设计** | `knowledge/kb_schema.sql` 表结构：method_cards / experiments / references / plugins / conflicts / embeddings | 0.5天 |
| 2.2 | **SQLite 访问层** | `knowledge/sqlite_store.py` CRUD + FTS5 全文索引 | 1天 |
| 2.3 | **FAISS 向量索引** | `knowledge/vector_store.py` 方法卡片/论文摘要/实验结论嵌入 + 检索 | 1.5天 |
| 2.4 | **KnowledgeBase 整合** | `knowledge/knowledge_base.py` 统一接口：query / add / detect_conflict / get_seed | 1天 |
| 2.5 | **种子方法卡片库** | `knowledge/seeds/` 至少5篇核心论文的手工方法卡片（JSON） | 0.5天 |
| 2.6 | **知识冲突检测** | `knowledge/conflict_detector.py` 比较两篇论文对同一方法的适用条件和效果 | 0.5天 |

### 验收标准

```
[✓] SQLite 表结构稳定，支持 method_cards / experiments / references 三类核心数据
[✓] FTS5 全文搜索能够快速检索中英文方法卡片
[✓] FAISS 向量检索支持语义搜索（"哪个方法与PDE滤波最相似"）
[✓] 首次运行时自动植入种子方法卡片（≥5篇）
[✓] 发现矛盾论文时自动标记 competing + 生成冲突报告
[✓] 每次实验结束后自动将结果+裁决写入知识库
```

### 新增文件

```
knowledge/kb_schema.sql              ← SQLite 建表语句
knowledge/sqlite_store.py            ← 结构化存储层
knowledge/vector_store.py            ← 向量检索层
knowledge/knowledge_base.py          ← 统一接口
knowledge/conflict_detector.py       ← 冲突检测
knowledge/seeds/                     ← 种子卡片目录
  ├── heaviside_projection.json
  ├── pde_filter.json
  ├── oc_optimizer.json
  ├── sensitivity_filter.json
  └── mma_optimizer.json
```

---

## Phase 3：演示冲刺

**目标**：前端完善、预计算结果、视频录制、全新环境复现通过。

### 任务列表

| # | 任务 | 产出 | 预估 |
|---|------|------|------|
| 3.1 | **科研驾驶舱 Web 版** | `frontend/web_cockpit/` 基于 FastAPI + React 简版 | 2天 |
| 3.2 | **预计算结果缓存** | `experiments/precompute_cache.py` 为演示准备离线结果集 | 1天 |
| 3.3 | **失败→修正完整录像** | 一次完整的第一轮失败→Agent回滚→第二轮成功的录制 | 1天 |
| 3.4 | **CPU/GPU 一致性验证录像** | 小网格 + 中网格的一致性对比画面 | 0.5天 |
| 3.5 | **全新环境复现测试** | 从 git clone → pip install → 运行，记录全部步骤 | 1天 |
| 3.6 | **答辩 PPT + 文档** | 10 分钟演示脚本、技术方案精简版、QA 预备 | 1.5天 |

### 验收标准

```
[✓] Web 驾驶舱能在浏览器中查看实验进度和结果
[✓] 10分钟演示全部 9 个阶段有对应的现场操作或预录制画面
[✓] 第一次失败 + 回滚 + 第二次成功的完整日志可播放
[✓] 全新环境（另一台机器）能一键复现完整实验结果
[✓] 答辩材料覆盖：问题→架构→Paper-to-Plugin→失败→修正→结论
```

### 新增文件

```
frontend/web_cockpit/
  ├── __init__.py
  ├── app.py                 # FastAPI 入口
  ├── templates/             # React/HTML 模板
  └── static/                # CSS/JS
experiments/precompute_cache.py ← 新增
```

---

## 时间线总表

```
Day 1-3    │ Phase 0.1-0.3: LLM Client + 结构化输出 + 编排器接入
Day 4-5    │ Phase 0.4-0.7: 异常处理 + NaN检测 + 实验队列 + API重试
           │ ─── 里程碑：Agent 首次输出来自官方 Pi/Qwen ───
Day 6-7    │ Phase 1.1-1.3: 状态机扩展 + 假设精炼 + 求解器诊断
Day 8-9    │ Phase 1.4-1.5: 区分性实验 + RollbackManager
Day 10     │ Phase 1.6-1.9: 回退链 + 模糊度检测 + 快速筛选 + 数值校验
           │ ─── 里程碑：第一次"失败→回滚→修正→第二轮"闭环 ───
Day 11-13  │ Phase 2.1-2.4: SQLite + FAISS + KnowledgeBase
Day 14     │ Phase 2.5-2.6: 种子卡片 + 冲突检测
           │ ─── 里程碑：知识库从关键词升级为语义检索 ───
Day 15-16  │ Phase 3.1-3.2: Web驾驶舱 + 预计算结果
Day 17-18  │ Phase 3.3-3.4: 录制视频 + 一致性验证
Day 19     │ Phase 3.5-3.6: 复现测试 + 答辩材料
           │ ─── 里程碑：可提交候选版本 ───
```

---

## 风险与缓冲

| 风险 | 概率 | 影响 | 应对 |
|------|------|------|------|
| Qwen API 不稳定 | 中 | 高（Agent 全依赖 API） | Pi RPC 重试+Rule Safe Mode+离线缓存 |
| CUDA MEX 调试超支 | 高 | 高 | Phase 0 边界仅完成后端框架，MEX细节可延到 Phase 3 |
| FAISS 嵌入模型选择耗时 | 中 | 中 | 先用可替换的国产嵌入模型，快速验证后再调优 |
| 演示现场 GPU 异常 | 中 | 高（核心演示环节） | Phase 3 预计算结果 + 录像备份 |
| 团队人力不足 | 高 | 高 | 优先 Phase 0+1，Phase 2 可裁剪为仅 SQLite、去 FAISS |

---

## 最终可交付成果

```
[✓] 一套可运行的 CPU-TOP3D_XL 基线与 CUDA MEX 求解器接口
[✓] 6个Agent角色完整 LLM 驱动（不再硬编码）
[✓] 扩展状态机（含假设精炼 / 求解器诊断 / 区分性实验）
[✓] 参数回滚 + 插件回退链
[✓] 三级知识库（SQLite + FAISS）
[✓] 种子方法卡片库（≥5篇核心论文）
[✓] 20-30 组可追溯实验（预计算或真实结果）
[✓] Web 科研驾驶舱原型
[✓] 失败→修正演示录像
[✓] 全新环境一键复现包
[✓] 答辩 PPT + 10分钟演示脚本
```
