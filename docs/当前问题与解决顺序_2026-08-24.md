# iDeskTop v2 当前问题与解决顺序

更新日期：2026-08-24
发布口径：标准版使用内置 PyInstaller sidecar 和用户电脑中的完整 MATLAB；MATLAB Runtime 为可选能力，不阻塞标准版。

## 当前结论

标准版的功能开发、确定性构建、隔离安装和已安装 sidecar 真实 MATLAB 小网格任务已经完成。当前不存在阻塞代码合并的 P0 功能缺陷。

- Agent 核心目录与 TopOptPilot `main@ce5e680` 保持一致，融合层继续提供工程工作区、F0-F3 校验和 Windows 稳定性修复。
- API Key 由环境变量或 Windows Credential Manager 提供，不进入 SQLite、普通设置、日志、报告或接口响应。
- 正式包使用 Python 3.12.13 packaged sidecar，不依赖用户安装 Python。
- MATLAB R2024b `24.2.0.2712019` 已通过真实 `matlab -batch` 探针；MCP warmup 状态为 `READY`。
- 最新标准 NSIS 不含 Runtime，短路径隔离安装的 14,534 个文件与 release staging 完全一致。
- 已安装 sidecar 完成 12×6、3 次迭代本机 MATLAB 任务；生成 3 份密度、3 份应力快照和报告，任务结束后相关新增进程为 0。

## 已完成门禁

- Python：157 passed。
- React/Vitest：53 passed；production build 通过。
- Rust：32 passed；`cargo fmt --check` 通过。
- Python `compileall` 和 `git diff --check` 通过。
- 离线 release audit：`offline_release_ready=true`。
- 标准安装包 SHA-256：`91103E61253A5C0D694537D161AF90FE0C4627BB9C51104DA7967DF5EF5439B5`。
- 安装包 Authenticode：`NotSigned`；MathWorks MCP Authenticode：`Valid`。

## 剩余非阻塞项

### P1：完整桌面窗口启动/正常退出记录

sidecar、资源、鉴权和真实 MATLAB 任务已从安装目录验证。短路径隔离安装的静默卸载退出码为 0，安装目录及 14,536 个文件已完全清理。仍建议在发布 Release 前补充一次 Tauri 窗口启动和正常关闭记录；这是发布运维验收项，不影响当前代码 PR。

### P1：在线 Qwen/Pi 真实连接

没有用户提供的 DashScope API Key 时，系统正确返回 `not_configured` 并允许离线 Safe Mode。真实在线连接、Pi 重启和受控 Agent 请求需由用户在客户端填写有效密钥后完成；密钥不得出现在日志或验收包中。

### P2：代码签名与独立机器矩阵

当前安装包未签名，也尚未在另一台干净 Windows 主机上复验。对外发布前应配置 Authenticode 证书并补做独立机器安装矩阵；在此之前只能标记 `NotSigned`，不能宣称已通过干净机器验证。

### P2：可选 Runtime 版

compiled Runtime 入口保留，但不属于本次标准版阻塞项。后续只有在 solver 与 MATLAB Runtime 版本匹配并通过无完整 MATLAB 的真实 E2E 后，才能单独发布 Runtime 版。

## 已确认不是产品缺陷的现象

最后一次 E2E 命令因把报告目录写成 `engineering-runs/<run_id>` 而退出 1；产品真实数据根使用 `runs/<run_id>`。正确目录下的 `report.md` 已存在，SHA-256 为 `A91FE0FC06596ADC14F6ABC076D7322B7EB4803922DA31D27D943DC900A3231D`。同一运行还包含 3 份密度和 3 份 von Mises 应力快照，因此不需要为该验收脚本路径错误修改产品代码。

## 后续顺序

1. 完成并合并当前代码 PR。
2. 使用客户端设置页填写合法 DashScope API Key，补做在线 Qwen/Pi 验收。
3. 配置代码签名并在独立 Windows 主机复验标准安装包。
4. 将匹配版本的 Runtime 版作为独立发布工作处理。

## 本次安全清理

- 删除 PyInstaller、Tauri、Vite、测试输出、重复 resources staging、可再生成 MATLAB 编译目录和 Python 缓存，共 8,744,555,724 bytes。
- 使用隔离安装自带卸载器清理 14,536 个安装文件，退出码为 0。
- 保留最终 NSIS、vendor、根与前端 node_modules、.venv、所有源码和 topoptpilot/storage；没有触及其他目录中正在运行的 sidecar。