
# TopOptPilot Desktop V6.0 产品与实施方案

## 基于 Pi Agent、Tauri 2 与 MATLAB MCP 的 2D/3D 拓扑优化闭环科研工作台

---

## 📑 快速导航

- [**本地环境配置（克隆到运行）**](#本地环境配置克隆到运行) — 新协作者从零开始的首选入口
- [系统定位](#系统定位)
- [四层架构](#四层架构)
- [目录结构](#目录结构)
- [求解器模块](#求解器模块solver)
- [测试](#测试)
- [开发指南](#开发指南)
- [常见问题](#常见问题faq)

---

---

# 一、产品最终定位

## 1.1 一句话定义

**TopOptPilot 是一个 Windows 原生桌面闭环仿真实验工作台：以 Pi Agent 负责科研规划和结果解释，以真实 2D/3D FEM 负责物理实验，以 Safety Policy、Evaluator 和 Human-in-the-Loop 保证实验安全与科学可信性。**

研究人员输入：

* 几何结构；
* 材料；
* 载荷；
* 边界条件；
* 体积分数；
* 优化目标；
* 计算预算；
* 可选研究假设；

系统完成：

```text
研究任务
   ↓
Pi Agent 规划实验意图
   ↓
Policy Engine 编译安全实验
   ↓
人工审批（必要时）
   ↓
2D / 3D 真实 FEM
   ↓
数据采集
   ↓
确定性 Evaluator
   ↓
Pi 分析实验结果
   ↓
下一轮实验决策
   ↓
达到目标 / 预算耗尽 / 研究停滞
   ↓
研究报告 + 可复现包
```

产品重点不再是“AI 生成科学假设”，而是：

> **AI 能否真正操作拓扑优化虚拟实验仪器，并根据实验数据合理决定下一次实验。**

---

# 二、从现有界面中保留什么，舍弃什么

## 2.1 保留的优点

| 当前设计优点             | 新版如何保留                                      |
| ------------------------ | ------------------------------------------------- |
| 输入、运行、结果同时可见 | 保留单窗口工作台，不在大量页面间跳转              |
| 参数必填/默认状态明显    | 使用参数来源标签：用户设定 / 默认 / AI建议 / 锁定 |
| 顶部能看到模型连接状态   | 升级为真实 Backend Health Bar                     |
| 实验运行步骤可视化       | 升级为真正的 Experiment Pipeline                  |
| 曲线和拓扑结果直接显示   | 作为中央 Experiment Canvas 的核心                 |
| 关键指标直接暴露         | 右侧 Evidence Inspector 持续显示                  |
| 主按钮明确               | 保留少量强操作按钮，避免按钮泛滥                  |

---

## 2.2 明确不继承的问题

新版不再采用：

```text
左边参数表单
+
中间大面积展示
+
右边结果面板
```

这种固定 Dashboard 思路。

原因是科研闭环运行之后，用户最需要看的已经不再是“输入参数”，而是：

```text
实验历史
为什么这样做
发生了什么
下一步为什么这样做
哪些证据支持这个判断
```

因此界面必须从：

> 参数驱动 Dashboard

转变为：

> **实验驱动 Research Workbench**

---

# 三、视觉方向

新版不模仿当前蓝白渐变卡片风格，也不复制 Codex 的视觉语言。

定义新的产品视觉方向：

# Engineering Research Workbench

特点：

* 中性深灰/浅灰双主题；
* 极少装饰性渐变；
* 工程软件式高信息密度；
* 数值采用等宽数字字体；
* 颜色只表达状态；
* topology、curve、evidence 是视觉中心；
* AI 内容不是聊天气泡，而是研究事件；
* 不使用装饰性 AI 大脑、机器人图标占据主视觉。

颜色只承担语义：

```text
正常 / 可行        Green
运行中             Blue
风险 / 待审批      Amber
失败 / 约束违反    Red
人工干预           Purple
未验证             Gray
```

---

# 四、桌面产品总体架构

```text
┌────────────────────────────────────────────────────────────┐
│                  TopOptPilot.exe                           │
│                    Tauri 2 Shell                           │
└──────────────────────┬─────────────────────────────────────┘
                       │
             React / TypeScript UI
                       │
              Local Authenticated API
                       │
                 ResearchService
                  Python Sidecar
                       │
       ┌───────────────┼────────────────┐
       │               │                │
       ▼               ▼                ▼
Research Core      Pi Bridge       Matlab Gateway
       │               │                │
       │          Pi RPC Process        │
       │                                ▼
       │                          MATLAB MCP Server
       │                                │
       │                                ▼
       │                            MATLAB R2024a
       │
       ▼
 Experiment Executor
   ↙       ↓        ↘
Python 2D Python 3D MATLAB F3
       │
       ▼
Deterministic Evaluator
       │
       ▼
Research Memory / SQLite / Files
```

核心原则：

> **Tauri 管桌面生命周期；Python 管科研状态；Pi 管推理；MATLAB MCP 管 MATLAB 会话；Evaluator 管事实。**

任何一个模块都不能越权承担另外一层职责。

---

# 五、正式桌面入口

正式版本取消 Streamlit 入口。

项目启动方式：

```text
TopOptPilot.exe
```

用户只看到一个 Windows 原生应用窗口。

正式版本：

```text
Tauri 2
+
React
+
TypeScript
+
Vite
```

Tauri 官方支持将 Python API Server 等程序作为 external binary/sidecar 打包，因此 Python ResearchService 可以随安装包一起分发，最终用户无需额外安装 Python。

Windows 使用系统 WebView2 渲染应用内容，但不打开外部浏览器。

Streamlit 保留为：

```text
dev:web
```

只用于开发调试。

---

# 六、新版桌面信息架构

整个产品始终只有一个 Research Workspace。

```text
┌──────────────────────────────────────────────────────────────────┐
│ TopOptPilot │ Project MBB-01 │ COPILOT │ ●2D ●Pi ●MATLAB ◐F3   │
├──────────────┬───────────────────────────────────┬───────────────┤
│ Research     │                                   │ Evidence      │
│ Navigator    │        Experiment Canvas          │ Inspector     │
│              │                                   │               │
│ Goal         │                                   │ Current Run   │
│ Constraints  │                                   │ Parameters    │
│              │                                   │ Metrics       │
│ Rounds       │                                   │ Risk          │
│ ├─ R1        │                                   │ Evidence      │
│ │  ├ E01 ✓   │                                   │               │
│ │  ├ E02 ✓   │                                   │ Budget        │
│ │  └ E03 ✕   │                                   │               │
│ └─ R2        │                                   │               │
│              │                                   │               │
│ Decisions    │                                   │               │
│ Evidence     │                                   │               │
│ Reports      │                                   │               │
├──────────────┴───────────────────────────────────┴───────────────┤
│ > 输入研究指令或命令...                         Approve │ Run   │
└──────────────────────────────────────────────────────────────────┘
```

不是传统“三栏看板”。

三个区域各有明确职责：

```text
左侧
= 研究历史

中央
= 当前科研活动

右侧
= 当前对象证据
```

---

# 七、顶部 Backend Health Bar

当前界面的“百炼已连接”思路保留，但重新定义。

禁止显示：

```text
已配置
=
已连接
```

必须区分：

```text
NOT_CONFIGURED
CONFIGURED
STARTING
READY
DEGRADED
FAILED
```

顶部显示：

```text
Pi Agent       ● READY

Qwen           ● READY

Python 2D      ● READY

Python 3D      ● READY

MATLAB MCP     ● READY

MATLAB         ● VERIFIED R2024a
```

点击任一项可以查看：

```text
版本
启动时间
最近 Health Check
最近错误
Restart
Logs
```

因此不会再出现“UI 显示绿色，但实际上 API Key 401”这种错误表达。

---

# 八、Research Navigator

左侧不再长期显示一堆参数输入框。

研究创建之后转化成：

```text
Research MBB-001
│
├─ Goal
├─ Constraints
├─ Hypothesis [optional]
│
├─ Round 1
│  ├─ E01 ✓ Baseline
│  ├─ E02 ✓ beta explore
│  └─ E03 ✕ Disconnected
│
├─ Round 2
│  ├─ E04 ✓
│  └─ E05 ▶
│
├─ Decisions
├─ Evidence
├─ Pareto
├─ Report
└─ Files
```

这样界面天然表达：

> 这是一个持续发展的科研过程，而不是一次参数计算器。

---

# 九、研究创建采用独立 Setup State

参数输入仍然保留当前方案的一个优点：

> 用户能清楚知道哪些参数没有填、哪些来自默认值。

但是只在创建研究时出现。

每个参数显示来源：

```text
USER
DEFAULT
AI SUGGESTED
LOCKED
```

例如：

```text
Volume Fraction
0.35
USER

Young's Modulus
200 GPa
DEFAULT

rmin
2.0
AI SUGGESTED

penal
3
LOCKED
```

用户确认后形成：

```text
Research Contract
```

后续 AI 不允许修改：

* Research Goal；
* 用户锁定参数；
* 不可变工程边界条件。

---

# 十、中央区域改为 Experiment Canvas

中央不再展示与当前实验弱相关的大面积占位图。

Canvas 根据 Research State 自动切换。

主要有五种状态：

```text
PLAN
RUN
ANALYZE
DECIDE
COMPARE
```

---

# 十一、PLAN 状态

展示：

```text
ROUND 2 PLAN

Objective
Restore connectivity while preserving gray improvement

Evidence
E03: beta=16 → gray 2.9%, components=3
E02: beta=8  → gray 4.5%, components=1

Pi Intent
TEST_COMPETING_EXPLANATIONS

Proposed Experiments
E04  decrease beta
E05  increase rmin

Estimated Cost
21–32 s

Risk
LOW
```

下方：

```text
Approve
Edit
Reject
Why
```

这比普通 AI 对话更容易让评委理解系统。

---

# 十二、RUN 状态

实验运行时中央区域优先给科研数据。

布局：

```text
┌───────────────────────┬─────────────────────────┐
│                       │ Compliance              │
│                       │                         │
│      TOPOLOGY         │ █████████ 149.31       │
│                       │                         │
│                       │ Gray        4.3%        │
│                       │ Connected   PASS        │
├───────────────────────┴─────────────────────────┤
│                                                 │
│        Convergence / Gray / Volume / beta       │
│                                                 │
└─────────────────────────────────────────────────┘

Iteration 82 / 100
████████████████░░░░ 82%
```

核心原则：

> 拓扑图和真实收敛历史必须比 AI 文本更醒目。

---

# 十三、ANALYZE 状态

实验结束后自动转变。

例如：

```text
E03 — PARTIAL SUCCESS

Gray
8.1% → 2.9%
✓ improved

Compliance
148.1 → 152.3
△ +2.8%

Connectivity
1 → 3
✕ violated

Evaluator Verdict
INFEASIBLE
```

下面才显示 Pi：

```text
Interpretation

The experiment strongly reduced intermediate density,
but connectivity was lost after projection became aggressive.

Possible explanations

A. beta too high
B. filter radius insufficient
```

顺序必须是：

```text
事实
→
评价
→
AI解释
```

而不是反过来。

---

# 十四、DECIDE 状态

显示整个安全决策链：

```text
PI AGENT

Intent
RESTORE_CONNECTIVITY
        ↓
POLICY ENGINE

Candidate mutation
beta 16 → 8
        ↓
SAFETY GUARD

SAFE
        ↓
BUDGET

Cost acceptable
        ↓
HUMAN GATE

Approval required?
NO
        ↓
READY TO RUN
```

这是比赛中非常有价值的一张界面。

评委能清楚看到：

> AI 没有直接控制物理求解器。

---

# 十五、COMPARE 状态

点击任意两个实验：

```text
E02 vs E03
```

中央显示：

|            |   E02 |   E03 |     Δ |
| ---------- | ----: | ----: | -----: |
| Compliance | 148.1 | 152.3 |  +2.8% |
| Gray       |  4.5% |  2.9% | -35.6% |
| Components |     1 |     3 | Failed |
| beta       |     8 |    16 |     +8 |
| rmin       |   1.5 |   1.5 |      0 |

并排显示 topology。

AI 只负责生成：

```text
Evidence Summary
```

而不是自行计算差值。

---

# 十六、右侧 Evidence Inspector

当前界面的“结果区”思路保留，但升级成：

# Evidence Inspector

根据选择对象改变。

选择实验时：

```text
EXPERIMENT E03

Backend
Python 2D

Fidelity
F1

Mesh
120 × 40

Parameters
beta       16
rmin       1.5
penal      3

Results
Compliance 152.3
Gray       2.9%
Components 3

Evaluator
INFEASIBLE

Failure
DISCONNECTION
```

---

选择 Decision：

```text
DECISION D04

Source
Pi Agent

Intent
RESTORE_CONNECTIVITY

Evidence
E02
E03

Policy
beta 16→8

Safety
PASS

Human
AUTO APPROVED
```

---

选择 MATLAB 实验：

另外显示：

```text
MATLAB
R2024a

MCP
<tested version>

Solver
topopt3d_main.m

File SHA256
...

Execution
REAL MATLAB

Result Source
MATLAB MCP
```

彻底解决“这到底是真 MATLAB 还是 Python 模拟”的可信度问题。

---

# 十七、底部 Command Composer

保留“随时给 AI 指令”的优点，但不做普通聊天框。

用户可以自然语言输入：

```text
为什么这两次实验的连通性不同？
```

也支持确定性命令：

```text
/run
/pause
/approve
/reject
/compare E02 E03
/rollback E02
/lock beta 8
/promote E04
/report
```

规则：

```text
确定性命令
→ ResearchService

科研问题
→ Pi Agent
```

不能所有按钮和命令都绕一遍大模型。

---

# 十八、Pi Agent 架构

Pi 保留为：

# Research Reasoning Kernel

主要职责：

* 理解目标；
* 选择实验意图；
* 解释实验结果；
* 诊断失败；
* 识别竞争解释；
* 提议下一实验；
* 生成研究叙述。

Pi 的 SDK 支持自定义 UI、工具、Session 和事件流；RPC 模式适合跨语言、进程隔离和 IDE/桌面应用嵌入。

正式架构：

```text
Python ResearchService
        │
        ▼
     PiBridge
        │
        ▼
Pi RPC Runtime
```

一项 Research：

```text
=
一个 Pi Session
```

不同 Research 不共享推理历史。

---

# 十九、Pi 不拥有 Research State

必须保持：

```text
Pi Session
=
推理记忆

Research State
=
实验事实
```

Pi 每轮决策前必须调用：

```text
research_get_context
```

不能因为 Session 中“记得 E03 的柔度是 152.3”就直接使用。

正式事实必须重新从 Research State 获取。

---

# 二十、Pi Tool 白名单

比赛模式下 Pi 只获得：

```text
research_get_context

research_query_history

research_get_budget

policy_compile_intent

experiment_preview

experiment_submit

experiment_status

experiment_result

experiment_compare

failure_get_evidence

research_get_pareto
```

禁用：

```text
bash

write

edit

shell

arbitrary MATLAB

database mutation

raw solver invocation
```

因此 Pi 是：

> 一个拥有科研工具的智能体。

而不是：

> 一个拥有电脑控制权限的 Coding Agent。

---

# 二十一、Research Memory

继续采用四层结构：

```text
L0
Raw Physics Data
density / convergence / MATLAB logs

↓
L1
Experiment Records
config / result / backend

↓
L2
Scientific Summary
trend / Pareto / failures / best

↓
L3
Decision Context
goal / relevant evidence / budget

↓
Pi
```

Pi 默认只读取 L3。

需要查看 E03 时：

```text
research_query_history
```

按需读取。

Pi 自身的 Session Compaction 只负责压缩聊天/推理历史，不能代替 Research Memory。Pi Session 本身支持持久化和继续会话。

---

# 二十二、MATLAB MCP 的正确安全架构

这里对原计划做一个重要修改。

不要：

```text
Pi
↓
MATLAB MCP
```

必须：

```text
Pi
↓
experiment_submit
↓
Python Executor
↓
MatlabGateway
↓
MATLAB MCP
↓
MATLAB
```

原因是官方 MATLAB MCP 自带：

```text
evaluate_matlab_code
run_matlab_file
...
```

同时也支持通过 extension file 增加自定义 MATLAB Function 工具。

因此：

> **定义一个安全 custom tool 并不会自动删除危险 built-in tools。**

真正的安全边界必须位于：

# MatlabGateway

---

# 二十三、MatlabGateway

唯一允许 Executor 调用：

```text
run_topopt_task(task_path, result_path)
```

Gateway 内部才调用 MATLAB MCP：

```text
topopt_run_task
```

Pi 根本不知道：

```text
evaluate_matlab_code
```

这个工具存在。

Gateway 验证：

```text
路径位于当前 Research Root

文件后缀合法

Task Schema 合法

参数范围合法

实验已有 Approval

Fidelity = F3
```

任一失败：

```text
DENY
```

---

# 二十四、MATLAB Custom Tool

MCP Extension 只定义一个项目工具：

```text
topopt_run_task
```

参数：

```text
task_json_path
result_json_path
```

MATLAB 包装层：

```text
topopt_run_task.m
```

只做：

```text
JSON Read
↓
Schema Mapping
↓
调用既有求解主函数
↓
收集结果
↓
JSON Write
```

严禁复制和改写原求解算法。

---

# 二十五、2D / 3D MATLAB 路径

MATLAB Adapter 根据受控 task 字段分派：

```text
2D MATLAB Verification
→ topopt_main.m

F3 3D
→ topopt3d_main.m
```

原：

* FE；
* OC；
* Filter；
* Boundary；
* Stress；

模块继续作为权威实现。

---

# 二十六、MatlabMcpWorker

MATLAB 不按实验重新启动。

启动 Desktop：

```text
TopOptPilot
↓
MatlabMcpWorker
↓
MATLAB MCP
↓
Persistent MATLAB Session
```

实验：

```text
E11
↓
Queue
↓
Current MATLAB Session
```

只允许：

```text
max_matlab_workers = 1
```

避免：

* License 冲突；
* 高内存并发；
* 多 MATLAB 会话不可控。

---

# 二十七、严格 F3 语义

定义：

```text
F0 = Python 2D coarse
F1 = Python 2D fine
F2 = Python 3D coarse
F3 = REAL MATLAB 3D
```

F3 永远不能：

```text
MATLAB Failed
↓
偷偷改 Python
↓
显示 SUCCESS
```

必须：

```text
FAILED
MATLAB_INFRASTRUCTURE
```

保存：

```text
error
MATLAB version
MCP version
stderr
timestamp
task hash
```

用户看到：

```text
Restart MATLAB
Retry Experiment
Skip F3
```

---

# 二十八、MATLAB MCP 版本管理修正

原计划直接写死：

```text
v0.12.0
```

不建议写死在产品规范里。

发布时使用：

```text
MATLAB MCP tested release
+
binary SHA256
```

例如：

```yaml
matlab_mcp:
  version: <release-tested-version>
  sha256: ...
```

原因是版本号是外部依赖状态，而真正重要的是：

> **你发布的软件到底验证了哪一个二进制。**

安装包和最终研究日志均记录实际版本。

---

# 二十九、MATLAB 许可证约束

官方 MATLAB MCP 文档说明它必须按照 MathWorks 软件许可协议与 MATLAB 一起使用，并明确不允许将 MCP Server 作为多用户共享/集中式服务器使用。

因此 TopOptPilot 的：

> 单机 Windows Desktop + 本机 MATLAB

架构是正确方向。

首版不要部署成：

```text
一台 MATLAB Server
↓
几十个客户端共享
```

---

# 三十、MATLAB 安装策略

TopOptPilot 安装包：

```text
不包含 MATLAB 本体
```

首次启动：

```text
Detect MATLAB
↓
Check Version
↓
Check License
↓
Check MCP
↓
Run Minimum Test
↓
VERIFIED
```

首个签署环境：

```text
Windows 10/11 x64
MATLAB R2024a
```

官方 MATLAB MCP 当前文档支持 R2021a 或之后的近五年版本，但正式 release 只对实际做过测试的环境声明 `VERIFIED`。

---

# 三十一、桌面 Sidecar 设计

Tauri 启动：

```text
research-service.exe
pi-runtime
```

MATLAB MCP 由：

```text
research-service
```

按需启动。

正式用户无需：

```text
Python
Node.js
Rust
```

开发环境仍然需要：

```text
Rust
Node
Python
```

---

# 三十二、本地 API 安全

Python API：

```text
127.0.0.1
random_port
```

禁止：

```text
0.0.0.0
```

Tauri 启动时生成：

```text
256-bit session token
```

通过环境变量传给 sidecar。

不要：

```text
将 token 打印进普通日志
```

HTTP：

```text
Authorization: Bearer ...
```

WebSocket 不长期使用 URL Query Token。

采用：

```text
HTTP authenticated request
↓
short-lived WS ticket
↓
WS connect
↓
ticket consumed
```

降低 token 暴露风险。

---

# 三十三、事件系统

统一定义 Event Envelope：

```json
{
  "event_id": "...",
  "research_id": "...",
  "timestamp": "...",
  "type": "...",
  "source": "...",
  "payload": {}
}
```

主要类型：

```text
AGENT_MESSAGE

AGENT_TOOL_CALL

POLICY_DECISION

SAFETY_RESULT

HUMAN_DECISION

EXPERIMENT_QUEUED

EXPERIMENT_PROGRESS

EXPERIMENT_RESULT

EVALUATOR_RESULT

MATLAB_STATUS

FAILURE

REPORT_READY
```

Tauri 只消费统一 Event。

不要分别为：

```text
Pi
MATLAB
Python
```

写三套 UI 数据结构。

---

# 三十四、WebSocket

正式 Workspace 不轮询。

使用：

```text
WS /api/research/{id}/stream
```

推送：

```text
Pi token
Agent event
Policy event
Experiment progress
MATLAB lifecycle
Evaluator result
Human approval state
```

这样能够真正达到桌面科研工具的实时感。

---

# 三十五、中英文系统

数据库和后端枚举永远使用稳定英文 Code：

```text
DISCONNECTION
GOAL_ACHIEVED
MATLAB_INFRASTRUCTURE
RESTORE_CONNECTIVITY
```

UI 层翻译：

```text
zh-CN:
结构断连

en-US:
Disconnection
```

这样避免换语言以后破坏历史数据。

---

# 三十六、Locale 分三层

## Global Locale

应用设置：

```text
zh-CN
```

首次默认简体中文。

---

## Research Locale

新 Research 创建时复制：

```text
locale = zh-CN
```

以后可以独立切换。

---

## Agent Locale

每次给 Pi 的当前 System Context 包含：

```text
response_locale
```

因此切换英文后：

```text
后续 Agent 回复
后续失败解释
后续报告
```

使用英文。

历史回答不强制重翻译。

---

# 三十七、正式模型策略

当前界面顶部直接显示：

```text
qwen-plus
```

不建议作为比赛正式路径。

正式 Release 应区分：

```text
MODEL
Qwen Open-Weight

PROVIDER
Local / Remote
```

比赛主验收路径优先使用：

```text
Qwen 开源权重
+
本地兼容服务
```

DashScope/qwen-plus 可以保留：

```text
DEV / FALLBACK
```

但不要让界面把：

> 能连商业 API

等价成：

> 已满足国产开源模型要求。

---

# 三十八、Human-in-the-Loop

三种模式继续保留。

## AUTOPILOT

允许：

```text
F0
F1
```

在安全策略内自动执行。

---

## COPILOT

默认。

每轮：

```text
AI Recommendation
Expected Purpose
Estimated Runtime
Risk
```

用户：

```text
Approve
Edit
Reject
Why
```

---

## EXPERT

允许：

```text
Lock Parameters
Force Experiment
Rollback
Change Budget
Disable Strategy
Promote Fidelity
```

所有人工动作进入 Research Log。

---

# 三十九、F3 必须人工审批

F3 永远：

```text
HUMAN_APPROVAL_REQUIRED
```

审批卡：

```text
MATLAB 3D HIGH-FIDELITY EXPERIMENT

Purpose
Verify 2D/3D transfer

Estimated Runtime
3–5 min

Remaining F3 Budget
1

MATLAB
R2024a VERIFIED

MCP
READY

[Approve]
[Edit]
[Cancel]
```

---

# 四十、Cache 与 Replay

Cache 保存的只能是：

> 真实运行结果。

必须区分：

```text
LIVE REAL RUN

CACHED REAL RESULT

VERIFIED REPLAY
```

UI 三种状态必须明显不同。

绝不使用：

```text
模拟数据
```

伪装真实 FEM。

---

# 四十一、三类比赛案例

## Case A：完整自主闭环

目标：

```text
Compliance ↓
Gray < 5%
Connected
Volume ≤ 40%
```

Pi：

```text
Baseline
→ Explore
→ Refine
→ Goal Achieved
```

证明：

> AI 可以自己选择下一实验。

---

## Case B：失败驱动实验

某实验：

```text
Gray ↓
Connectivity FAIL
```

Pi 提出：

```text
beta 太高
vs
rmin 太低
```

DOE：

```text
E08 改 beta
E09 改 rmin
```

真实 FEM 给出新证据。

这是主 Demo。

---

## Case C：2D → F3

```text
F0
↓
F1
↓
F2
↓
F3 MATLAB
```

证明：

> 系统能够管理昂贵实验资源，并调用真实 MATLAB。

---

# 四十二、Baseline

最终比较：

```text
Random Search

Grid Search Reference

Bayesian / TPE

Rule-based Feedback

TopOptPilot + Pi
```

核心指标：

# Best Feasible Objective under Fixed High-Fidelity Budget

辅助：

```text
Experiments-to-Feasible

High-Fidelity Runs

Constraint Violation Rate

Best Compliance

Gray Ratio

Human Interventions

Final Regret

Invalid AI Intent Rate

Safety Rejection Rate
```

不要主打：

```text
总 Wall Clock 比 Grid 快
```

因为便宜小空间 Grid Search 很可能比 LLM 更快。

---

# 四十三、项目目录

```text
topoptpilot/
│
├── desktop/
│   ├── src/
│   │   ├── workspace/
│   │   ├── navigator/
│   │   ├── inspector/
│   │   ├── experiment/
│   │   ├── compare/
│   │   ├── report/
│   │   └── i18n/
│   │
│   ├── src-tauri/
│   └── package.json
│
├── service/
│   ├── api/
│   ├── websocket/
│   └── research_service.py
│
├── agent_runtime/
│   ├── pi_bridge.py
│   ├── session_manager.py
│   └── event_mapper.py
│
├── .pi/
│   ├── extensions/
│   ├── skills/
│   └── prompts/
│
├── orchestrator/
│
├── policy/
│   ├── intent_compiler.py
│   ├── safety_guard.py
│   ├── approval_policy.py
│   └── doe_templates.py
│
├── memory/
│
├── executor/
│
├── evaluator/
│
├── solver/
│   ├── python2d/
│   ├── python3d/
│   └── matlab/
│       ├── gateway.py
│       ├── mcp_worker.py
│       ├── extensions/
│       │   └── topopt-tools.json
│       └── wrappers/
│           └── topopt_run_task.m
│
├── storage/
├── reports/
├── tests/
└── dev/
    └── streamlit/
```

---

# 四十四、技术栈

| 层            | 技术                                |
| ------------- | ----------------------------------- |
| Desktop Shell | Tauri 2                             |
| Desktop UI    | React + TypeScript                  |
| Build         | Vite                                |
| Research Core | Python                              |
| Local API     | FastAPI                             |
| Realtime      | WebSocket                           |
| Agent Runtime | Pi Agent RPC                        |
| Model         | Qwen 开源权重优先                   |
| Schema        | Pydantic                            |
| State DB      | SQLite                              |
| Raw Data      | Filesystem                          |
| 2D FEM        | Python                              |
| F2            | Python 3D                           |
| F3            | MATLAB                              |
| MATLAB Bridge | Official MATLAB MCP + MatlabGateway |
| Charts        | Plotly/ECharts                      |
| 3D Viewer     | Three.js / vtk.js 二选一            |
| i18n          | i18next                             |
| Packaging     | Tauri Installer + Sidecars          |

---

# 四十五、15 天实施计划

## Day 1 — 冻结协议

冻结：

```text
Research State
Experiment Schema
Result Schema
Event Schema
Pi Tool Contract
MATLAB Task Contract
Safety Rules
Fidelity Semantics
```

同时冻结 UI Information Architecture。

---

## Day 2 — Python 2D Gate

必须跑通：

```text
真实 MBB
SIMP
OC
```

并产生统一 ExperimentResult。

### Gate 1

失败则不开发复杂前端。

---

## Day 3 — 2D 完整能力

完成：

```text
Filter
Projection
Metrics
Failure Detector
```

---

## Day 4 — ResearchService

建立：

```text
Research API
SQLite
Experiment Queue
Event Bus
WebSocket
```

---

## Day 5 — Pi

接通：

```text
Pi RPC
Research Context
Policy Intent
Experiment Submit
Result Read
```

只做最小 Tool Set。

---

## Day 6 — Safety + Memory

完成：

```text
Policy Compiler
Safety Guard
L0-L3 Memory
Budget
Termination
```

---

## Day 7 — 真闭环

必须完成：

```text
Pi
→ Experiment
→ Real FEM
→ Evaluator
→ Pi
→ New Experiment
```

第二轮参数不能人工指定。

### Gate 2

这是比赛核心门槛。

---

## Day 8 — Tauri Skeleton

完成：

```text
Desktop Shell
Backend Health
Research Navigator
Experiment Canvas
Inspector
Command Composer
```

不追求美术。

---

## Day 9 — 实时 Workspace

接入：

```text
WebSocket
Pi events
Experiment Progress
Topology
Convergence
Approval
```

---

## Day 10 — MATLAB MCP

只做：

```text
MCP Process
Persistent MATLAB
MatlabGateway
topopt_run_task
Minimum 2D MCP Test
```

当天如果不能稳定：

停止扩展，只定位问题。

---

## Day 11 — F3

真实运行一个小型：

```text
MATLAB 3D
```

完成严格失败路径。

### Gate 3

必须能证明：

```text
F3 = MATLAB
```

---

## Day 12 — Fidelity + DOE

加入：

```text
F0 → F1 → F2 → F3

beta vs rmin
```

模板控制实验。

---

## Day 13 — Baseline + 正式案例

运行：

```text
Case A
Case B
Case C

Random
Grid
Rule
TPE
TopOptPilot
```

冻结所有科学评价口径。

### Gate 4

从当天开始禁止修改结果判据。

---

## Day 14 — UI / 双语 / 打包

完成：

```text
中英文
Compare
Report
Installer
Reproduction Bundle
PPT
Demo Video
```

---

## Day 15 — Release Freeze

只执行：

```text
Fresh Install

Windows 10 Test

Windows 11 Test

Pi Restart

Qwen Failure

MATLAB License Failure

MCP Kill

MCP Restart

F3 Retry

Cache Miss

Language Restart

Demo Rehearsal
```

禁止新增功能。

---

# 四十六、Release Gate

最终发布必须全部通过：

```text
desktop_app

python_2d_real

pi_closed_loop

safety_policy

research_memory

matlab_mcp_2d

matlab_mcp_3d

strict_f3

human_approval

websocket_realtime

i18n_zh_default

reproducibility
```

任一硬门禁失败：

```text
release_ready = false
```

---

# 四十七、10 分钟 Demo

## 0:00–1:00

启动：

```text
TopOptPilot.exe
```

不出现浏览器。

Backend Health：

```text
Pi ✓
Qwen ✓
Python ✓
MATLAB MCP ✓
MATLAB ✓
```

---

## 1:00–2:00

创建 Research。

用户只输入：

```text
Geometry
Boundary
Volume
Goal
Budget
```

确认 Research Contract。

---

## 2:00–3:00

Pi 提出第一轮实验。

中央显示：

```text
Intent
Evidence
Estimated Cost
Safety
```

---

## 3:00–4:00

运行真实 FEM。

Canvas 显示：

```text
Topology
Convergence
Metrics
```

---

## 4:00–5:00

出现断连。

Evaluator：

```text
INFEASIBLE
DISCONNECTION
```

Pi 给出两个解释。

---

## 5:00–6:00

DOE 生成两个控制实验。

展示：

```text
Pi
↓
Policy
↓
Safety
↓
Experiment
```

完整决策链。

---

## 6:00–7:10

第二轮实验产生证据。

AI 更新判断。

---

## 7:10–8:00

系统提升到 F2 Python 3D。

---

## 8:00–9:00

提出：

```text
F3 MATLAB 3D
```

显示：

```text
MATLAB version
MCP status
estimated time
remaining budget
```

用户审批。

现场可运行极小真实 F3，正式结果可同时展示此前验证实验。

---

## 9:00–9:35

Compare：

```text
Baseline
vs
Final
```

---

## 9:35–10:00

生成：

```text
Research Report
+
Reproduction Bundle
```

切换英文，展示固定 UI 和下一条 Pi 回复同步变成英文。

---

# 四十八、比赛时最终只讲四项创新

## 1. AI 真正执行科研闭环

```text
Plan
→ Run
→ Observe
→ Decide
→ Run
```

---

## 2. 意图—策略—物理解耦

```text
Pi
决定研究意图

Policy
决定合法实验

FEM
产生物理事实

Evaluator
进行客观裁决
```

---

## 3. 多保真 2D/3D 实验预算

```text
cheap evidence
→
expensive evidence
```

只在值得的时候调用 MATLAB F3。

---

## 4. 失败成为科研证据

```text
Failed Experiment
↓
Structured Evidence
↓
Diagnosis
↓
Controlled Experiment
↓
New Evidence
```

---

# 四十九、正式产品价值主张

TopOptPilot 不再被描述为：

> 一个会聊天、会生成实验建议的 AI 工具。

而定义为：

> **一个面向拓扑优化研究人员的原生科研实验工作台。它把原本分散在 MATLAB、Python、参数表、实验日志和人工经验中的“设计—运行—观察—诊断—调整—验证”流程，统一成一个受物理约束、可人工控制、能够真实调用 2D/3D 求解器并持续利用实验结果进行反馈迭代的 AI 科研闭环。**

最终向评委证明的核心命题：

> **在不给 AI 人工指定下一组实验参数的情况下，TopOptPilot 能否依据真实实验结果和研究预算，安全地决定下一次拓扑优化实验，并通过连续的 2D/3D 仿真实验逐步形成更好的可行方**

# TopOptPilot 产品方案补充修订

## V4.1 — Streamlit Codex-like 本地科研工作台

---

# 一、前端技术路线调整

原方案：

```text
Gradio
+
FastAPI
```

调整为：

```text
Streamlit Local Workspace
+
FastAPI Test API
```

其中：

* **Streamlit**：产品唯一主交互界面；
* **FastAPI**：只承担赛题要求的可调用测试 API、外部自动化测试和程序接口；
* 不开发独立网页；
* 不开发 React / Vue；
* 不使用 Gradio；
* 不进行复杂前后端分离；
* 所有科研交互集中在一个 Streamlit 工作台中完成。

TopOptPilot 的交互形式不再设计成传统 Dashboard，而是设计成类似 Codex 的：

> **AI 工作区 + 实验终端 + 文件/任务上下文 + 实时执行状态**

---

# 二、产品界面定位

TopOptPilot 不应该看起来像：

```text
传统数据看板
├── 图表
├── 表格
├── 参数表单
└── 一堆按钮
```

而应该更接近：

```text
科研任务工作区
│
├── 当前研究上下文
├── AI 对话 / 决策流
├── 实验执行过程
├── 仿真结果
├── 项目文件
└── 可随时人工接管的命令区
```

核心体验：

> **研究人员像使用 Codex 操作代码项目一样，通过一个统一 Workspace 与 TopOptPilot 协作完成拓扑优化研究。**

---

# 三、Codex-like 总体布局

界面采用三栏 + 底部输入区布局：

```text
┌──────────────────────────────────────────────────────────────────────────────┐
│ TopOptPilot                     Research: MBB-001       ● Solver Ready       │
├─────────────────┬────────────────────────────────────┬───────────────────────┤
│                 │                                    │                       │
│  Research       │          Main Workspace            │     Inspector         │
│  Explorer       │                                    │                       │
│                 │                                    │                       │
│ ▾ MBB-001       │  USER                              │  Current Experiment   │
│   Goal          │  Minimize compliance while...      │                       │
│   Constraints   │                                    │  E07                  │
│                 │  TOPOPTPILOT                       │  2D Fine              │
│ ▾ Experiments   │  I will first run three coarse     │                       │
│   ✓ E01         │  experiments to determine...       │  beta       8         │
│   ✓ E02         │                                    │  rmin       2.0       │
│   ✗ E03         │  ┌──────────────────────────────┐  │  penal      3         │
│   ▶ E04         │  │ EXPERIMENT E04              │  │                       │
│                 │  │ ███████████████░░ 78%       │  │  Compliance 149.3    │
│ ▾ Evidence      │  │ Iteration 76 / 100          │  │  Gray       4.3%     │
│                 │  └──────────────────────────────┘  │  Connected  PASS     │
│ ▾ Files         │                                    │                       │
│   research.yml  │  ANALYSIS                          │  Safety               │
│   metrics.csv   │  Grayness improved, but...         │  ● SAFE               │
│   report.md     │                                    │                       │
│                 │  NEXT ACTION                       │  Budget               │
│                 │  RESTORE_CONNECTIVITY              │  7 / 12              │
│                 │                                    │                       │
├─────────────────┴────────────────────────────────────┴───────────────────────┤
│ > Ask TopOptPilot or enter command...                         [ Run / Send ] │
└──────────────────────────────────────────────────────────────────────────────┘
```

---

# 四、左侧：Research Explorer

左侧不是传统导航菜单，而是研究项目树。

展示完整科研上下文。

```text
Research
│
├── Goal
├── Constraints
├── Hypothesis       optional
│
├── Experiments
│   ├── E01 ✓
│   ├── E02 ✓
│   ├── E03 ✗
│   └── E04 ▶
│
├── Evidence
│   ├── Trends
│   ├── Failures
│   └── Pareto Front
│
├── Decisions
│
└── Files
    ├── research.yml
    ├── metrics.csv
    ├── decisions.jsonl
    └── report.md
```

### 状态符号

```text
○ Waiting

▶ Running

✓ Success

✗ Failed

⚠ Warning

◆ Best Candidate
```

点击任意实验，例如：

```text
E07
```

中央工作区自动切换到该实验上下文。

---

# 五、中央：Main Research Workspace

这是整个产品最重要的区域。

不采用传统 ChatBot UI。

而采用：

> **Research Activity Stream**

即 AI 对话、实验执行、系统事件、分析结果共同组成一条时间流。

---

# 六、Workspace 事件类型

每条记录必须明确来源。

## User

```text
USER

Find a topology satisfying:
volume ≤ 40%
gray ≤ 5%
connected = true
```

---

## Planner

```text
PLANNER

Round 1 Strategy

I will use three F0 experiments to establish
the effect of beta before spending the F1 budget.
```

---

## Safety Engine

```text
SAFETY POLICY

Proposal:
beta 8 → 16

Risk:
MEDIUM

Reason:
High beta combined with current rmin may
increase disconnection risk.

Status:
SAFE WITH WARNING
```

---

## Experiment

```text
EXPERIMENT E04

2D Fine
120 × 40

████████████████░░░ 82%

Iteration:
82 / 100

Compliance:
149.31

Gray:
4.7%
```

---

## Analyst

```text
ANALYSIS

Gray Ratio
8.1% → 3.9% ✓

Compliance
148.1 → 151.7 △

Connectivity
1 → 3 ✗

Result:
PARTIAL SUCCESS
```

---

## Feedback

```text
NEXT DECISION

RESTORE_CONNECTIVITY

Reason:
E07 reduced grayness but violated the
connectivity constraint.
```

---

## Human

人工干预也进入 Timeline：

```text
HUMAN OVERRIDE

AI proposed:
beta = 12

User changed:
beta = 8

Reason:
Manual expert adjustment
```

---

# 七、不要暴露大模型 Chain-of-Thought

中央 Workspace 显示：

* Observation；
* Evidence；
* Decision；
* Reason Summary；
* Expected Purpose。

不显示大模型内部长篇推理过程。

例如展示：

```text
Reason:

Two recent high-beta experiments caused
connectivity failure while lower-beta runs
remained connected.
```

而不是展示内部逐 Token 推理。

这样界面更接近科研系统，而不是普通聊天机器人。

---

# 八、右侧：Inspector

右侧永远显示当前选中对象的结构化信息。

例如选中 E07：

```text
EXPERIMENT E07

Status
FAILED

Fidelity
F1 — 2D Fine

Mesh
120 × 40

Parameters
beta       16
rmin       1.5
penal      3
volfrac    0.4

────────────

Results

Compliance
152.1

Gray
2.9%

Volume Error
0.4%

Components
3

Runtime
18.4 s

────────────

Safety
MEDIUM

────────────

Initialization
Warm Start: E04
```

这样：

* 中间负责“发生了什么”；
* 右边负责“具体数据是多少”。

避免中央区域被参数表格淹没。

---

# 九、Inspector 根据对象动态变化

如果选中 Research：

显示：

```text
Goal
Constraints
Budget
Current Best
Termination Status
```

如果选中 Experiment：

显示：

```text
Configuration
Metrics
Safety
Runtime
```

如果选中 Decision：

显示：

```text
Intent
Evidence
Policy Result
Human Approval
```

如果选中 Failure：

显示：

```text
Failure Type
Evidence
Possible Causes
Related Experiments
```

---

# 十、底部 Command Bar

整个界面最下方始终存在输入区：

```text
> Ask TopOptPilot or enter command...
```

用户既可以输入自然语言：

```text
为什么 E07 会发生断连？
```

也可以执行命令：

```text
/run
```

```text
/pause
```

```text
/approve
```

```text
/reject
```

```text
/rollback E06
```

```text
/lock beta 8
```

```text
/unlock beta
```

```text
/compare E04 E07
```

```text
/promote E06
```

```text
/report
```

这会使 TopOptPilot 更像科研 IDE，而不是网页应用。

---

# 十一、命令系统

正式定义：

```text
/run
运行当前批准实验

/pause
暂停闭环

/resume
继续研究

/stop
终止研究

/approve
批准 AI 当前实验建议

/reject
拒绝 AI 当前建议

/edit
编辑当前实验

/rollback <experiment>
回退到指定实验状态

/compare <A> <B>
比较实验

/lock <parameter> <value>
锁定参数

/unlock <parameter>
解除参数锁定

/promote <experiment>
提升实验保真度

/retry <experiment>
重新执行基础设施失败实验

/report
生成研究报告

/export
导出复现包
```

自然语言与 Command 共用同一个输入区域。

---

# 十二、Human-in-the-Loop 在界面中的实现

当系统运行在 Copilot 模式时：

AI 给出建议：

```text
PROPOSED EXPERIMENT

Purpose:
Restore connectivity while preserving
the grayness improvement.

Changes:

beta
16 → 8

rmin
1.5 → 2.0

Estimated Runtime
12–18 s

Risk
LOW
```

直接在中央区域出现：

```text
[ Approve ] [ Edit ] [ Reject ] [ Why? ]
```

不是跳转到另一个设置页面。

---

# 十三、昂贵 3D 实验审批

例如：

```text
HIGH-FIDELITY EXPERIMENT

MATLAB 3D

Estimated runtime
3–5 min

Remaining HF Budget
1 / 1

Purpose
Verify whether the best 2D policy
transfers to 3D.

[ Approve Run ]

[ Use Python 3D First ]

[ View Cached Verified Run ]

[ Cancel ]
```

用户始终清楚知道：

* 为什么要跑；
* 要等多久；
* 花多少预算。

---

# 十四、实验运行界面

Experiment Executor 通过 Queue 将状态实时写入 Streamlit Session。

例如：

```text
E04  ▶  82%
E05  ▶  61%
E06  ○  Waiting
```

中央实验卡实时更新：

```text
Iteration
82 / 100

Compliance
149.2

Gray
4.1%

Runtime
11.8 s
```

---

# 十五、中间结果实时可视化

当前运行实验可以显示三个主要 Tab：

```text
[ Topology ] [ Convergence ] [ Metrics ]
```

## Topology

直接显示当前 density field。

---

## Convergence

显示：

```text
Compliance

Gray Ratio

Volume

beta
```

随 iteration 的变化。

---

## Metrics

显示最终结构化指标。

不需要打开新页面。

---

# 十六、Research Timeline

Timeline 不单独设置成传统网页。

而是中央 Workspace 的一种 View：

```text
CHAT | TIMELINE | COMPARE
```

---

## Timeline View

```text
ROUND 1
│
├─ E01 ✓ Baseline
├─ E02 ✓ beta=4
└─ E03 ✓ beta=8
       │
       ▼
   EXPAND
       │
ROUND 2
│
├─ E04 ✓
└─ E05 ✗ DISCONNECTED
       │
       ▼
 TEST COMPETING EXPLANATIONS
       │
ROUND 3
│
├─ E06 ✓ beta↓
└─ E07 ✗ rmin↑
       │
       ▼
  Evidence supports:
  excessive beta
```

---

# 十七、Compare View

用户输入：

```text
/compare E04 E07
```

中央区域切换：

```text
              E04             E07

Topology      [image]         [image]

Compliance    149.2           152.1

Gray          4.4%            2.9%

Connected     YES             NO

beta          8               16

rmin          1.5             1.5
```

下面自动生成：

```text
ANALYSIS

Increasing beta reduced grayness by 34%,
but caused connectivity failure.
```

---

# 十八、Workspace Session 设计

Streamlit 使用：

```python
st.session_state
```

管理前端状态。

核心状态：

```text
selected_research

selected_experiment

workspace_view

current_round

execution_status

approval_pending

selected_compare_runs

chat_history

ui_mode
```

但真正的 Research State 不存放在 Session 中。

---

# 十九、前端状态与科研状态分离

架构：

```text
Streamlit Session State
        │
        │ UI only
        ↓
Research Orchestrator
        │
        ↓
SQLite / Filesystem
```

Streamlit 只保存：

* 当前选中实验；
* 当前页面状态；
* 输入框；
* UI 展开状态。

真正科研记录保存在：

```text
Research State Store
```

因此即使 Streamlit 刷新：

> 科研实验不会丢失。

---

# 二十、后台任务设计

绝不能让 Streamlit 主线程直接长时间执行 FEM。

采用：

```text
Streamlit
    ↓
Experiment Queue
    ↓
Background Worker
    ↓
FEM
```

Streamlit 只负责：

```text
submit
poll
render
```

原有 ExperimentQueue 可以直接复用。

---

# 二十一、后台执行模型

建议：

```text
2D Solver
↓
ProcessPoolExecutor

Python 3D
↓
ProcessPoolExecutor

MATLAB
↓
Persistent MATLAB Worker
```

状态统一：

```text
WAITING

RUNNING

SUCCESS

FAILED

CANCELLED
```

Streamlit 定时获取最新状态。

---

# 二十二、刷新策略

避免整个页面频繁重绘。

优先刷新：

```text
Experiment Progress

Metrics

Topology Preview

Queue Status
```

而：

```text
Research Explorer

Historic Messages

Completed Experiments
```

只在状态变化时更新。

目标是让 Streamlit 的体验更接近 IDE，而不是不断闪烁的 Dashboard。

---

# 二十三、Streamlit 组件映射

推荐：

| 功能          | Streamlit                               |
| ------------- | --------------------------------------- |
| 左栏          | `st.sidebar`                          |
| 主工作区      | `st.container`                        |
| 三栏          | `st.columns`                          |
| Command Input | `st.chat_input`                       |
| 对话/事件     | `st.chat_message` + custom containers |
| 实验展开      | `st.expander`                         |
| 状态          | `st.status`                           |
| 进度          | `st.progress`                         |
| Metrics       | `st.metric`                           |
| Tab           | `st.tabs`                             |
| 图形          | `st.pyplot` / `st.plotly_chart`     |
| 配置编辑      | `st.form`                             |
| 文件          | `st.download_button`                  |

---

# 二十四、视觉风格

界面风格不要做成传统蓝色 BI Dashboard。

目标：

> **Dark Engineering Workspace**

特点：

```text
深色背景

低饱和度

代码编辑器式层级

紧凑信息密度

实验状态用少量颜色区分

大面积留给 Research Stream
```

参考感觉：

```text
Codex
+
VS Code
+
实验日志终端
```

而不是：

```text
企业 BI Dashboard
```

---

# 二十五、顶部 Status Bar

顶部始终显示：

```text
TopOptPilot

Project:
MBB-001

Mode:
COPILOT

Solver:
● 2D
● Python 3D
○ MATLAB

Budget:
7 / 12

Status:
RUNNING
```

这部分要做到一眼可见。

---

# 二十六、应用启动方式

比赛版本不设计网站首页。

用户直接：

```bash
streamlit run app.py
```

进入科研工作台。

进一步提供：

```bash
python launch.py
```

由 Launcher 自动：

```text
1. 检查 Python 环境
2. 检查 Solver
3. 启动后台 Worker
4. 启动 Streamlit
5. 打开 TopOptPilot Workspace
```

用户体验：

```text
双击 TopOptPilot
↓
Workspace
```

---

# 二十七、如果最终要求完全不显示浏览器

Streamlit 本身依赖浏览器渲染。

如果最终希望视觉上成为真正独立桌面程序，可在赛后或时间充足时增加：

```text
PyWebView
    ↓
Local Streamlit
```

或者：

```text
Streamlit
+
Desktop Wrapper
```

最终用户看到：

```text
TopOptPilot.exe
```

而不是浏览器标签页。

但该功能属于：

```text
P2
```

不能影响 15 天核心开发。

---

# 二十八、FastAPI 的重新定位

FastAPI 不用于驱动 UI。

它只用于：

### 赛题测试接口

例如：

```text
GET /api/health

POST /api/research

GET /api/research/{id}

POST /api/experiments

GET /api/experiments/{id}

POST /api/decision/{id}/approve

GET /api/report/{id}
```

架构变成：

```text
               Research Core
                 ↑       ↑
                 │       │
          Streamlit    FastAPI
           Human UI    Test/API
```

Streamlit 和 API 调用同一个：

```text
ResearchService
```

禁止写两套业务逻辑。

---

# 二十九、代码架构相应修改

最终工程目录：

```text
topoptpilot/
│
├── app/
│   │
│   ├── streamlit_app.py
│   │
│   ├── workspace/
│   │   ├── explorer.py
│   │   ├── research_stream.py
│   │   ├── inspector.py
│   │   ├── command_bar.py
│   │   ├── timeline.py
│   │   └── compare.py
│   │
│   ├── components/
│   │   ├── experiment_card.py
│   │   ├── decision_card.py
│   │   ├── failure_card.py
│   │   ├── topology_view.py
│   │   └── status_bar.py
│   │
│   └── theme/
│       └── style.css
│
├── api/
│   └── fastapi_app.py
│
├── service/
│   └── research_service.py
│
├── orchestrator/
│   └── research_orchestrator.py
│
├── agent/
│   ├── planner.py
│   ├── analyst.py
│   └── feedback.py
│
├── policy/
│   ├── action_policy.py
│   ├── safety_guard.py
│   ├── approval_policy.py
│   └── doe_templates.py
│
├── memory/
│   ├── research_state.py
│   ├── compressor.py
│   └── retriever.py
│
├── executor/
│   ├── executor.py
│   ├── queue.py
│   └── cache.py
│
├── solver/
│   ├── topopt2d/
│   ├── topopt3d_python/
│   └── matlab_adapter/
│
├── evaluator/
│   ├── metrics.py
│   ├── failure_detector.py
│   └── evaluator.py
│
├── fidelity/
│   └── manager.py
│
├── schemas/
│
├── storage/
│
├── cases/
│
├── reports/
│
└── tests/
```

---

# 三十、前端数据流

必须坚持单向数据流：

```text
User
 ↓
Streamlit
 ↓
ResearchService
 ↓
ResearchOrchestrator
 ↓
Planner / Policy / Executor / Analyst
 ↓
Research State
 ↓
ResearchService
 ↓
Streamlit Render
```

Streamlit：

**绝不能直接调用 FEM 内部函数修改科研状态。**

这样未来无论换：

```text
Streamlit

Desktop

CLI

API
```

核心系统都不需要改变。

---

# 三十一、开发计划调整

原 15 天计划中，不再单独开发 Gradio。

调整如下。

## Day 1–8

保持原计划：

* FEM；
* Orchestrator；
* Memory；
* Planner；
* Safety；
* Analyst；
* Feedback；
* Human Approval。

---

## Day 9

Python 3D。

---

## Day 10

MATLAB Adapter。

---

## Day 11 — Streamlit Workspace 骨架

必须完成：

```text
Top Status Bar

Research Explorer

Main Workspace

Inspector

Command Input
```

暂时不追求视觉细节。

---

## Day 12 — 实时实验交互

完成：

```text
Experiment Queue

Progress

Topology

Metrics

Approve / Reject

Pause / Resume
```

并接入 Baseline 实验。

---

## Day 13 — Timeline + Compare

完成：

```text
Research Timeline

Failure Card

Decision Card

/compare

Research Report
```

并冻结三个正式案例。

---

## Day 14 — UI 打磨

重点优化：

```text
Codex-like layout

dark workspace

信息密度

错误提示

Demo workflow

PPT

Video
```

---

## Day 15

Release Freeze。

仅：

```text
Bug Fix

Fresh Environment

Streamlit Restart Test

Worker Crash Test

Replay Mode Test

Demo Rehearsal
```

---

# 三十二、P0 前端要求

必须完成：

1. Research Explorer；
2. Main Research Stream；
3. Experiment Progress；
4. Inspector；
5. Command Bar；
6. Approve / Reject；
7. Pause / Stop；
8. Topology 显示；
9. Metrics；
10. Research Timeline。

---

# 三十三、P1 前端功能

争取完成：

* Compare View；
* 参数锁定；
* Expert Mode；
* 文件 Explorer；
* Report Preview；
* Cached / Live 状态；
* 3D Viewer；
* Failure Evidence 页面。

---

# 三十四、P2

时间允许再做：

* 桌面 Wrapper；
* 拖拽布局；
* Monaco Editor；
* 完整项目文件编辑；
* 多 Workspace；
* 多研究项目并行；
* 插件 Marketplace。

---

# 三十五、比赛 Demo 调整

10 分钟 Demo 不再在多个“网页页面”之间切换。

从头到尾只使用一个：

# TopOptPilot Workspace

---

## 0:00–1:00

左侧创建：

```text
New Research
```

输入：

```text
MBB Beam

Volume 40%

Goal:
low compliance
gray < 5%
connected
```

---

## 1:00–2:00

中央 Research Stream：

```text
PLANNER

I will begin with three F0 experiments...
```

右边同步出现：

```text
Budget
0 / 10

Fidelity
F0
```

---

## 2:00–3:30

三个 Experiment Card 同时运行：

```text
E01 ▶

E02 ▶

E03 ▶
```

Topology 和 convergence 实时刷新。

---

## 3:30–4:30

E03 出现：

```text
FAILED

DISCONNECTION
```

左侧 E03 变成：

```text
✗ E03
```

---

## 4:30–5:30

Research Stream 自动出现：

```text
ANALYSIS

High beta improved grayness but
connectivity failed.
```

接着：

```text
NEXT ACTION

TEST_COMPETING_EXPLANATIONS
```

---

## 5:30–6:00

Policy Engine 给出：

```text
E04:
beta ↓

E05:
rmin ↑
```

右侧：

```text
Risk
LOW
```

中央：

```text
[ Approve ] [ Edit ] [ Reject ]
```

用户点击：

# Approve

---

## 6:00–7:10

第二轮实验。

Timeline 自动形成。

---

## 7:10–8:00

AI 推荐：

```text
UPGRADE FIDELITY
```

运行 Python 3D。

---

## 8:00–8:40

系统提出 MATLAB 高保真验证。

用户选择：

```text
View Verified Real Run
```

显示真实缓存结果。

---

## 8:40–9:30

输入：

```text
/compare E01 E08
```

Workspace 自动显示结构和指标对比。

---

## 9:30–10:00

输入：

```text
/report
```

生成最终科研报告与复现包。

整个过程中：

**不离开 TopOptPilot Workspace。**

---

# 三十六、更新后的技术栈

| 模块             | 技术                       |
| ---------------- | -------------------------- |
| 主 UI            | **Streamlit**        |
| UI 风格          | Codex-like Local Workspace |
| 测试 API         | FastAPI                    |
| 业务服务层       | Python ResearchService     |
| Orchestrator     | Python                     |
| Schema           | Pydantic                   |
| LLM              | 国产开源 Qwen              |
| 2D FEM           | NumPy + SciPy              |
| Fast 3D          | NumPy/SciPy/Numba          |
| High-Fidelity 3D | MATLAB Persistent Worker   |
| 并行             | ProcessPoolExecutor        |
| Research DB      | SQLite                     |
| Raw Assets       | Filesystem                 |
| Cache            | Disk Cache                 |
| Visualization    | Plotly / matplotlib        |
| 配置             | YAML                       |
| 日志             | JSONL                      |

明确删除：

```text
Gradio

React

Vue

独立网页前端

复杂前后端工程
```

---

# 三十七、最终产品形态

TopOptPilot 最终不是：

> 一个 AI 网站。

也不是：

> 一个拓扑优化 Dashboard。

而是：

# 一个 AI 科研工作台

研究人员进入一个统一 Workspace 后，可以：

```text
提出目标
↓
观察 AI 设计实验
↓
看 FEM 实时运行
↓
检查失败
↓
与 AI 讨论原因
↓
批准下一轮实验
↓
比较拓扑
↓
提升到 3D
↓
人工接管
↓
生成研究报告
```

因此界面承担的不是“展示结果”这么简单。

它本身就是：

> **Human Researcher 与 Autonomous Experiment Agent 协作的操作界面。**

这也更加符合 TopOptPilot 最终的产品定位：

> **把拓扑优化中分散在代码、MATLAB、日志、参数表和研究者经验里的实验流程，统一进一个类似 Codex 的 AI 科研 Workspace 中，让真实 2D/3D 仿真实验变得可规划、可观察、可干预、可迭代和可复现。**
