# TopOptPilot 产品方案

> **V5.0 覆盖说明**：当前实现以官方 Pi JSON-RPC、Qwen 3.7 Plus、11 工具沙箱、
> Safety Policy、L0–L3 Research Memory 和真实 Python 2D/3D FEM 为准。
> 本文后续 V4 段落仅保留为演进记录，现行契约见 `V5实施与验收.md`。

> 面向三维工程结构拓扑优化的可验证假设生成与自动实验智能体
> 对应赛题：XH-202619 基于国产开源大模型的 AI Scientist 的研发与应用

---

## 一、产品概述

### 1.1 一句话定义

TopOptPilot 是一个**桌面端 AI Scientist**——它读取论文和工程需求，自主生成可证伪科研假设，调用真实物理求解器完成对照实验，根据结果反复修正方案，最终输出被证据支持或否定的科学结论。

### 1.2 核心原则

| 原则 | 含义 |
|------|------|
| **大模型不代替物理求解** | LLM 负责推理/规划/诊断，MATLAB/CUDA 负责 FEM 计算 |
| **论文方法工程化** | 论文转化为带证据/验证状态的 MATLAB 插件（Paper-to-Plugin） |
| **客观裁决** | 评价器用残差/柔度/灰度/连通等指标裁决方案，非 LLM 自评 |
| **全链路可复现** | DOI/页码/配置/代码提交/随机种子/原始结果全部绑定 |
| **桌面原生** | 终端 TUI 应用，无浏览器依赖，键盘驱动，离线可用 |

### 1.3 目标用户

- **参赛评委**：通过 10 分钟现场演示理解系统能力
- **拓扑优化研究者**：用它做方法对比和参数探索
- **工程设计人员**：用它生成初始拓扑方案

---

## 二、系统架构

### 2.1 四层架构

```
┌──────────────────────────────────────────────────────────────┐
│                      输入与知识层                              │
│  论文PDF · 科研任务包 · 体素几何 · 边界条件 · 材料 · 历史结果  │
├──────────────────────────────────────────────────────────────┤
│                      AI Scientist 层                          │
│                                                              │
│  研究主管 → 证据Agent → 假设Agent → 审稿Agent                │
│           → 实验Agent → 审计Agent                            │
│                                                              │
│  状态机：输入验证→文献挖掘→假设生成→审稿→实验设计→            │
│          实验执行→审计→假设精炼/参数调优/结论                 │
├──────────────────────────────────────────────────────────────┤
│                    方法与求解层                                │
│                                                              │
│  +------------------+  +------------------+                   │
│  | MATLAB 插件体系   |  | CUDA MEX 求解器  |                   │
│  | OC/MMA/滤波/投影  |  | Matrix-free K·u |                   │
│  | 控制器/评价器      |  | PCG/MGCG/灵敏度 |                   │
│  +------------------+  +------------------+                   │
│                                                              │
│  +------------------+  +------------------+                   │
│  | 轻量2D求解器      |  | 三级知识库       |                   │
│  | (Python, 演示用)  |  | SQLite+FAISS+FS  |                   │
│  +------------------+  +------------------+                   │
├──────────────────────────────────────────────────────────────┤
│                      前端与输出层                              │
│                                                              │
│  +------------------+  +------------------+                   │
│  | Textual TUI 应用  |  | 可视化引擎       |                   │
│  | 4面板/命令系统    |  | 曲线/热力图/3D   |                   │
│  | 快捷键/实时反馈   |  | 实时求解callback |                   │
│  +------------------+  +------------------+                   │
│                                                              │
│  输出：STL/VTK · 指标曲线 · 研究报告 · 一键复现包             │
└──────────────────────────────────────────────────────────────┘
```

### 2.2 科研闭环

```
                  ┌─────────────────────────────┐
                  │     输入验证 (规则逻辑)       │
                  └─────────────┬───────────────┘
                                ▼
                  ┌─────────────────────────────┐
                  │     文献挖掘 (LLM)            │
                  │     - 检索方法卡片             │
                  │     - 识别知识缺口             │
                  └─────────────┬───────────────┘
                                ▼
                  ┌─────────────────────────────┐
                  │     假设生成 (LLM)            │
                  │     - 3~5 个候选假设           │
                  │     - 每假设含成功/失败条件    │
                  └─────────────┬───────────────┘
                                ▼
                  ┌─────────────────────────────┐
                  │     假设审稿 (LLM)            │
                  │     - 四维评审+反例            │
                  │     - 排序                    │
                  └─────────────┬───────────────┘
                                ▼
                  ┌─────────────────────────────┐
                  │     实验设计 (LLM+规则)       │
                  │     - 实验矩阵生成             │
                  │     - 插件组合验证             │
                  └─────────────┬───────────────┘
                                ▼
                  ┌─────────────────────────────┐
                  │     实验执行 (异步队列)       │
                  │     - 轻量2D求解器(演示)      │
                  │     - MATLAB/CUDA(生产)      │
                  └─────────────┬───────────────┘
                                ▼
                  ┌─────────────────────────────┐
                  │     结果审计 (LLM+规则)       │
                  │     - NaN/Inf检测             │
                  │     - 结论等级判定             │
                  │     - 失败诊断                 │
                  └─────────────┬───────────────┘
                                │
            ┌───────────────────┼───────────────────┐
            ▼                   ▼                   ▼
   ┌──────────────┐   ┌────────────────┐   ┌──────────────┐
   │ 假设精炼      │   │ 参数调优        │   │ 结论输出      │
   │ 保留方向      │   │ 调整β/p/rmin   │   │ 假设等级      │
   │ 调整条件      │   │ 重跑实验        │   │ 适用边界      │
   └──────────────┘   └────────────────┘   └──────────────┘
            │                   │
            └───────────────────┘
                      ▼
            ┌─────────────────────┐
            │   跨轮成效追踪       │
            │   - 灰度趋势        │
            │   - 柔度趋势        │
            │   - 连通率趋势      │
            └─────────────────────┘
```

---

## 三、Agent 系统

### 3.1 六角色定义

| 角色 | 职责 | 输入 | 输出 | LLM 驱动 |
|------|------|------|------|---------|
| **研究主管** | 任务分解、进度监控、终止决策 | 科研目标、当前状态、预算 | 下一步指令 | 规则逻辑 |
| **证据Agent** | 文献检索、方法卡片提取、知识缺口识别 | 研究目标、论文库 | 方法列表、缺口列表 | ✅ LLM |
| **假设Agent** | 3~5 个可证伪候选假设生成 | 知识缺口、场景、历史结果 | 假设集 | ✅ LLM |
| **审稿Agent** | 四维评审(新颖/自洽/可证伪/成本)、反例生成 | 候选假设、证据 | 评审排序 | ✅ LLM |
| **实验Agent** | 实验矩阵设计、插件组合选择、兼容性验证 | 假设排序、插件注册表 | 实验任务JSON | ✅ LLM+规则 |
| **审计Agent** | 实验结果分析、结论等级判定、失败诊断、下一步建议 | 数值结果、假设 | 裁决、诊断 | ✅ LLM+规则 |

### 3.2 状态机

```
状态：
  INIT → INPUT_VALIDATION → LITERATURE_MINING → HYPOTHESIS_GENERATION
  → HYPOTHESIS_REVIEW → EXPERIMENT_DESIGN → EXPERIMENT_EXECUTION
  → RESULT_AUDIT → [HYPOTHESIS_REFINEMENT | PARAMETER_TUNE | CONCLUSION]

动作类型：
  CONTINUE    — 正常推进
  REITERATE   — 回退到上一状态
  STOP        — 终止

终止条件：
  1. 成功达标 + 跨网格/跨载荷验证
  2. 连续 N 轮无信息增益
  3. 计算预算耗尽
  4. 证据链不可信 → 转人工
  5. 假设被明确否定
```

### 3.3 LLM 集成

```
agent/llm/
├── client.py             # PiAgent runtime + DeepSeek Provider 适配器
│   ├── 指数退避重试 ×3
│   ├── JSON 模式约束
│   └── 降级输出（API 不可用时保持管线不中断）
├── message_builder.py    # 6 角色的 System Prompt + JSON Schema
└── response_parser.py    # LLM 回复 → 结构化数据
    ├── 纯 JSON 解析
    ├── Markdown code block 提取
    └── 自由文本 JSON 提取
```

### 3.4 降级策略

```
LLM 可用 ──→ LLM 驱动决策 ──→ 结构化输出 → 下游处理
                 │
                 ▼ (3次重试失败)
LLM 不可用 ──→ Python 规则层 ──→ 预设假设/规则审稿/规则审计
                 │
                 ▼
           管线不中断，输出降级标记
```

### 3.4 可自选技能系统 (Skill System)

这是一个跨 Agent 角色的**技能加载机制**，继承自 Hermes Agent 的 skill 体系。每个技能封装一个特定的领域能力——论文解析、失效模式诊断、联合反馈控制设计等——Agent 根据当前任务阶段动态选择加载哪些技能。

#### 3.4.1 技能 vs 插件的区别

| 维度 | 技能 (Skill) | 插件 (Plugin) |
|------|-------------|---------------|
| **本质** | LLM 可用的领域知识 + 推理模式 | MATLAB/CUDA 数值方法实现 |
| **运行环境** | Agent 的 system prompt 上下文 | MATLAB/CUDA 运行时 |
| **加载方式** | 按阶段动态注入 prompt | 按实验静态组合 |
| **更新频率** | 每次 LLM 调用可换 | 版本锁定，验证后才变 |
| **典型例子** | "2D 拓扑优化收敛判据知识" | "Heaviside 投影 MEX" |

#### 3.4.2 技能定义规范

```yaml
# skills/topopt_convergence_diagnosis.yaml
skill:
  name: "topopt_convergence_diagnosis"
  version: "1.0.0"
  description: "拓扑优化收敛诊断技能——分析收敛曲线判断是否陷入局部最优"
  
  # 触发条件：哪个Agent的哪个阶段会加载此技能
  trigger:
    agent: ["audit_agent", "experiment_agent"]
    phase: ["result_audit", "experiment_design"]
    condition: "上一轮实验已产生至少一条收敛曲线"
  
  # 注入到 system prompt 的领域知识
  knowledge: |
    拓扑优化的标准收敛判据包括：
    1. 柔度变化 < 0.1% 连续 5 轮 → 收敛
    2. 灰度比例 > 0.15 且 50 轮不下降 → 已停滞，需调整参数
    3. PCG 迭代数突增 > 2x → 刚度矩阵条件数恶化
    4. 连通分量 > 1 → 结构断连，应回退参数
  
  # 注入到 response_format 的输出约束
  output_schema:
    type: "json_object"
    properties:
      convergence_status:
        type: "string"
        enum: ["converged", "stalled", "diverged", "oscillating"]
      diagnosed_cause:
        type: "string"
      recommended_action:
        type: "string"
        enum: ["continue", "rollback_beta", "increase_filter", "switch_controller"]
  
  # 依赖的其他技能
  depends_on: []
  
  # 互斥技能
  conflicts_with: []
```

#### 3.4.3 技能注册表

```python
class SkillRegistry:
    """技能注册表 — 类似 Hermes Agent 的 skill 系统"""
    
    def __init__(self, skill_dir: str = "./skills/"):
        self._skills: dict[str, SkillDef] = {}
        self._load_all(skill_dir)
    
    def get_skill(self, name: str) -> Optional[SkillDef]:
        return self._skills.get(name)
    
    def query(self, agent: str = None, phase: str = None) -> list[SkillDef]:
        """按 Agent 角色和阶段查询可用技能"""
        results = []
        for skill in self._skills.values():
            if agent and agent not in skill.trigger.get("agent", []):
                continue
            if phase and phase not in skill.trigger.get("phase", []):
                continue
            results.append(skill)
        return results
    
    def build_prompt_extension(self, agent: str, phase: str,
                               context: dict) -> str:
        """为指定 Agent+阶段构建技能注入文本"""
        skills = self.query(agent=agent, phase=phase)
        if not skills:
            return ""
        
        sections = []
        for skill in skills[:3]:  # 最多加载 3 个技能
            sections.append(f"=== {skill.name} ===\n{skill.knowledge}")
        return "\n\n".join(sections)
```

#### 3.4.4 首批技能清单

| 技能名称 | 所属 Agent | 触发阶段 | 用途 |
|---------|-----------|---------|------|
| `topopt_convergence_diagnosis` | audit_agent | result_audit | 收敛曲线诊断（停滞/发散/振荡） |
| `paper_method_extraction` | evidence_agent | literature_mining | 从论文 PDF 提取公式、参数、条件 |
| `hypothesis_refinement_patterns` | hypothesis_agent | hypothesis_generation | 从失败实验中提炼精炼方向 |
| `controller_design_knowledge` | experiment_agent | experiment_design | 四种控制器策略的适用场景 |
| `failure_mode_library` | audit_agent | result_audit | 拓扑优化典型失效模式库 |
| `mesh_convergence_knowledge` | experiment_agent | experiment_design | 粗/中/细网格的结果差异规律 |
| `abaqus_altair_comparison` | research_lead | conclusion | 与商业软件的结果对比知识 |

#### 3.4.5 Agent 调用技能的工作流

```python
# 实验 Agent 即将设计实验矩阵，先加载相关技能
skills_prompt = self.skill_registry.build_prompt_extension(
    agent="experiment_agent",
    phase="experiment_design",
    context={"hypothesis": current_hypothesis}
)

# 构建 system prompt 时注入技能知识
system_content = BASE_EXPERIMENT_AGENT_PROMPT
if skills_prompt:
    system_content += f"\n\n## 当前加载的技能\n{skills_prompt}"

# 后续 LLM 调用自动带上技能知识
messages = [
    {"role": "system", "content": system_content},
    {"role": "user", "content": user_prompt}
]
```

#### 3.4.6 技能加载策略

```
每次 LLM 调用前：
  1. 查询 SkillRegistry：当前 Agent + 当前阶段 → 可用技能列表
  2. 从列表中选出与当前上下文最相关的 ≤3 个
  3. 构建技能注入文本 → 追加到 system prompt
  4. 调用 LLM

设计原则：每次最多加载 3 个技能（token 预算控制），技能支持 depends_on（依赖）和 conflicts_with（互斥）关系。
```

---

### 3.5 降级策略

---

## 四、知识库

### 4.1 三级存储架构

```
┌──────────────────────────────────────────────────────────────┐
│  L3: 语义检索层 (FAISS / ChromaDB)                           │
│  - 方法卡片向量索引   - 论文摘要向量索引   - 实验结论向量索引  │
│  - 跨语言检索（中/英） - "这个失败像上次哪次？" 语义匹配       │
└──────────────────────┬───────────────────────────────────────┘
                       │ 向量检索
┌──────────────────────▼───────────────────────────────────────┐
│  L2: 结构化存储层 (SQLite + FTS5)                             │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────────┐    │
│  │方法卡片表 │ │实验记录表 │ │引用元数据 │ │插件注册表     │    │
│  │参数/条件  │ │配置/结果 │ │DOI/作者   │ │接口/状态/版本 │    │
│  │DOI/页码   │ │裁决/指标 │ │年份/期刊  │ │兼容性矩阵     │    │
│  └──────────┘ └──────────┘ └──────────┘ └──────────────┘    │
│  FTS5 全文索引：中英文混合关键词搜索                          │
└──────────────────────┬───────────────────────────────────────┘
                       │ 文件系统
┌──────────────────────▼───────────────────────────────────────┐
│  L1: 原始资产层 (Filesystem)                                  │
│  - 论文 PDF （/knowledge/storage/papers/）                    │
│  - 实验结果 （/experiments/output/）                          │
│  - 检查点   （/knowledge/storage/checkpoints/）               │
│  - 3D 结构  （STL/VTK/NIfTI）                                │
└──────────────────────────────────────────────────────────────┘
```

### 4.2 方法卡片规范

```yaml
method_card:
  method:
    name: "Heaviside Projection"
    type: "projection"          # solver/filter/projection/optimizer/controller/evaluator
    status: "verified"          # candidate/experimental/verified/deprecated
  formula:
    text: "x̃ = tanh(β·η) + tanh(β·(x-η)) / tanh(β·η) + tanh(β·(1-η))"
    source_page: 6
    source_equation: "(12)"
  parameters:
    - name: "beta"
      meaning: "投影陡峭度"
      range: [1, 32]
      suggested: [1, 2, 4, 8, 16]
    - name: "eta"
      meaning: "投影阈值"
      range: [0.3, 0.7]
      default: 0.5
  conditions:
    applicable: ["密度法拓扑优化", "需要清晰0-1边界"]
    known_risks: ["过早提高beta可能陷入局部最优", "可能造成结构断连"]
  evidence:
    doi: "10.1007/s00158-025-04127-3"
    pages: [5, 6, 11]
    figures: [4, 7]
  verification:
    finite_difference_check: "passed"   # 最大误差 < 1e-4
    mbb_reproduction: "passed"          # 柔度误差 < 0.1%
    cpu_gpu_consistency: "passed"
    verified_by: "Dr. Zhang"
```

### 4.3 种子知识库

首次运行时自动植入 ≥ 5 篇核心论文的方法卡片：
1. Heaviside Projection（密度法 0-1 化）
2. PDE/Helmholtz Filter（各向同性滤波）
3. Sensitivity Filter（棋盘格抑制）
4. OC Optimizer（最优准则法）
5. MMA Optimizer（移动渐近线法）

---

## 五、桌面应用界面

### 5.1 技术选型

| 层次 | 技术 | 说明 |
|------|------|------|
| TUI 框架 | **Textual** | Python 异步终端 UI，CSS 布局，60fps |
| 降级方案 | **Rich** | Textual 不可用时纯控制台显示 |
| 图表渲染 | **matplotlib** → 内嵌 PNG | 收敛曲线/热力图/3D 结构截图 |
| 3D 渲染 | matplotlib 3D / trimesh | 体素密度场可视化 |
| 命令输入 | Textual Input + Prompt Toolkit | 命令补全/历史记录 |

### 5.2 四面板布局

```
┌──────────────────────────────────────────────────────────────────┐
│  TopOptPilot v1.0                      模式：[演示]  状态：● 运行中 │
├────────────────┬─────────────────────────┬──────────────────────┤
│ 📋 任务面板    │ 📊 监控面板              │ 💡 智能体面板        │
│ Ctrl+1         │ Ctrl+2                  │ Ctrl+3               │
│                │                         │                      │
│ 任务ID         │ ┌──────────────────┐    │ 当前阶段: 实验执行   │
│ 几何状态       │ │ 收敛曲线 4-in-1  │    │ 迭代: 3/5           │
│ 材料           │ │ ┌──╲╱──╲╱──╲╱─┐ │    │ 预算: 12/30         │
│ 载荷工况       │ │ │ 柔度/灰度/   │ │    │                      │
│ 体积分数       │ │ │ PCG/体积分数 │ │    │ H1: 🥇 联合反馈       │
│ 预算           │ │ └───────────────┘ │    │ H2: 🥈 灰度反馈       │
│                │ │  柔度:128.4       │    │ H3: 🥉 固定调度       │
│ 演示参数:      │ │  灰度:0.037       │    │                      │
│  β ──●── 8    │ │  PCG:146次        │    │ [查看决策链路]       │
│  p ──●── 3    │ └──────────────────┘    │                      │
│  r ──●── 1.5  │                         │                      │
│                │                         │                      │
│ [F1固定调度]   │ 3D 结构预览:            │ [F5刷新3D]           │
│ [F2灰度反馈]   │ ████████████████        │                      │
│ [F3联合反馈]   │ ██░░░░████░░░░██        │                      │
│                │ ████████████████        │                      │
├────────────────┴─────────────────────────┴──────────────────────┤
│ 📝 输出面板 (Ctrl+4)                                            │
│ [10:15:23] ✅ Phase 2 文献挖掘 — 3方法, 2缺口                    │
│ [10:15:28] 💡 Phase 3 假设生成 — H1:联合反馈优于固定调度          │
│ [10:15:32] 🔍 Phase 4 假设审稿 — 反例:粗网格可能振荡              │
│ [10:15:38] 🧪 Phase 5 实验设计 — 6组×3网格×2载荷=36组             │
│ [10:15:40] ⚡ Phase 6 实验执行 — GPU求解中  ████████░░░░ 67%     │
│                                                                │
│ > /run bracket_3d_001 —mesh medium —controller joint_feedback   │
│ └────────────────────────────────────────────────────────       │
└──────────────────────────────────────────────────────────────────┘
```

### 5.3 命令系统

```bash
# === 顶层命令 ===
/run <task.json>               # 运行完整科研闭环
/quick <params>                # 快速参数扫描（演示模式）
/plot <experiment_id>          # 查看特定实验的收敛曲线
/view <experiment_id>          # 查看 3D 结构
/compare <id1> <id2>           # 对比两组实验结果

# === 演示参数调节 ===
/beta <1-32>                   # 设置投影陡峭度
/penal <1-5>                   # 设置 SIMP 惩罚指数
/rmin <0.5-6.0>                # 设置滤波半径
/volfrac <0.1-0.8>             # 设置体积分数
/controller <type>             # 切换控制器策略 (fixed|gray|joint|ablation)

# === 系统命令 ===
/status                        # 系统状态总览
/plugins                       # 插件注册表
/kb                            # 知识库统计
/export <path>                 # 导出完整研究报告
/help                          # 帮助
```

### 5.4 快捷键

| 快捷键 | 功能 |
|--------|------|
| `Ctrl+1~4` | 切换面板焦点 |
| `Ctrl+S` | 启动/暂停科研闭环 |
| `Ctrl+R` | 重置当前任务 |
| `Ctrl+E` | 导出报告 |
| `Ctrl+D` | 切换演示/科研模式 |
| `Ctrl+L` | 清屏 |
| `← →` | 调节 beta（演示模式） |
| `↑ ↓` | 调节 penal（演示模式） |
| `[ ]` | 调节 rmin（演示模式） |
| `F1~F4` | 切换控制器策略 |
| `F5` | 刷新 3D 渲染 |
| `F10` | 全屏 |
| `F12` | 开发者工具 |
| `Tab` | 循环切换面板 |
| `Esc` | 取消当前操作 |

### 5.5 前端接口

```python
# frontend/interfaces.py — 前端桥接器（唯一耦合点）

@dataclass
class UIUpdatePayload:
    panel: str       # "task" | "monitor" | "agent" | "output"
    action: str      # "update" | "append" | "clear" | "focus"
    data: dict
    timestamp: float

class FrontendBridge:
    """后端通过此接口推数据到 UI，前端通过此接口接收命令"""
    
    def subscribe(self, panel: str, callback: Callable): ...
    def publish(self, payload: UIUpdatePayload): ...
    
    def push_log(self, level: str, message: str): ...
    def push_convergence(self, compliance: float, gray: float, cg: int): ...
    def push_agent_state(self, phase: str, iteration: int, trace: list): ...
    def push_3d(self, density_field: np.ndarray): ...
```

---

## 六、可视化与交互功能

### 6.1 可视化组件

| 组件 | 渲染方式 | 更新频率 | 用途 |
|------|---------|---------|------|
| **收敛曲线套图** | matplotlib 2×2 网格 | 每轮迭代 | 柔度/灰度/PCG/体积分数 |
| **灰度曲线对比** | matplotlib 叠加 | 跨轮比较 | 多实验组灰度收敛对比 |
| **实验矩阵热力图** | seaborn | 实验完成后 | B0/B1/A1/A2/Ours/Ablation 指标对比 |
| **3D 结构** | matplotlib 3D / trimesh | 每轮实验完成 | 体素密度场可视化 |
| **Agent 决策链路** | networkx 有向图 | 每阶段完成 | Agent 角色间数据流 |
| **失败→修正对比** | matplotlib 并排 | 演示模式 | 第一轮失败 vs 第二轮修正 |
| **跨轮成效趋势** | matplotlib 折线 | 每轮完成 | 灰度/柔度/连通率随迭代趋势 |

### 6.2 演示模式实时交互

```
用户操作                    系统响应                    前端反馈
─────────                  ────────                   ────────
← → 调 beta               触发轻量2D求解             收敛曲线实时刷新
↑ ↓ 调 penal              (2-3秒完成)               3D结构实时更新
F3 切换联合反馈            调用callback推送            灰度曲线叠加显示
                           面板数据自动更新
```

### 6.3 轻量 2D 求解器

```python
class Lightweight2DSolver:
    """纯 Python 2D 拓扑优化，演示模式用"""
    
    def solve(self, nelx=60, nely=20, volfrac=0.4, penal=3.0,
              rmin=1.5, projection="none", beta=1,
              controller="fixed", callback=None, max_iter=100):
        """每轮迭代调用 callback，实现实时推送"""
        for i in range(max_iter):
            # 1. FEM 分析
            # 2. 灵敏度计算 + 滤波
            # 3. 投影 + 控制策略
            # 4. OC 更新
            if callback:
                callback(x_phys, compliance, gray_ratio, i)
```

---

## 七、任务规划 → 数据分析 → 自调整迭代

### 7.1 核心闭环

这是赛题 B 的核心，也是系统的灵魂：

```
                          ┌─────────────┐
                          │  任务规划     │
                          │  (LLM驱动)   │
                          └──────┬──────┘
                                 │ 实验矩阵
                                 ▼
                          ┌─────────────┐
                          │  实验执行     │
                          │  (队列异步)   │
                          └──────┬──────┘
                                 │ 原始结果
                                 ▼
                          ┌─────────────┐
                          │  数据分析     │
                          │  (LLM审计)   │
                          └──────┬──────┘
                                 │ 裁决+诊断
         ┌───────────────────────┼───────────────────────┐
         ▼                       ▼                       ▼
  ┌──────────────┐      ┌────────────────┐      ┌──────────────┐
  │ 假设精炼      │      │ 参数调优        │      │ 结论输出      │
  │ 保留假设方向  │      │ 修改β/p/rmin   │      │ 假设等级      │
  │ 调整条件/阈值 │      │ 保留插件组合    │      │ 适用边界      │
  │ 重跑验证      │      │ 重跑实验        │      │ 研究报告      │
  └──────────────┘      └────────────────┘      └──────────────┘
         │                       │
         └───────────┬───────────┘
                     ▼
            ┌─────────────────┐
            │  跨轮成效追踪    │
            │  展示逐步提升    │
            └─────────────────┘
```

### 7.2 自调整机制

```python
class SelfAdjustingIteration:
    """自调整迭代引擎"""
    
    def run_iteration(self, iteration_num, previous_results):
        # 第一步：分析上一轮结果
        verdicts = self.audit_agent.analyze(previous_results)
        
        # 第二步：决定本轮类型
        if all(v.level == SUPPORTED for v in verdicts):
            return self._conclude()          # 全部通过 → 结论
        elif any(v.level == NOT_SUPPORTED for v in verdicts):
            return self._hypothesis_refine()  # 假设被否定 → 精炼
        elif any(v.level == PARTIALLY_SUPPORTED for v in verdicts):
            return self._parameter_tune()     # 部分通过 → 参数调优
        else:
            return self._design_discriminating()  # 无法区分 → 区分性实验
        
    def _parameter_tune(self):
        """参数调优：不重新生成假设，只调整参数"""
        # 审计Agent 输出的 recommended_fix 直接应用
        fix = self.audit_agent.get_recommended_fix()
        new_params = {**current_params, **fix}
        return self._run_experiment(new_params)
    
    def _hypothesis_refine(self):
        """假设精炼：保留合理部分，调整条件"""
        # LLM 基于失败诊断生成精炼后的假设
        refined = self.llm.refine_hypothesis(
            original=self.current_hypothesis,
            failure_diagnostics=self.audit_agent.diagnostics
        )
        return self._run_experiment_matrix(refined)
```

### 7.3 跨轮成效追踪

```python
class ProgressiveTracker:
    """跨轮成效追踪器"""
    
    def __init__(self):
        self.records = []       # 每轮成效记录
        self.best_result = None # 全局最优
    
    def record(self, results):
        self.records.append({
            "iteration": len(self.records),
            "best_compliance": min(r.compliance for r in results),
            "best_gray_ratio": min(r.gray_ratio for r in results),
            "connected_rate": sum(1 for r in results if r.connected) / len(results),
            "timestamp": time.time(),
        })
    
    def get_progress(self) -> dict:
        """返回成效提升报告（供演示使用）"""
        if len(self.records) < 2:
            return {"message": "需要至少两轮数据"}
        first, last = self.records[0], self.records[-1]
        return {
            "compliance_delta": f"{(last['best_compliance'] - first['best_compliance']) / first['best_compliance'] * 100:+.1f}%",
            "gray_ratio_delta": f"{(last['best_gray_ratio'] - first['best_gray_ratio']) / first['best_gray_ratio'] * 100:+.1f}%",
            "connected_rate_delta": f"{(last['connected_rate'] - first['connected_rate']) * 100:+.1f}%",
            "rounds": len(self.records),
            "history": self.records,
        }
    
    def get_narrative(self) -> str:
        """生成自然语言成效摘要"""
        p = self.get_progress()
        return (f"经过 {p['rounds']} 轮迭代，系统自主将灰度比例{p['gray_ratio_delta']}、"
                f"柔度{p['compliance_delta']}、连通率{p['connected_rate_delta']}。")
```

### 7.4 物理一致的模拟

```python
def _simulate_physics_aware_results(task):
    """参数感知的模拟结果（非纯随机）"""
    beta = task.params.get("beta_max", 8)
    penal = task.params.get("p_start", 3)
    has_heaviside = task.projection == "heaviside_projection"
    controller_type = task.controller
    
    # 物理趋势：Heaviside → 更低灰度
    base_gray = 0.15 if not has_heaviside else 0.08
    gray = base_gray * (4 / max(beta, 1))  # beta 越高灰度越低
    gray = min(max(gray, 0.02), 0.25)
    
    # 物理趋势：高 penal → 更清晰但可能断连
    if has_heaviside and beta > 12 and "feedback" not in controller_type:
        connected = random.choices([1, 2, 3], weights=[0.3, 0.5, 0.2])[0]
    else:
        connected = 1
    
    # 物理趋势：联合反馈 → 最佳综合表现
    if "joint" in controller_type:
        gray *= 0.7  # 联合反馈进一步降低灰度
        compliance *= 0.95  # 略微改善刚度
    
    return {"gray_ratio": gray, "connected_components": connected, ...}
```

---

## 八、插件体系

### 8.1 六类插件

| 类型 | 职责 | 第一版实现 | 接口 |
|------|------|-----------|------|
| **求解器** | FEM 计算 | CPU-TOP3D_XL, CUDA MEX | `create/analyze/destroy` |
| **优化器** | 设计变量更新 | OC, MMA | `update(x, obj, grad, constraints)` |
| **滤波** | 棋盘格/长度尺度控制 | 灵敏度滤波, PDE滤波 | `apply(x, dc, model, params)` |
| **投影** | 0-1 清晰化 | 无投影, Heaviside, 延续策略 | `apply(x_filtered, params)` |
| **控制器** | 参数自适应调整 | 固定, 周期性, 灰度反馈, 联合反馈 | `update(history, metrics, params)` |
| **评价器** | 指标裁决 | 标准评价器（柔度/灰度/连通/残差） | `evaluate(result, x_phys, history)` |

### 8.2 生命周期

```
Candidate (论文提炼完成, 不可调用)
    │ 规格说明完整 + 接口定义完成
    ▼
Experimental (有代码原型, 仅隔离实验)
    │ 单元测试通过 + 梯度检查通过 + MBB复现通过
    │ + 三维小案例通过 + CPU/GPU一致性 + 人工批准
    ▼
Verified (可正式使用)
    │ 发现缺陷或被更优方法替代
    ▼
Deprecated (禁止用于新结论)
    │ 缺陷修复 + 重新验证
    ▼
Verified (恢复使用)
```

### 8.3 兼容性规则

| 组合 | 状态 | 原因 |
|------|------|------|
| OC + 体积分数 + PDE 滤波 | ✅ 允许 | 单约束最小柔度基线 |
| OC + 多个局部应力约束 | ❌ 禁止 | 当前 OC 不支持多约束 |
| MMA + 位移约束 | ⚠️ 条件允许 | 需通过梯度检查 |
| Heaviside + 无链式导数 | ❌ 禁止 | 梯度不完整 |
| Experimental 插件 + 正式结论 | ❌ 禁止 | 未完成验证 |

### 8.4 回退链

```python
FALLBACK_CHAIN = {
    "heaviside_projection": ["none"],
    "joint_feedback_controller": ["gray_feedback_controller", "periodic_controller"],
    "cuda_mex": ["cpu_top3dxl"],
    "mma": ["oc"],
}
```

---

## 九、边界情况处理

### 9.1 运行时异常

| 异常 | 检测 | 处理 |
|------|------|------|
| CUDA MEX 崩溃 | mexAtExit 钩子 | 自动重启 MATLAB + 回滚 checkpoint |
| NaN/Inf 结果 | NaNChecker 每轮检测 | 停止本轮，回滚到最后稳定参数 |
| LLM API 超时 | 指数退避 3 次 | 降级到规则逻辑，管线不中断 |
| 磁盘空间不足 | 每轮前检查 | 清理历史缓存 / 报警 |
| 论文 PDF 解析失败 | PyMuPDF 异常捕获 | 降级 OCR → arxiv API → 人工标注 |

### 9.2 数据异常

| 异常 | 检测 | 处理 |
|------|------|------|
| 任务包字段缺失 | validate_task_package() | 完整报告缺失字段 |
| 材料参数不合理 | 数值范围校验 | E>0, ν∈(0,0.5), vf∈(0,1] |
| 几何文件不存在 | 文件存在性校验 | 提示路径错误 |
| 知识库为空 | 首次运行检测 | 自动植入种子方法卡片 |

### 9.3 决策异常

| 异常 | 检测 | 处理 |
|------|------|------|
| 无假设可审 | 空列表检测 | 回退到假设生成 |
| 连续无信息增益 | 计数器 | 达到阈值→终止 |
| 两个假设无法区分 | 审计判定 | 自动设计区分性实验 |
| GPU/CPU 偏差过大 | 一致性检查 | 进入求解器诊断模式 |

---

## 十、实施路线

### Phase 0 — 地基（已完成 ✅）

| 模块 | 状态 |
|------|------|
| LLM Client + 重试 + 降级 | ✅ |
| MessageBuilder + JSON Schema | ✅ |
| ResponseParser（纯JSON/MD/自由文本） | ✅ |
| NaNChecker（NaN/Inf/None/越界） | ✅ |
| ExperimentQueue（异步提交/轮询/回调） | ✅ |
| 编排器 LLM 驱动 8 Phase | ✅ 37/37 测试通过 |
| PiAgent + DeepSeek V4 Flash 集成验证 | ✅ 真实 Provider 调用通过 |

### Phase 1 — 自调整闭环（7天）

| 任务 | 产出 |
|------|------|
| ProgressiveTracker 跨轮成效追踪 | `agent/tracking/progressive_tracker.py` |
| 物理一致模拟（参数感知） | `experiments/experiment_queue.py` 修改 |
| 自调整迭代引擎 | `agent/orchestrator.py` 修改 |
| 参数调优路径 | `agent/roles/parameter_tuner.py` |
| 假设精炼路径 | `agent/roles/hypothesis_refiner.py` |
| 状态机扩展（3 新状态） | `agent/state_machine.py` 修改 |
| 实验矩阵依结果变化 | `experiments/experiment_matrix.py` 修改 |

### Phase 2 — 桌面应用 + 可视化（7天）

| 任务 | 产出 |
|------|------|
| 前端接口层（FrontendBridge） | `frontend/interfaces.py` |
| 可视化组件（曲线/热力图/3D） | `frontend/visualizers.py` |
| Rich 降级版 | `frontend/rich/app.py` |
| Textual 主应用 4 面板 | `frontend/textual/` 整套 |
| 轻量 2D 求解器 | `solver/topopt_2d.py` |
| 演示模式交互 | `frontend/textual/widgets/` 扩展 |

### Phase 3 — 知识库升级（5天）

| 任务 | 产出 |
|------|------|
| SQLite 表结构 + 访问层 | `knowledge/sqlite_store.py` |
| FTS5 全文索引 | `knowledge/sqlite_store.py` |
| FAISS 向量检索 | `knowledge/vector_store.py` |
| 统一 KnowledgeBase 接口 | `knowledge/knowledge_base.py` |
| 种子方法卡片 ×5 | `knowledge/seeds/` |

### Phase 4 — 演示冲刺（5天）

| 任务 | 产出 |
|------|------|
| 预计算结果缓存 | `experiments/precompute_cache.py` |
| 失败→修正演示录制 | 视频 + 日志 |
| CPU/GPU 一致性验证 | 验证报告 |
| 全新环境复现 | 复现脚本 |
| 答辩 PPT + 视频 | 答辩材料 |

---

## 十一、演示脚本（10分钟）

| 时间 | 环节 | 画面 | 交互 |
|------|------|------|------|
| 0:00–0:50 | 问题引入 | 3D 支架问题 + 传统方法对比 | 自动播放 |
| 0:50–1:40 | 系统架构 | Agent 决策链路图 | 自动播放 |
| 1:40–2:40 | 上传任务 | 任务面板 + 3D 几何 | 现场加载 JSON |
| 2:40–3:40 | Paper-to-Plugin | 方法卡片面板 | 现场上传 PDF |
| 3:40–4:40 | 假设竞争 | 假设面板 + 审稿评分 | 现场 /run |
| 4:40–6:10 | 真实求解 | 收敛曲线实时刷新 | 现场观察 |
| **6:10–7:20** | **失败→修正** | **参数调 beta→结构变化** | **← → 调参数** |
| 7:20–8:30 | 对比 | 热力图 + 成效趋势 | F1/F2/F3 切策略 |
| 8:30–9:20 | 独立复核 | 3D 结构 + 位移云图 | F5 刷新 |
| 9:20–10:00 | 结论 | 报告导出 | /export |
