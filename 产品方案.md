# TopOptPilot V6.1 产品方案：知识驱动的拓扑优化科研智能体

> **V6.1 核心升级**：从"可运行的科研工具"升级为"低门槛、可审查、可复现的拓扑优化科研系统"。
> V6.1 在 V6.0 深色桌面工作台基础上，引入 Qwen API Key 安全管理、自然语言引导向导、
> MATLAB 全保真度求解、Subagent 多角色审查、离线知识库和模板化阶段报告。

---

## 一、方案总结

### 1.1 核心改动

- **Qwen API Key 安全管理**：设置中心允许填写 API Key、模型 ID 和请求 URL，统一 OpenAI-compatible Chat Completions 协议；密钥保存到 Windows Credential Manager，不进入 SQLite、日志、报告或复现包。
- **自然语言引导向导**：新建研究改为"自然语言描述 → AI 提取 → 分步确认 → 冻结 Research Contract"，同时保留折叠式专家设置。
- **MATLAB 全保真度求解**：F0/F1 为 MATLAB 2D，F2/F3 为 MATLAB 3D；仅 F3 逐次强制人工审批。Python 求解器退出正式执行路径。
- **参数化几何与掩膜导入**：支持工程基础模板和 PNG/CSV/MAT/JSON 掩膜导入，几何生成在 MATLAB 中完成。
- **模板化阶段报告**：每轮生成 Markdown 阶段报告；研究终止时生成 Markdown + PDF，结构遵循模板，数值全部来自真实 Research State。

### 1.2 核心闭环

```text
自然语言需求
→ AI 引导建立 Research Contract
→ 假设生成（Guide Subagent）
→ 实验设计（Planner Subagent）
→ 多角色审查（Reviewer Subagent）
→ Policy 编译参数
→ MATLAB MCP 运行真实求解器
→ Evaluator 确定性评估
→ 受控实验对比
→ Pi Research Lead 决定下一轮
→ Markdown/PDF 阶段报告
→ 目标达成 / 停滞 / 预算耗尽
```

产品重点证明：

> Pi 能否依据真实拓扑优化实验结果和预算，安全地决定下一次实验，而不依赖用户人工指定下一组参数。

### 1.3 借鉴能力

- "假设—实验—评估—迭代"科研循环
- Artifact 血缘记录输入、求解、证据与决策
- 自然语言向导降低使用门槛
- 常驻主 Agent + 独立 Subagent 审查
- 离线知识库 + Research Memory 支持长期科研
- 不允许语言模型直接修改参数、运行任意 MATLAB 或制造结果

---

## 二、产品视觉与前端原则

### 2.1 保留 V5/V6 视觉风格

- 深色 Codex/VS Code 式工作台
- 蓝色交互色、绿色成功、琥珀色待审批、红色失败、紫色人工干预、灰色离线
- 继续使用现有字体、间距、边框、按钮和卡片语言
- 不重新设计主题系统，不引入新的蓝白渐变或独立视觉品牌

### 2.2 科研内容优先

视觉中心依次为：

```text
Topology → Convergence → Metrics → Evaluator Evidence
→ Decision Chain → Agent Explanation → Report
```

Pi 输出显示为结构化研究事件：Observation / Interpretation / Hypothesis / Intent / Evidence Summary / Recommended Next Experiment。不显示 Chain-of-Thought。

---

## 三、Qwen/OpenAI-compatible 设置

### 3.1 凭据管理

- "Agent 与模型"增加 API Key 密码框、显示/隐藏、删除凭据、模型 ID、Base URL、超时、重试和连接测试
- 新增 API：
  - `PUT /api/settings/agent-credential` — 保存凭据
  - `DELETE /api/settings/agent-credential` — 删除凭据
  - `POST /api/settings/test-agent` — 允许用尚未保存的 Key/URL/模型测试
- `GET /api/settings` 只返回 `credential_status`、`credential_source` 和最近验证状态，不返回密钥

### 3.2 安全约束

- Windows Credential Manager 目标固定为 `TopOptPilot/QwenOpenAICompatible`
- 运行时优先读取凭据库，缺失时兼容 `DASHSCOPE_API_KEY` 环境变量
- Base URL 要求 HTTPS；仅 `localhost/127.0.0.1` 可使用 HTTP
- 测试通过真实 `/chat/completions` 最小请求验证，`CONFIGURED ≠ VERIFIED`
- Pi RPC 使用动态生成的 OpenAI-compatible provider 配置，密钥只注入 Pi 子进程环境
- 保存配置后提示重启 Pi Session

### 3.3 界面显示

```text
Runtime: Pi Agent
Provider: DashScope / Compatible Qwen API
Model: 当前实际模型 ID
API Key: ●●●●●●●●（不显示）
```

---

## 四、自然语言引导式 Research Setup

### 4.1 向导步骤

新建研究进入 Guided Setup：

1. **需求描述**：用户用自然语言描述希望设计的 2D/3D 结构、用途、受力和期望
2. **结构与维度**：选择 MBB/悬臂梁/简支梁/L型/桥梁/自定义 Mask，设定尺寸
3. **材料与单位**：杨氏模量、泊松比、单位制
4. **载荷与约束**：载荷位置/方向/大小，支撑区域
5. **优化目标与预算**：目标函数、体积分数、灰度约束、连通性、F0-F3 分预算
6. **合同确认**：逐项确认 AI 建议，冻结 Research Contract

### 4.2 AI 解析接口

`POST /api/research/guide/parse` 将自然语言解析为结构化草案：

```json
{
  "suggested_contract": { ... },
  "field_sources": { "volfrac": "AI_SUGGESTED", "geometry": "USER" },
  "assumptions": ["假设材料为各向同性线弹性"],
  "missing_fields": ["单位制未明确"],
  "warnings": ["载荷位置可能超出设计域"],
  "confidence": 0.82
}
```

### 4.3 字段来源标记

每个字段显示来源：`USER` / `DEFAULT` / `AI_SUGGESTED` / `LOCKED`

AI 推断字段标记为 `AI_SUGGESTED`，逐项显示解释、单位和影响；未被用户确认的必填字段不得创建合同。

### 4.4 专家模式

- `penal/rmin/beta`、迭代细节和网格等级默认隐藏，由 Policy 编译
- 专家模式只允许修改 Policy 公开的安全范围
- 提供结构示意、支撑/载荷图例、单位换算、运行成本预估
- MATLAB 就绪检查和通俗错误说明

### 4.5 无 MATLAB 降级

无 MATLAB 时仍可使用 AI 向导、创建合同和查看报告草稿，但运行按钮禁用，并显示安装、许可证、自动检测和手动选择路径的引导。

---

## 五、科研状态机与 Subagent 架构

### 5.1 科研状态机

每轮研究统一经过：

```text
SETUP → HYPOTHESIZE → PLAN → REVIEW → SUBMIT → RUN
→ EVALUATE → ANALYZE → COMPARE → DECIDE → REPORT
```

Pi Research Lead 每轮必须先读取研究上下文、预算、求解器能力和相关知识，再决定科学意图。

### 5.2 7 种科学意图

Pi 只能提交稳定科学意图：

```text
ESTABLISH_BASELINE
EXPLORE_PARAMETER
REDUCE_GRAYNESS
RESTORE_CONNECTIVITY
TEST_COMPETING_EXPLANATIONS
UPGRADE_FIDELITY
VERIFY_CANDIDATE
```

数值参数由 Policy Engine 编译，Pi 不接触。

### 5.3 Subagent 角色

每个 Research 保持一个常驻 Pi Research Lead，由它按状态机分发预定义 Subagent：

| Subagent | 职责 | 权限 |
|---|---|---|
| **Guide** | 解释术语、引导需求、推荐模板 | 只读知识和研究草稿 |
| **Hypothesis** | 提出假设和竞争解释 | 读取上下文、历史、失败证据 |
| **Experiment Planner** | 将假设转成科学意图 | 可调用 Policy 预览，不可提交 |
| **Experiment Executor** | 提交已审查的受控实验 | 仅可调用 preview/submit/status/result |
| **Independent Reviewer** | 审查证据、因果性、预算和升级价值 | 全部只读，不可提交 |
| **Report Writer** | 按模板组织报告 | 只读已确认事实和 Artifact |

Subagent 使用独立、短生命周期 Pi Session，具有各自工具白名单。主 Agent 常驻，Subagent 按需启动；歧义失败、保真度升级和最终结论必须触发 Independent Reviewer。

### 5.4 安全隔离

- Experiment Executor 不能直接看到或调用 MATLAB MCP
- Subagent 的具体白名单由角色固定，不能由主 Agent 动态扩大
- 任何 Agent 都不能直接调用 MATLAB MCP 或任意 MATLAB

实验执行路径：

```text
Subagent → experiment_submit → Policy → Safety → Budget
→ Human Gate → Deterministic Executor → MATLAB MCP
```

---

## 六、内置知识库

### 6.1 知识架构

采用"版本化技能库 + SQLite FTS5 检索 + Research Memory"，首版完全离线，不依赖在线论文检索。

### 6.2 知识内容

- 拓扑优化基础概念
- SIMP、OC、滤波、投影和收敛说明
- 2D/3D 工程模板
- 材料、载荷和边界条件指南
- 求解器参数语义和安全范围
- MATLAB MCP 工具使用规范
- 成功实验案例
- 断连、棋盘格、灰度过高、不收敛和基础设施失败案例
- 假设—实验—对比—决策示例
- 报告模板与科研审查规则

### 6.3 知识检索

Agent 在规划前调用：

```text
knowledge_search
solver_get_capabilities
research_get_context
research_get_budget
```

Agent 通过受控知识和案例掌握求解器使用方式，不宣称对 Qwen 模型权重进行了微调。

### 6.4 Knowledge Center

用户可在界面的 Knowledge Center 查看相同内容，并从参数、错误、实验和报告直接打开对应解释。

---

## 七、MATLAB 全保真度求解

### 7.1 四级保真度

所有正式拓扑优化实验经 MATLAB MCP 运行：

```text
F0 = MATLAB 2D coarse — 快速基线
F1 = MATLAB 2D fine — 精细验证
F2 = MATLAB 3D coarse — 低成本三维验证
F3 = MATLAB 3D fine — 高精度最终验证
```

F0–F2 通过 Policy、Safety 和 Budget 后可自动运行；F3 始终需要逐次人工审批。

### 7.2 Python 求解器定位

- Python 求解器退出新研究正式执行路径
- 仅保留为历史结果读取、回归验证和开发参考
- 禁止任何 MATLAB 失败后的 Python fallback
- 旧 Research、旧 Python 结果和旧报告保持可读，不改写 provenance

### 7.3 工程基础模板

模板包括：2D/3D MBB、悬臂梁、简支梁、L 形支架、桥式域。桥式域采用简支边界、可配置集中/分布载荷及非设计桥面带。

### 7.4 掩膜导入

- **2D**：PNG 二值图、CSV 掩膜
- **3D**：MAT 文件中的 `domain_mask`、JSON 布尔体素/RLE 掩膜
- 不支持 STL、STEP 或其他 CAD 导入

### 7.5 几何与载荷

- 模板目录接口和受控几何上传接口
- 上传文件复制到当前研究数据目录，记录 SHA256
- 不接受路径引用、脚本、CAD、函数句柄或可执行内容
- MATLAB 负责模板生成、掩膜读取、尺寸重采样、连通性预检
- MATLAB 负责语义化载荷/支撑区域到节点和自由度的映射；Agent 不接触原始 DOF
- MatlabGateway 校验几何资产归属、维度、尺寸、哈希和任务 Schema
- 非法、空域、无支撑、载荷落在域外或刚体约束不足时拒绝运行

### 7.6 加速求解

保留现有 `.m` 算法作为权威参考实现，增加经过等价性验证的加速变体：

- 预计算单元自由度和稀疏矩阵索引
- 复用滤波矩阵、设计域 Mask 和不变边界数据
- 向量化刚度装配与灵敏度计算
- 关闭非必要绘图和磁盘写入
- MATLAB 会话、求解器路径和函数保持热启动
- 可用时启用 Parallel Computing Toolbox
- 可用时启用经过验证的 MEX/GPU 模块
- 不存在额外工具箱或 GPU 时回到 MATLAB CPU 实现
- 加速实现失败时只能回到同保真度的 MATLAB 权威实现，不得回退 Python
- F3 的任何 MATLAB/MCP 基础设施失败仍标记为 `FAILED/MATLAB_INFRASTRUCTURE`

### 7.7 缓存与预热

MATLAB MCP 保持单会话队列，增加启动预热、能力探测、任务缓存和进度事件。缓存键包含 Research Contract、参数、网格、求解器版本、入口 SHA256 和加速变体。

---

## 八、阶段报告与最终报告

### 8.1 报告组装

报告由确定性 `ReportBuilder` 组装，Pi 只提供观察、解释、竞争假设、决策理由和下一步建议；所有数字、达成判断、实验差值及文件路径来自数据库、Evaluator 和真实产物。

### 8.2 阶段报告

每轮 Pi 完成结果解释与下一决策后生成 `round-XXXX.md`；Pi 不可用时仍生成报告，但明确标记 `RULE_FALLBACK`。

### 8.3 最终报告

研究因目标达成、停滞、预算耗尽或用户终止后生成：

- `final-report.md`
- `final-report.pdf`

### 8.4 报告结构

报告固定包含模板第一至第六章：

1. 研究概述（原始需求 + AI 建议 + 确认结果）
2. 研究假设与实验设计
3. Policy 参数与求解配置
4. MATLAB 求解结果与 Evaluator 指标
5. 对比分析与结论
6. 人工干预、失败实验与可行性结论

摘要和参考文献按真实内容决定是否启用。附录填写说明不进入正式报告。

### 8.5 数据诚实性

- 缺失指标写"未计算"
- 不存在的 STL/STEP、应力图、位移图或基准实验不得列出
- 只有 Evaluator 判定可行时才能写"成功"
- 异常轮次使用模板的异常分支，保留原始错误、失败阶段、MATLAB/MCP 状态
- 对比表只包含真实且可控的实验；多参数同时变化时标记"非受控比较"

### 8.6 PDF 生成

- 使用 ReportLab 和随包分发的开源中文字体
- 蓝色标题、表格和图注风格
- 修复图注跨页、空白页和本机绝对路径进入交付清单的问题

### 8.7 报告管理

- 阶段报告列表、预览、打开目录
- 下载最终 Markdown/PDF 和重新生成操作
- 重新生成保持报告编号稳定并增加修订号

---

## 九、Workspace 信息架构

### 9.1 总体布局

```text
┌──────────────────────────────────────────────────────────────┐
│ TopOptPilot │ Research │ Mode │ Backend Health │ Settings   │
├──────────────┬──────────────────────────────┬────────────────┤
│ Research     │                              │ Evidence       │
│ Navigator    │     Experiment Canvas        │ Inspector      │
│              │                              │                │
│ Goal         │ SETUP / HYPOTHESIS / PLAN    │ Parameters     │
│ Constraints  │ RUN / ANALYZE / COMPARE      │ Metrics        │
│ Rounds       │ DECIDE / REPORT / TIMELINE   │ Evaluator      │
│ Experiments  │                              │ Provenance     │
│ Decisions    │                              │ Budget         │
│ Reports      │                              │                │
├──────────────┴──────────────────────────────┴────────────────┤
│ Command Composer                           Approve │ Run     │
└──────────────────────────────────────────────────────────────┘
```

### 9.2 Backend Health Bar

分别显示：Pi RPC / Qwen API / Python 2D / Python 3D / MATLAB MCP / MATLAB / Desktop Sidecar

统一状态：

```text
NOT_CONFIGURED → CONFIGURED → STARTING → READY → VERIFIED
                                                     ↓
                                               DEGRADED → FAILED
```

`CONFIGURED` 不得显示为绿色可用。只有真实连接测试或最小求解通过后才能显示 `READY/VERIFIED`。

### 9.3 Research Navigator

左侧按研究过程组织，实验按 Round 分组：

```text
Research MBB-001
├─ Goal / Constraints / Contract
├─ Round 1
│  ├─ E01 Baseline ✓
│  ├─ E02 Explore ✓
│  └─ E03 Disconnection ✕
├─ Round 2
│  ├─ E04 ▶
│  └─ E05 ○
├─ Decisions / Evidence / Pareto
├─ Reports（阶段报告列表）
└─ Files
```

### 9.4 Evidence Inspector

右侧根据选中对象切换：

- **Research**：目标、约束、预算、当前最优、终止状态
- **Experiment**：参数、后端、保真度、指标、风险、运行来源、Solver Variant
- **Decision**：Pi 意图、证据、Policy 结果、Safety、人工审批
- **Failure**：失败类型、确定性证据、相关实验、可能解释
- **MATLAB**：MATLAB/MCP 版本、求解入口、文件 SHA256、原始证据位置
- **Knowledge**：引用的知识条目、Subagent 审查意见、对比实验受控变量检查

---

## 十、接口、数据与新增类型

### 10.1 新增核心类型

```text
AgentRole · SubagentTask · SubagentVerdict
KnowledgeDocument · KnowledgeCitation
SolverCapability · SolverVariant
ExperimentHypothesis · ControlledComparison · ArtifactLineage
```

### 10.2 实验溯源

每个实验额外记录：

```text
lead_session_id · subagent_task_ids · decision_source
intent_source · knowledge_ids · evidence_ids
solver_variant · acceleration_mode · solver_sha256
task_hash · review_verdict · human_decision
```

### 10.3 Pi 工具白名单

正式工具：

```text
research_get_context · research_query_history · research_get_budget
policy_compile_intent · experiment_preview · experiment_submit
experiment_status · experiment_result · experiment_compare
failure_get_evidence · research_get_pareto
knowledge_search · knowledge_get · solver_get_capabilities
subagent_dispatch · subagent_status
```

### 10.4 桌面 API 新增

```text
GET  /api/knowledge/search
GET  /api/knowledge/{id}
GET  /api/solvers/capabilities
GET  /api/research/{id}/agent-tasks
POST /api/research/{id}/guide
PUT  /api/settings/agent-credential
DELETE /api/settings/agent-credential
POST /api/settings/test-agent
POST /api/research/guide/parse
```

### 10.5 WebSocket 新增事件

```text
SUBAGENT_STARTED · SUBAGENT_RESULT · HYPOTHESIS_CREATED
REVIEW_VERDICT · KNOWLEDGE_REFERENCED · SOLVER_CAPABILITY
SOLVER_WARMUP · SOLVER_VARIANT_SELECTED · COMPARISON_RESULT
```

### 10.6 统一事件信封

```json
{
  "event_id": "EV-...",
  "research_id": "MBB-001",
  "experiment_id": "E03",
  "timestamp": "...",
  "type": "EVALUATOR_RESULT",
  "source": "EVALUATOR",
  "payload": {}
}
```

---

## 十一、实施顺序

### 阶段一：协议与凭据

- 冻结 Research、Experiment、Result、Event、Intent 和 MATLAB Task Schema
- 增加统一 provenance/source
- 实现 Windows Credential Manager API Key 管理
- 完成 OpenAI-compatible provider 动态配置

### 阶段二：引导向导与 Subagent

- 实现 Guided Setup 和 `POST /api/research/guide/parse`
- 建立 Pi Lead 编排器、独立 Subagent Session 与角色工具白名单
- 建立离线知识库、检索工具和用户 Knowledge Center

### 阶段三：MATLAB 全保真度

- 将 F0–F3 统一迁移到 MATLAB MCP
- 实现 MATLAB 热启动、稀疏装配优化和可选 MEX/GPU 能力探测
- 保留权威 MATLAB 基线，完成等价性验证
- 实现工程基础模板和掩膜导入

### 阶段四：报告与发布

- 完成 ReportBuilder 阶段报告和最终 Markdown/PDF
- 实现 Guided Setup、科研状态画布、多角色审查和模板报告
- 运行物理回归、Agent 闭环、性能记录和 Windows 安装测试
- 冻结 Case A/B/C，运行 Random/Grid/TPE/Rule/Pi 对照

---

## 十二、测试与验收

### 12.1 凭据测试

- 跨重启可用；删除生效
- SQLite、日志、诊断包、报告和复现包中均不存在密钥
- 401、429、超时和非兼容 URL 状态正确

### 12.2 向导测试

- 模糊需求、缺失单位、未知材料、2D/3D 选择和高风险假设均正确提示
- 未经确认的 `AI_SUGGESTED` 字段不能冻结合同

### 12.3 几何与求解测试

- 所有基础模板分别通过 2D/3D MATLAB 最小求解
- PNG、CSV、MAT、JSON 掩膜正常导入
- 越界、空域、超大体素和恶意 MAT 被拒绝
- F0–F2 均记录真实 `matlab_mcp_2d/3d` provenance
- F3 未审批不启动
- MCP/MATLAB 失败严格标记 `MATLAB_INFRASTRUCTURE`，不出现 Python fallback

### 12.4 报告测试

- 每轮生成 Markdown，终止生成 Markdown+PDF
- 六章顺序、原始提示词、参数来源、假设、需求矩阵和审查结论完整
- 缺失字段不编造
- PDF 逐页渲染检查中文字体、分页、表格、图片清晰度和图注位置
- 文件清单与磁盘真实产物逐项一致

### 12.5 Agent 闭环测试

- Pi 能自主提出第二轮意图，用户无需给出第二轮数值参数
- Subagent 权限隔离有效
- Evaluator 证据先于 Agent 解释
- Reviewer 能识别多变量对比的因果歧义

### 12.6 Windows 验收

- 干净 Windows 10/11 安装，无需 Python、Node 或单独下载 MATLAB MCP
- 没有 MATLAB 时引导可用但求解被阻止
- 安装并验证 MATLAB 后可完成引导、F0–F3、阶段报告和最终交付
- 首版记录冷启动时间、热启动时间、MCP 调度时间、迭代时间、内存和加速比

---

## 十三、发布门禁

必须通过：

```text
desktop_app
v5_visual_consistency
research_contract
matlab_mcp_2d_real
matlab_mcp_3d_real
pi_qwen_api
pi_closed_loop
pi_second_round_without_manual_parameters
agent_provenance
subagent_isolation
safety_policy
research_memory
evaluator_first
websocket_realtime
matlab_mcp_2d
matlab_mcp_3d
strict_f3
human_approval_f3_only
i18n_zh_default
benchmark_pi_vs_baselines
knowledge_offline
guided_setup
round_report
final_report_pdf
reproducibility
credential_manager
```

任一硬门禁失败：

```text
release_ready = false
```

---

## 十四、锁定假设

- 首版仍为 Windows 10/11 x64，MATLAB R2024a 为签署环境
- Qwen 是正式模型来源，但允许任意兼容 OpenAI Chat Completions 的请求地址；不扩展为多供应商模型市场
- API Key 使用 Windows Credential Manager，仅来自用户手动输入
- 不支持 STL、STEP 或其他 CAD 导入；自定义几何仅限安全的二维/三维离散掩膜
- 旧 Research、旧 Python 结果和旧报告保持可读，不改写 provenance；只有新建 Research 使用 MATLAB-only 四级保真度
- 日常 Agent 事件继续采用 Observation、Evidence、Interpretation、Intent 和 Reason Summary；完整模板用于每轮阶段报告和最终汇总
- Pi Research Lead 常驻，Subagent 按任务隔离运行
- 只有 F3 强制人工审批；用户可在任意阶段暂停、编辑、拒绝或要求解释
- MATLAB MCP Server、Python sidecar、Node/Pi Runtime 和知识库随安装包提供
- MATLAB 本体和许可证不随应用打包，应用自动检测路径，也允许用户在设置中心修正
- 首版知识库为离线内置内容加 Research Memory，不包含在线文献检索和用户 PDF 导入
- V5/V6 视觉、设置中心、本地安全令牌、WebSocket Ticket 和报告模板继续沿用
- 不重写桌面框架、基础服务和 Policy Engine
