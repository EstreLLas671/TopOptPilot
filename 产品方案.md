# TopOptPilot Desktop V6.0 产品与实施方案（修订版）

## 一、产品定位

TopOptPilot 是面向拓扑优化研究人员的 Windows 原生闭环实验工作台。

系统以：

- Pi Agent + Qwen API 负责研究意图、实验规划、失败诊断和结果解释；
- Policy Engine 负责生成合法、可控的实验参数；
- Python 2D/3D 与真实 MATLAB 3D 负责物理计算；
- Evaluator 负责确定性指标计算和可行性判断；
- Human-in-the-Loop 负责高风险实验审批与专家干预。

核心闭环：

```text
Research Contract
→ Pi 选择研究意图
→ Policy 编译实验参数
→ Safety / Budget / Human Gate
→ 真实 FEM
→ Evaluator 生成证据
→ Pi 分析证据
→ 下一轮实验
→ 目标达成 / 停滞 / 预算耗尽
```

产品重点是证明：

> Pi 能否依据真实拓扑优化实验结果和预算，安全地决定下一次实验，而不依赖用户人工指定下一组参数。

---

## 二、产品视觉与前端原则

### 2.1 保留 V5 视觉风格

V6 不更换配色体系，继续沿用 V5：

- 深色 Codex/VS Code 式工作台；
- 蓝色作为主要交互与运行状态色；
- 绿色表示成功、可行和已验证；
- 琥珀色表示待审批和风险；
- 红色表示失败、约束违反和基础设施错误；
- 紫色表示人工干预；
- 灰色表示离线、未验证和不可用；
- 继续使用现有字体、间距、边框、按钮和卡片语言。

不重新设计主题系统，不引入新的蓝白渐变或独立视觉品牌。

### 2.2 科研内容优先

视觉中心依次为：

```text
Topology
→ Convergence
→ Metrics
→ Evaluator Evidence
→ Decision Chain
→ Agent Explanation
```

不使用大型机器人图标、AI 大脑插画和聊天气泡作为主视觉。

Pi 的输出显示为结构化研究事件：

- Observation；
- Interpretation；
- Hypothesis；
- Intent；
- Evidence Summary；
- Recommended Next Experiment。

不显示 Chain-of-Thought。

---

## 三、Workspace 信息架构

保留现有 Tauri 三栏桌面框架，升级中央区域。

```text
┌──────────────────────────────────────────────────────────────┐
│ TopOptPilot │ Research │ Mode │ Backend Health │ Settings   │
├──────────────┬──────────────────────────────┬────────────────┤
│ Research     │                              │ Evidence       │
│ Navigator    │     Experiment Canvas        │ Inspector      │
│              │                              │                │
│ Goal         │ PLAN / RUN / ANALYZE         │ Parameters     │
│ Constraints  │ DECIDE / COMPARE / TIMELINE  │ Metrics        │
│ Rounds       │                              │ Evaluator      │
│ Experiments  │                              │ Provenance     │
│ Decisions    │                              │ Budget         │
│ Reports      │                              │                │
├──────────────┴──────────────────────────────┴────────────────┤
│ Command Composer                           Approve │ Run     │
└──────────────────────────────────────────────────────────────┘
```

### 3.1 顶部 Backend Health Bar

分别显示：

- Pi RPC；
- Qwen API；
- Python 2D；
- Python 3D；
- MATLAB MCP；
- MATLAB；
- Desktop Sidecar。

统一状态：

```text
NOT_CONFIGURED
CONFIGURED
STARTING
READY
VERIFIED
DEGRADED
FAILED
```

`CONFIGURED` 不得显示为绿色可用。只有真实连接测试或最小求解通过后才能显示 `READY/VERIFIED`。

点击状态项显示版本、模型、启动时间、最近检查、最近错误、日志和重启操作。

### 3.2 Research Navigator

左侧按研究过程组织：

```text
Research MBB-001
├─ Goal
├─ Constraints
├─ Research Contract
├─ Round 1
│  ├─ E01 Baseline ✓
│  ├─ E02 Explore ✓
│  └─ E03 Disconnection ✕
├─ Round 2
│  ├─ E04 ▶
│  └─ E05 ○
├─ Decisions
├─ Evidence
├─ Pareto
├─ Report
└─ Files
```

已有实验按 Round 分组，不再只是平铺列表。

### 3.3 Evidence Inspector

右侧根据选中对象切换：

- Research：目标、约束、预算、当前最优、终止状态；
- Experiment：参数、后端、保真度、指标、风险、运行来源；
- Decision：Pi 意图、证据、Policy 结果、Safety、人工审批；
- Failure：失败类型、确定性证据、相关实验、可能解释；
- MATLAB：MATLAB/MCP 版本、求解入口、文件 SHA256、原始证据位置。

---

## 四、Research Setup 与 Contract

新建研究进入独立 Setup State，输入：

- 几何和尺寸；
- 材料；
- 载荷；
- 边界条件；
- 体积分数；
- 灰度率和连通性约束；
- 优化目标；
- 研究模式；
- 总预算及 F0/F1/F2/F3 分预算；
- 可选研究假设。

每个字段显示来源：

```text
USER
DEFAULT
AI_SUGGESTED
LOCKED
```

确认后生成不可变的 Research Contract。

Pi 不得修改：

- Research Goal；
- 用户锁定参数；
- 几何与工程边界；
- Safety Guard 范围；
- 已经完成实验的事实记录。

全局设置中的默认值只影响新 Research。

---

## 五、Experiment Canvas

### 5.1 PLAN

展示当前轮次、研究目的、相关证据、Pi Intent、Policy 生成的候选实验、成本和风险。

操作：

```text
Approve
Edit
Reject
Why
```

Edit 后必须重新经过 Policy 和 Safety，不允许直接绕过参数校验。

### 5.2 RUN

中央优先显示：

- 实时 topology；
- compliance；
- gray ratio；
- volume fraction/error；
- connectivity；
- convergence history；
- iteration 和进度；
- LIVE/CACHED/REPLAY 来源。

AI 文本不占据主要空间。

### 5.3 ANALYZE

顺序固定为：

```text
Raw FEM Result
→ Evaluator Verdict
→ Constraint Changes
→ Pi Interpretation
→ Competing Explanations
```

确定性 Evaluator 和 Pi 文本必须使用不同来源标签。

### 5.4 DECIDE

显示完整安全链：

```text
PI_AGENT
Intent
↓
POLICY_ENGINE
Candidate Experiment
↓
SAFETY_GUARD
PASS / DENY
↓
BUDGET
AVAILABLE / EXHAUSTED
↓
HUMAN_GATE
REQUIRED / AUTO
↓
EXECUTOR
READY
```

### 5.5 COMPARE

支持选择两个或多个实验。

差值、改善率和约束变化由后端确定性计算；Pi 只生成 Evidence Summary。

显示：

- 并排 topology；
- 参数差异；
- compliance/gray/volume/connectivity 差异；
- fidelity 和 backend；
- controlled comparison 判断。

### 5.6 TIMELINE

保留 V5 的研究事件流作为审计视图，显示：

- Agent 消息和工具调用；
- Policy/Safety 决策；
- 人工修改；
- FEM 生命周期；
- Evaluator 证据；
- MATLAB 生命周期；
- 报告和复现包事件。

---

## 六、Pi Agent 与 Qwen API

### 6.1 模型策略

正式主路径：

```text
Pi Agent RPC
+
Qwen API
+
DashScope/OpenAI-compatible endpoint
```

默认模型沿用当前配置 `qwen3.7-plus`，允许在设置中心修改模型 ID、Base URL、超时和重试次数。

界面显示：

```text
Runtime: Pi Agent
Provider: DashScope / Compatible Qwen API
Model: 当前实际模型 ID
```

API Key 只读取：

```text
DASHSCOPE_API_KEY
```

不显示、不写入 SQLite、不进入日志、报告和复现包。

保存模型设置后，新 Pi Session 生效；支持重启全部 Pi Session。

### 6.2 Agent 职责

Pi 负责：

- 理解研究目标；
- 选择科学意图；
- 判断需要查看哪些历史证据；
- 识别失败模式和竞争解释；
- 决定下一实验的研究方向；
- 决定是否建议提升保真度；
- 解释结果并形成研究叙述。

Pi 不负责：

- 直接生成最终数值参数；
- 直接调用 FEM；
- 直接调用 MATLAB MCP；
- 直接修改数据库；
- 计算指标和实验差值；
- 绕过 Safety、Budget 或 Human Gate。

### 6.3 Session 与 Research State

一个 Research 对应一个持久化 Pi Session。

```text
Pi Session = 推理历史
Research State = 科研事实
```

每轮决策前必须调用：

```text
research_get_context
research_get_budget
```

需要历史时调用：

```text
research_query_history
experiment_result
experiment_compare
failure_get_evidence
```

### 6.4 工具白名单

正式工具：

```text
research_get_context
research_query_history
research_get_budget
policy_compile_intent
experiment_preview
experiment_submit
experiment_status
experiment_result
experiment_compare
failure_get_evidence
research_get_pareto
```

禁用 shell、文件编辑、任意数据库修改、任意 MATLAB、原始 Solver 调用和内置 Coding Agent 工具。

### 6.5 Agent 贡献标记

每项实验和决策记录：

```text
decision_source:
  PI_AGENT
  RULE_FALLBACK
  HUMAN
  REPLAY

intent_source
policy_version
model
provider
session_id
evidence_ids
```

Pi 不可用时可以进入 Safe Mode，但必须明显显示 `RULE_FALLBACK`，且其结果不能计入 Pi benchmark。

---

## 七、Policy、Evaluator 与多保真求解

### 7.1 意图—参数解耦

Pi 只能提交稳定科学意图：

```text
ESTABLISH_BASELINE
EXPLORE_PARAMETER
REDUCE_GRAYNESS
RESTORE_CONNECTIVITY
TEST_COMPETING_EXPLANATIONS
UPGRADE_FIDELITY
VERIFY_CANDIDATE
```

Policy 根据意图、历史、锁定参数和预算生成合法实验。

### 7.2 保真度定义

```text
F0 = Python 2D coarse
F1 = Python 2D fine
F2 = Python 3D
F3 = REAL MATLAB 3D
```

F3 永远要求人工审批。

### 7.3 Evaluator

Evaluator 独立计算：

- compliance 有限性；
- volume fraction/error；
- gray ratio；
- connected components；
- convergence；
- constraint verdict；
- failure type；
- feasible/infeasible；
- Pareto 状态。

事件顺序必须保证 Evaluator 证据先于 Pi 解释进入 Workspace。

---

## 八、MATLAB MCP 与严格 F3

调用链固定为：

```text
Pi
→ experiment_submit
→ ResearchService
→ Policy / Approval
→ Executor
→ MatlabGateway
→ MATLAB MCP
→ topopt_run_task
→ MATLAB
```

Pi 不接触 MATLAB MCP 工具列表。

MatlabGateway 验证：

- Research Root 路径；
- JSON 后缀和 Schema；
- 参数边界；
- Experiment 与 Research 归属；
- Approval 状态；
- Fidelity = F3；
- 输出路径；
- Task Hash。

MATLAB MCP 使用单会话队列，`max_matlab_workers = 1`。

安装包记录：

```text
tested MCP version
MCP binary SHA256
MATLAB version
solver entry SHA256
```

F3 失败时：

```text
status = FAILED
failure_type = MATLAB_INFRASTRUCTURE
```

绝不回退到 Python。

保留错误、stderr、版本、task hash、时间戳和重试证据。

---

## 九、事件、实时通信与本地安全

统一 Event Envelope：

```json
{
  "event_id": "EV-...",
  "research_id": "MBB-001",
  "experiment_id": "E03",
  "timestamp": "...",
  "type": "EVALUATOR_RESULT",
  "source": "EVALUATOR",
  "payload": {}
}
```

主要事件：

```text
AGENT_MESSAGE
AGENT_TOOL_CALL
POLICY_DECISION
SAFETY_RESULT
HUMAN_DECISION
EXPERIMENT_QUEUED
EXPERIMENT_PROGRESS
EXPERIMENT_RESULT
EVALUATOR_RESULT
MATLAB_STATUS
FAILURE
REPORT_READY
```

桌面 API 仅绑定 `127.0.0.1` 和随机端口。

HTTP 使用桌面会话令牌；WebSocket 改为：

```text
Authenticated HTTP
→ 获取一次性 WS Ticket
→ 建立 WebSocket
→ Ticket 立即失效
```

正式 Workspace 依赖 WebSocket 推送，不使用周期性页面轮询。

---

## 十、设置中心

继续使用现有全屏 Settings Workspace，保留 V5 风格。

分区：

- 通用；
- Agent 与模型；
- MATLAB 与计算；
- 新研究默认值；
- 数据与诊断。

语言分三层：

- Global Locale：应用固定 UI；
- Research Locale：当前 Research；
- Agent Locale：后续 Pi 回复、错误解释和报告。

首次启动默认 `zh-CN`；历史 Agent 内容不回溯翻译。

---

## 十一、证明 Agent 科研贡献

### 11.1 正式案例

- Case A：自主完成 baseline、探索、改进和终止；
- Case B：断连后提出 `beta` 与 `rmin` 两种竞争解释并运行控制实验；
- Case C：从 F0/F1/F2 提升到人工审批的真实 MATLAB F3。

Case B 为主 Demo 和核心验收案例。

### 11.2 固定预算对照

比较：

```text
Random
Grid
TPE
Rule
Pi + Policy
```

主指标：

```text
Best Feasible Objective under Fixed Budget
```

辅助指标：

- Experiments-to-Feasible；
- Best Compliance；
- Gray Ratio；
- Constraint Violation Rate；
- High-Fidelity Runs；
- Human Interventions；
- Repeated Experiment Rate；
- Invalid Intent Rate；
- Safety Rejection Rate；
- Unnecessary Fidelity Upgrade Rate；
- Final Regret。

Pi benchmark 必须满足：

- 在线 Qwen API 成功；
- 第二轮实验意图来自 Pi 工具调用；
- 用户没有指定第二轮数值参数；
- 未触发 Rule Fallback；
- 所有结果来自真实 FEM；
- 记录完整模型、工具、Policy 和证据来源。

至少使用多个固定随机种子重复测试，报告均值、离散程度和失败次数。

---

## 十二、实施顺序

### 阶段一：协议与科研闭环

- 冻结 Research、Experiment、Result、Event、Intent 和 MATLAB Task Schema；
- 增加统一 provenance/source；
- 完成真实 Pi 第二轮闭环门禁；
- 将 Rule Fallback 与 Pi 成绩彻底分离。

### 阶段二：中央 Experiment Canvas

- 保留 V5 桌面壳和配色；
- 增加 PLAN/RUN/ANALYZE/DECIDE/COMPARE/TIMELINE；
- 将 topology、convergence 和 Evaluator 证据移到中央；
- 左侧按 Round 分组；
- Inspector 支持 Experiment/Decision/Failure/MATLAB。

### 阶段三：基础设施可信性

- 完整 Backend Health Bar；
- WS ticket；
- MatlabGateway；
- MCP 测试版本和 SHA256；
- 严格 F3 失败、重启和重试；
- LIVE/CACHED/REPLAY 来源标记。

### 阶段四：案例、基线与发布

- 冻结 Case A/B/C；
- 运行 Random/Grid/TPE/Rule/Pi 对照；
- 完成中英文报告和复现包；
- Fresh Install、Windows 10/11、Qwen 失败、MCP Kill、MATLAB License Failure 和重启恢复测试。

---

## 十三、发布门禁

必须通过：

```text
desktop_app
v5_visual_consistency
research_contract
python_2d_real
python_3d_real
pi_qwen_api
pi_closed_loop
pi_second_round_without_manual_parameters
agent_provenance
safety_policy
research_memory
evaluator_first
websocket_realtime
matlab_mcp_2d
matlab_mcp_3d
strict_f3
human_approval
i18n_zh_default
benchmark_pi_vs_baselines
reproducibility
```

任一硬门禁失败：

```text
release_ready = false
```

---

## 十四、锁定决策

- 保留 V5 深色视觉和配色，不进行品牌级换肤。
- 产品定位为拓扑优化实验闭环工作台。
- 中央区域升级为科研状态画布，Timeline 继续保留。
- 不突出聊天机器人形象，真实 topology、曲线和证据优先。
- 正式模型路径为 Pi Agent + Qwen API，当前默认 `qwen3.7-plus`。
- API Key 仅来自环境变量。
- Pi 只选择科学意图，Policy 拥有数值参数。
- Safe Mode 必须标记为 Rule Fallback。
- F3 只代表真实 MATLAB 3D，永不 Python 回退。
- V6 在当前 Tauri V5 工程上增量实现，不重写桌面框架、设置中心和基础服务。
