# 赛题 B 项缺口分析

> 对标要求：**"作品重点不是生成一次性实验方案，而是展示AI如何根据实验结果调整下一轮计划，并逐步提升实验成效。"**

---

## 一、评分项 → 当前状态映射

| 赛题要求 | 当前状态 | 差距等级 |
|----------|---------|---------|
| 根据研究目标、约束条件、已有数据做任务规划 | ✅ LLM 驱动的实验设计已实现 | 无差距 |
| 实验运行与数据获取 | ⚠️ ExperimentQueue 存在但生成随机占位数据，非真实物理一致结果 | **中** |
| 数据分析与反馈迭代 | ⚠️ 审计 Agent 存在，LLM 分析结果，但迭代逻辑是固定 pipeline | **高** |
| **AI 根据实验结果调整下一轮计划** | ❌ 当前迭代回到文献挖掘重来，不利用上一轮结果调整参数 | **最高** |
| **逐步提升实验成效** | ❌ 无跨轮成效追踪，无法展示 "失败→分析→调整→改善" 链 | **最高** |
| 不是一次性实验方案 | ⚠️ 有多轮迭代，但每轮独立，不构成递进关系 | **高** |

---

## 二、6 个具体缺口

### 缺口 #1 — 最高优先级：实验方案不随上一轮结果变化

**现状**：每轮迭代中，实验 Agent 独立生成实验矩阵，不参考上一轮的实验结果。

```python
# orchestrator.py 第 103-113 行 — 每轮固定 pipeline
while not self.sm.should_stop():
    self._phase_hypothesis_generation_llm()      # 重新生成假设
    self._phase_hypothesis_review_llm()           # 重新审稿
    self._phase_experiment_design_llm(task_package)  # 重新设计实验
    self._phase_experiment_execution_async()      # 执行
    self._phase_result_audit_llm()                # 审计
```

**应该做的**：审计 Agent 的输出应直接驱动下一轮实验设计的变化：

```python
# 应该这样
while not self.sm.should_stop():
    # 第一轮：全量实验
    if iteration == 0:
        self._phase_full_experiment_design()
    
    # 后续轮次：基于审计结果做参数调整
    else:
        self._phase_parameter_mutation_based_on_audit()
        # 只调整关键参数，不重新生成假设和实验矩阵
```

**影响**：这是赛题 B 的核心——**展示 AI 如何根据结果调整下一轮**。现在完全没做到。

---

### 缺口 #2 — 最高优先级：无跨轮成效追踪

**现状**：审计 Agent 每轮独立输出裁决，不追踪指标的变化趋势。

**应该做的**：

```python
class ProgressiveTracker:
    """跨轮成效追踪器"""
    
    def __init__(self):
        self.history = []  # [{iteration, experiment_group, metrics}]
    
    def record(self, iteration, results):
        """记录本轮成效"""
        summary = {
            "iteration": iteration,
            "best_compliance": min(r.compliance for r in results),
            "best_gray_ratio": min(r.gray_ratio for r in results),
            "connected_rate": sum(1 for r in results if r.connected) / len(results),
        }
        self.history.append(summary)
    
    def get_progress_report(self) -> dict:
        """生成成效提升报告"""
        if len(self.history) < 2:
            return {"message": "需要至少两轮数据"}
        
        first = self.history[0]
        last = self.history[-1]
        return {
            "compliance_improvement": f"{(first['best_compliance'] - last['best_compliance']) / first['best_compliance'] * 100:.1f}%",
            "gray_ratio_improvement": f"{(first['best_gray_ratio'] - last['best_gray_ratio']) / first['best_gray_ratio'] * 100:.1f}%",
            "connected_rate_improvement": f"{(last['connected_rate'] - first['connected_rate']) * 100:.1f}%",
        }
```

**影响**：评委要看到 **"逐步提升实验成效"**，没有跨轮追踪就展示不了。

---

### 缺口 #3 — 高优先级：模拟结果与实验参数无关

**现状**：`ExperimentQueue._simulate_run()` 生成随机结果：

```python
# experiment_queue.py 第 77-101 行
result = {
    "compliance": round(random.uniform(100, 200), 1),         # 纯随机
    "gray_ratio": round(random.uniform(0.02, 0.15), 3),       # 纯随机
    "connected_components": random.choice([1, 1, 1, 2]),       # 纯随机
}
```

**问题**：高 beta 投影和高惩罚指数会产生更低灰度但更高柔度的结果——但当前随机生成完全不反映这种物理关系。

**应该做的**：

```python
def _simulate_physics_aware(self, task):
    """参数感知的模拟（反映真实物理趋势）"""
    beta = task.params.get("beta_max", 8)
    penal = task.params.get("p_start", 3)
    has_heaviside = task.projection == "heaviside_projection"
    has_feedback = "feedback" in task.controller
    
    # 物理趋势：高 beta → 低灰度但可能断连
    base_gray = 0.12 if not has_heaviside else 0.08
    gray = base_gray * (8 / beta)  # beta 越高灰度越低
    gray = min(max(gray, 0.02), 0.20)
    
    # 物理趋势：高 beta + 无反馈 → 可能断连
    if has_heaviside and beta > 12 and not has_feedback:
        connected = random.choices([1, 2, 3], weights=[0.3, 0.5, 0.2])[0]
    else:
        connected = 1
    
    # 物理趋势：联合反馈 → 灰度最低且保持连通
    if has_feedback and "joint" in task.controller:
        gray *= 0.6  # 联合反馈进一步降低灰度
        connected = 1
```

**影响**：如果 beta=4 和 beta=32 产生相同的随机灰度，系统无法展示"调参→改善"的因果关系。

---

### 缺口 #4 — 高优先级：固定 pipeline，不智能跳过

**现状**：每轮都完整执行 8 个 Phase。

**应该做的**：引入迭代类型概念：

```python
class IterationType(Enum):
    FULL = "full"                    # 完整 8 Phase（首轮）
    PARAMETER_TUNE = "parameter_tune"  # 仅调整参数（后续轮）
    HYPOTHESIS_REFINE = "hypothesis_refine"  # 仅精炼假设
    DISCRIMINATING = "discriminating"  # 仅做区分性实验
```

```python
if iteration == 0:
    # 首轮：完整 pipeline
    ...
elif audit_says_parameter_tuning:
    # 参数调整轮：跳过文献挖掘和假设生成
    self._phase_parameter_tune()
    self._phase_execute()
    self._phase_audit()
elif audit_says_hypothesis_refinement:
    # 假设精炼轮：跳过文献挖掘
    self._phase_hypothesis_refine()
    self._phase_experiment_design()
    self._phase_execute()
    self._phase_audit()
```

---

### 缺口 #5 — 中优先级：无"失败案例 → 分析 → 修复"的完整记录

**现状**：审计 Agent 输出裁决，但没有保存完整的"失败上下文"供下一轮使用。

**应该做的**：

```python
@dataclass
class FailureRecord:
    """失败记录（供下一轮参考）"""
    iteration: int
    experiment_id: str
    hypothesis_id: str
    symptoms: list[str]        # ["gray_ratio_high", "disconnected", "compliance_spike"]
    diagnosed_cause: str      # "投影过于激进 (beta=16 at iter 5)"
    recommended_fix: dict     # {"beta": 4, "delay_projection": True}
    fixed_in_iteration: Optional[int] = None
    fix_result: Optional[str] = None  # "success" | "partial" | "failed"
```

审计 Agent 输出 `recommended_fix` → 下一轮实验设计直接应用 → 展示"同样的假设，调整参数后成功了"。

---

### 缺口 #6 — 中优先级：【已规划但未实现】三个缺失状态

架构文档已识别但未实现的三个状态：

| 缺失状态 | 用途 | 代码状态 |
|---------|------|---------|
| HYPOTHESIS_REFINEMENT | 保留假设方向，仅调整参数/条件 | ❌ 未实现 |
| SOLVER_DIAGNOSIS | GPU/CPU 偏差过大时诊断求解器 | ❌ 未实现 |
| DISCRIMINATING_EXPERIMENT | 两个假设无法区分时设计对比实验 | ❌ 未实现 |

---

## 三、修复优先级

```
P0 — 赛题 B 核心要求（不改无法满足赛题）
  └── #1 实验矩阵随结果变化    (orchestrator.py, experiment_agent.py)
  └── #2 跨轮成效追踪          (progressive_tracker.py — 新文件)
  
P1 — 演示说服力（不改演示说服力不够）
  └── #3 物理一致的模拟结果    (experiment_queue.py)
  └── #4 智能迭代类型          (state_machine.py)

P2 — 加分项
  └── #5 失败→修复完整记录     (audit_agent.py)
  └── #6 三个缺失状态          (state_machine.py + 新角色)
```

---

## 四、修复后的演示叙事

```
迭代 0 (首轮):
  B0: 无投影基线 → gray=0.152, disconnected ❌
  B1: 固定调度  → gray=0.089, connected  ✅ 但 compliance 偏高
  
迭代 1 (审计发现 B0 失败，B1 部分成功):
  → Agent 决策: 放弃 B0，精炼 B1 参数
  → B1.1: beta_step=1 (更温和) → gray=0.062, compliance 改善 ✅
  
迭代 2 (审计对比 B1 vs B1.1):
  → Agent 决策: 尝试联合反馈
  → Ours: joint_feedback → gray=0.037, compliance 最优 ✅✅

最终成效:
  灰度: 0.152 → 0.089 → 0.062 → 0.037 (↓75.6%)
  柔度: 195.2 → 148.7 → 135.4 → 128.4 (↓34.2%)
  连通: ❌ → ✅ → ✅ → ✅
```

这才是赛题 B 要求的 **"不是一次性方案，而是逐步提升"**。