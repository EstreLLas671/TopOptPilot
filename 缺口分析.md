# TopOptPilot V6.1.1 缺口分析

> **V6.1.1 当前状态**：V6.1.0 的 7 项缺口（凭据管理 / AI 引导向导 / Subagent 审查 / MATLAB 全保真度 / 离线知识库 / 模板报告 / 工程模板+掩膜）均已实现并落地；
> V6.1.1 补齐方案文档与代码的最后偏差：求解变体等价性验证、MATLAB 启动预热与 SOLVER_WARMUP 事件、MCP 启动/求解耗时统计、
> API 命名对齐（agent-credential、guide/parse）、PDF 中文字体显式回退，以及缓存目录可配置与迁移。
> 以下分析同时覆盖 V6.0.0 已关闭缺口和 V6.1.x 待实施缺口。

> 对标要求：**"作品重点不是生成一次性实验方案，而是展示 AI 如何根据实验结果调整下一轮计划，并逐步提升实验成效。"**

---

## 一、评分项 → 当前状态映射（V6.0.0 基线 + V6.1.0 升级方向）

| 赛题要求 | 当前状态 | 差距等级 |
|----------|---------|---------|
| 根据研究目标、约束条件、已有数据做任务规划 | ✅ IntentCompiler + Pi RPC 驱动，意图编译为合法 FEM 配置 | 无差距 |
| 实验运行与数据获取 | ✅ solver/ 真实 FEM（Python 2D/3D + MATLAB MCP），随机模拟仅保留 legacy 路径 | 无差距 |
| 数据分析与反馈迭代 | ✅ Memory L2/L3 + 13 项 campaign_metrics + 10 状态机 + Reviewer 子工作流 | 无差距 |
| **AI 根据实验结果调整下一轮计划** | ✅ IntentCompiler.compile() 按科学意图（REDUCE_GRAYNESS / RESTORE_CONNECTIVITY / TEST_COMPETING_EXPLANATIONS 等）映射到差异化实验配置；Evaluator.detect_failure() 触发下一动作 | 无差距 |
| **逐步提升实验成效** | ✅ Memory L2 parameter_trends + pareto_candidates + known_failures 自动追踪；release_audit cases C 实机验证 F0→F1→F2→F3 逐保真提升 | 无差距 |
| 不是一次性实验方案 | ✅ 多保真（F0/F1/F2/F3）+ 多轮迭代 + 证据链 + 一键复现包 | 无差距 |

---

## 二、已关闭的缺口（V4.1 → V6.0.0）

### ✅ 缺口 #1 — 实验方案随上一轮结果变化（最高优先级）

**现状**：`topoptpilot/policy/intent_compiler.py` 的 `IntentCompiler.compile()` 将 7 种科学意图映射为差异化实验配置：

| IntentType | 行为 |
|-----------|------|
| `ESTABLISH_BASELINE` | F0 基线：beta=1.0, rmin=1.5, penal=3.0 |
| `EXPLORE_PARAMETER` | 扫描 factor levels（beta: [2,4,8], rmin: [1.25,1.75,2.25], penal: [2,3,4]） |
| `REDUCE_GRAYNESS` | beta 翻倍（clamped [2, 32]） |
| `RESTORE_CONNECTIVITY` | 两个候选：减半 beta 或 rmin 增 0.5 |
| `TEST_COMPETING_EXPLANATIONS` | 委托给 `doe_templates.discriminating_experiments()` 三组命名模板 |
| `UPGRADE_FIDELITY` | 由 FidelityManager 升保真 |
| `VERIFY_CANDIDATE` | 同上 |

**验证文件**：
- `topoptpilot/policy/intent_compiler.py`
- `topoptpilot/policy/doe_templates.py`（beta_vs_rmin / beta_vs_penal / projection_vs_controller）
- `topoptpilot/orchestrator/research_orchestrator.py`（analyze() → evaluate_result → choose_action → 下一意图）

---

### ✅ 缺口 #2 — 跨轮成效追踪（最高优先级）

**现状**：`topoptpilot/memory/research_memory.py` 的 `ResearchMemory.build()` 自动构建 L2 Scientific Memory：

```python
L2 = {
    "best_candidate":       # 最高保真下柔度最低的可行结果
    "known_failures":       # 最近 12 个 DISCONNECTION/HIGH_GRAY 失败
    "parameter_trends":     # beta/rmin/penal 对 compliance/gray/components 的时序趋势
    "pareto_candidates":    # 同保真下柔度 vs 灰度的 Pareto 前沿
}
```

**验证文件**：
- `topoptpilot/memory/research_memory.py`（`_scientific_memory()`）
- `topoptpilot/memory/research_state.py`（SQLite 持久化，8 张表含 app_settings）
- `topoptpilot/benchmarks/metrics.py`（13 项 campaign_metrics，含 experiments_to_feasible / final_regret / repeated_experiment_rate）

---

### ✅ 缺口 #3 — 真实求解替代随机模拟（高优先级）

**现状**：`solver/` 是完整的真实 FEM 引擎：

| 文件 | 职责 |
|------|------|
| `fe_solver.py` | 2D Quad4 FEM + SIMP 组装 + 5 种边界条件 + scipy 稀疏直解 |
| `oc_solver.py` | OC 最优性准则 + Lagrange 二分 + passive 掩码 |
| `filters.py` | 灵敏度/密度滤波 + Heaviside 投影 + 链式伴随 |
| `continuation.py` | 4 种控制器（fixed/periodic/gray_feedback/joint_feedback） |
| `topopt3d.py` | 3D Hex8 FEM，Gauss 2×2×2 |
| `matlab_backend.py` | MATLAB Engine 双后端（自动降级） |
| `matlab3d_adapter.py` | F3 已验证回放（**禁止** Python 伪回退） |
| `mcp/matlab_mcp/` | 官方 MathWorks MATLAB MCP Server v0.12.0，JSON-RPC 2.0 |

`solver/` 目录内 **零** `random` / `np.random` 调用，全程确定性。
旧 `ExperimentQueue._simulate_run` 仅作为 `backend="simulate"` legacy 路径保留
（`experiments/solver_runner.py`，明确标注为"演示预计算"）。

**验证文件**：
- `solver/topopt_engine.py`（run_topopt）
- `solver/topopt3d.py`（run_topopt3d）
- `mcp/matlab_mcp/matlab_connector.py` + `topopt_run_task.m`
- `tests/test_solver.py`（T3: 与 MATLAB top99 真值逐点核对 iter1=391.91, iter60=127.69, ±2%）

---

### ✅ 缺口 #4 — 智能迭代类型（高优先级）

**现状**：状态机保持 10 个显式状态（不引入新状态），但**科学意图分化**通过
`IntentCompiler` + `Evaluator.choose_action()` + `FailureDetector` 的组合实现：

```
结果 → Evaluator.evaluate_result()
     → 失败 → detect_failure() 识别 5 类失败（INFRASTRUCTURE/VOLUME/
              DISCONNECTION/HIGH_GRAY/OSCILLATION）
     → ActionPolicy.choose_action() 映射到 4 类下一动作：
         PROMOTE_OR_REPORT / RESTORE_CONNECTIVITY / REDUCE_GRAYNESS / RETRY_OR_REVISE
     → IntentCompiler.compile(intent) 编译成差异化 FEM 配置
```

`doe_templates.discriminating_experiments()` 提供 3 个命名模板：beta_vs_rmin、
beta_vs_penal、projection_vs_controller，用于无法区分的假设对。

**验证文件**：
- `topoptpilot/evaluator/evaluator.py`（evaluate_result）
- `topoptpilot/evaluator/failure_detector.py`（5 类失败）
- `topoptpilot/policy/action_policy.py`（choose_action）
- `topoptpilot/policy/doe_templates.py`（discriminating_experiments）

---

### ✅ 缺口 #5 — 失败 → 分析 → 修复完整记录（中优先级）

**现状**：Memory L2 `known_failures` 保存最近 12 个 DISCONNECTION / HIGH_GRAY
类型失败（含 evidence + 时间戳），events append-only 流保留所有事件，decisions 表
记录人工审批 + 理由 + 提议配置 + 风险等级。

**验证文件**：
- `topoptpilot/memory/research_state.py`（events / decisions 表）
- `topoptpilot/memory/research_memory.py`（`known_failures`）
- `topoptpilot/agent_runtime/reviewer.py`（ReviewerWorkflow 4 类触发器：
  HIGH_FIDELITY_ESCALATION / HYPOTHESIS_REVISION / RESEARCH_CONCLUSION /
  AMBIGUOUS_FAILURE）

---

### ✅ 缺口 #6 — 区分性实验（中优先级）

**现状**：`IntentCompiler.TEST_COMPETING_EXPLANATIONS` 委托
`doe_templates.discriminating_experiments(template, control)`，支持 3 个命名
对比模板。`HYPOTHESIS_REFINEMENT` 与 `SOLVER_DIAGNOSIS` 未作为独立状态实现，
其功能由 IntentCompiler 参数变异 + FailureDetector 诊断覆盖。

---

## 三、仍开放的缺口（V6.0.0 遗留）

### ⚠️ 遗留 #1 — Paper-to-Plugin 流水线核心环节

**现状**：支撑基础设施已就位，但核心流水线仍是 stub。

| 文件 | 状态 |
|------|------|
| `knowledge/evidence_db.py` | ✅ 实现（JSON 存储、query/add/statistics） |
| `knowledge/method_card.py` | ✅ 实现（YAML 模板 + format_method_card） |
| `agent/tools/reference_checker.py` | ✅ 实现（DOI 正则 + 页码 claim 校验） |
| `agent/tools/paper_reader.py` | ⚠️ 部分实现（PyMuPDF 抽文本 + 启发式章节抽取；无 LLM/结构化） |
| `knowledge/paper_processor.py` | ❌ stub（仅 docstring，零代码） |
| `demo/paper_to_plugin.py` | ❌ stub（9 步列表 + 硬编码返回 dict） |
| `knowledge/storage/` 三个目录 | ❌ 空（无实际方法卡片数据） |

**缺失**：
- 公式/参数提取（需要 LLM/多模态模型）
- 方法卡片自动填充
- 端到端流水线编排
- 任何模块间的 import 串联
- 测试覆盖

**影响**：当前 Paper-to-Plugin 是"演示叙事 + 手工方法卡片"，不是"上传 PDF 一键生成插件"。
Demo 仍可运行（demo/paper_to_plugin.py 返回硬编码结果），但评委无法实机上传论文复现。

---

### ⚠️ 遗留 #2 — 知识库三级架构（SQLite + FAISS）

**现状**：`knowledge/` 仍为 JSON 文件 + 关键词匹配。`topoptpilot/memory/research_state.py`
的 SQLite（8 张表）用于会话/实验/事件/决策/应用设置持久化，但**未用于论文证据检索**。

**目标架构**：
```
L1 文件系统 → PDF/STL/MAT
L2 SQLite+FTS5 → 结构化元数据 + 全文索引
L3 FAISS → 向量嵌入 + 语义检索
```

**当前差距**：L2/L3 未实现。`agent/tools/paper_reader.py` 和 `EvidenceDB` 都基于关键词。

---

### ⚠️ 遗留 #3 — CUDA MEX 求解器 MCP

**现状**：`mcp/solver_mcp/solver_mcp_server.py` 与 `mex_handle_manager.py` 均为
docstring-only 占位，无实现。

**实际求解路径**：Python 2D/3D FEM（默认）或官方 MATLAB MCP Server（F3）。
CUDA MEX 通道对 V5.1 无功能影响。

---

### ⚠️ 遗留 #4 — online_qwen 发布门禁

**现状**：`release_audit.py` 的 online_qwen 门禁需要有效 `DASHSCOPE_API_KEY`
启动 Pi 运行时并调用 `research_get_context` 工具。当前凭据返回 HTTP 401，
门禁被标记为 `skipped`，`release_ready = false`（离线门禁 `offline_release_ready = true`）。

**性质**：外部凭据问题，非代码缺陷。换有效 Key 即可通过。

---

## 四、修复优先级（V6.0.0 视角）

```
P0 — 赛题核心（已全部关闭）
  ✅ #1 实验矩阵随结果变化 → IntentCompiler.compile()
  ✅ #2 跨轮成效追踪 → Memory L2 parameter_trends / pareto_candidates
  ✅ #3 真实物理求解 → solver/（零 random）+ MATLAB MCP
  ✅ #5 失败→修复记录 → Memory L2 known_failures + ReviewerWorkflow

P1 — 演示说服力（大部分已关闭）
  ✅ #4 智能迭代类型 → IntentCompiler + FailureDetector + ActionPolicy
  ✅ #6 区分性实验 → doe_templates.discriminating_experiments()
  ⚠️ 遗留 #1 Paper-to-Plugin → 需 LLM 集成（公式/参数提取）
  ⚠️ 遗留 #4 online_qwen → 更换有效 API Key

P2 — 加分项（V5.1 未排期）
  ⚠️ 遗留 #2 SQLite+FAISS 知识库
  ⚠️ 遗留 #3 CUDA MEX MCP
  ⚠️ 假设精炼/求解器诊断（作为独立状态未引入；功能由 IntentCompiler 覆盖）
```

---

## 五、V6.0.0 演示叙事（实机验证）

以下数据来自 `release_audit.json`（2026-08-21T16:50:23+0800）：

### 案例 C — 多保真逐步提升（F0→F1→F2→F3）

```
迭代轨迹（7 次实验）：
  F0 × 4  → F1 × 1 → F2 × 1 → F3 × 1
  后端: python → python3d → matlab_mcp_3d

证据链：
  每次 F3 严格通过 run_matlab3d_or_replay，失败时记为 MATLAB_INFRASTRUCTURE，
  不降级到 Python；最后一轮包含真实 MATLAB 3D 求解（topopt3d_main.m）。
```

### 基线矩阵（5 方法等预算对比，budget=5, max_iter=40）

| 方法 | best_compliance | best_gray_ratio | 首个可行实验序号 | total_fem_cost |
|------|-----------------|-----------------|------------------|----------------|
| Random | 102.27 | 0.0556 | — | 5 |
| Grid | 112.36 | 0.2267 | — | 5 |
| **TPE** | **98.72** | **0.0089** | **2** | **5** |
| Rule | 122.44 | 0.2778 | — | 5 |
| Pi | 实机运行 | 实机运行 | 实机运行 | 实机运行 |

### Safe Mode 验证

```
预算=2 的自主研究在 BUDGET_EXHAUSTED 下正常终止；
FEM 残差 2.87e-13 / 3.40e-13，证明规则策略下仍为真实物理求解。
意图：ESTABLISH_BASELINE, REDUCE_GRAYNESS。
```

---

## 六、发布门禁结果（`python -m topoptpilot.release_audit`）

| Gate | Pass |
|------|:----:|
| artifacts（6 技能/11 工具/研究章程） | ✅ |
| desktop_app（EXE + NSIS 安装包） | ✅ |
| strict_f3（禁止 Python 伪回退） | ✅ |
| i18n_zh_default（中文默认） | ✅ |
| matlab_mcp_2d（MATLAB R2024a + MCP 0.12.0） | ✅ |
| matlab_mcp_3d（MATLAB R2024a + MCP 0.12.0） | ✅ |
| cases（A/B/C 三案例闭环） | ✅ |
| baselines（Random/Grid/TPE/Rule 等预算） | ✅ |
| safe_mode（规则策略下残差 <1e-3） | ✅ |
| online_qwen | ⚠️ skipped（外部凭据） |

**结论**：`offline_release_ready = true`。全部核心赛题要求已满足，唯一阻塞为外部 API 凭据。

---

## 七、V6.1.x 实施缺口（V6.1.1 全部落地）

### ✅ V6.1 缺口 #1 — Qwen API Key 凭据管理

**现状**：API Key 仅从 `.env`（`DASHSCOPE_API_KEY`）读取，无 GUI 管理。

**V6.1 目标**：设置中心支持 API Key 填写 + 密码框 + 连接测试，密钥保存到 Windows Credential Manager（目标 `TopOptPilot/QwenOpenAICompatible`），支持任意 OpenAI-compatible Base URL。

**实现文件**（已落地）：
- `topoptpilot/security/credentials.py` — Windows Credential Manager 读写（目标 `TopOptPilot/QwenOpenAICompatible`）
- `topoptpilot/service/research_service.py` — 凭据状态 API
- `topoptpilot/api/fastapi_app.py` — PUT/DELETE `/api/settings/agent-credential`（兼容 agent-key）+ POST `/api/settings/test-agent`
- `desktop/src/SettingsWorkspace.tsx` — 凭据 UI（密码框/删除/测试连接）
- `agent/llm/client.py` — 密钥只注入 Pi 子进程环境

**验收**：密钥不出现在 SQLite、日志、诊断包或报告中；删除凭据后重启不可用；非兼容 URL 状态正确。

---

### ✅ V6.1 缺口 #2 — AI 引导式 Research Setup

**现状**：新建研究需要用户直接填写结构化参数（CreateResearch 模态框）。

**V6.1 目标**：6 步引导向导（自然语言描述 → 结构维度 → 材料单位 → 载荷约束 → 目标预算 → 合同确认），AI 解析自然语言为结构化草案，字段标记来源（USER/DEFAULT/AI_SUGGESTED/LOCKED），逐项确认后冻结 Research Contract。

**实现文件**（已落地）：
- `topoptpilot/service/research_service.py`（`preview_guidance`/`guide_research`）+ `topoptpilot/schemas/models.py` — AI 解析 + 字段来源标记（USER/DEFAULT/AI_SUGGESTED/LOCKED）
- `topoptpilot/api/fastapi_app.py` — `POST /api/research/guide/parse` 与 `POST /api/guide`
- `desktop/src/ResearchSetup.tsx` — 引导式 Setup UI（含折叠式专家设置与 MATLAB 就绪检查）
- Guide 知识来自离线知识库

**验收**：模糊需求、缺失单位、未知材料、高风险假设均正确提示；未确认的 AI_SUGGESTED 字段不冻结合同。

---

### ✅ V6.1 缺口 #3 — Subagent 多角色审查

**现状**：Pi Research Lead 单角色处理所有任务；Reviewer 仅在特定触发器激活。

**V6.1 目标**：常驻 Pi Research Lead + 6 个 Subagent（Guide/Hypothesis/Planner/Executor/Reviewer/Report Writer），各自独立工具白名单，通过 `subagent_dispatch`/`subagent_status` 工具调度。

**实现文件**（已落地）：
- `topoptpilot/agent_runtime/subagents.py` — Subagent Session 管理 + 6 角色固定工具白名单
- `topoptpilot/agent_runtime/pi_bridge.py` — Subagent 调度协议与事件
- `topoptpilot/tools/research_tools.py` — `subagent_dispatch`/`subagent_status`
- `topoptpilot/api/fastapi_app.py` — `GET /api/research/{id}/agent-tasks`
- WebSocket 事件：`SUBAGENT_STARTED`/`SUBAGENT_RESULT`/`HYPOTHESIS_CREATED`/`REVIEW_VERDICT`/`SOLVER_WARMUP`

**验收**：Subagent 权限隔离有效；歧义失败/保真度升级/最终结论必须触发 Independent Reviewer。

---

### ✅ V6.1 缺口 #4 — MATLAB 全保真度求解

**现状**：F0/F1/F2 使用 Python FEM，仅 F3 使用 MATLAB MCP。

**V6.1 目标**：F0/F1 为 MATLAB 2D（coarse/fine），F2/F3 为 MATLAB 3D（coarse/fine），所有正式实验经 MATLAB MCP。F0–F2 可自动运行，F3 强制人工审批。Python 求解器退出正式执行路径。

**实现文件**（已落地）：
- `topoptpilot/fidelity/manager.py` — F0/F1=MATLAB 2D、F2/F3=MATLAB 3D
- `mcp/matlab_mcp/matlab_connector.py`/`matlab_mcp_server.py` — 2D/3D 统一调度、预热与耗时统计
- `topoptpilot/policy/approval_policy.py` — 仅 F3 强制人工审批
- `topoptpilot/benchmarks/equivalence.py` — `matlab_equivalence` 变体等价性门禁
- `求解器模块/` — MATLAB 2D/3D 求解入口（topopt_main / topopt3d_main）

**验收**：F0–F2 均记录 `matlab_mcp_2d/3d` provenance；F3 未审批不启动；MCP 失败严格标记 `MATLAB_INFRASTRUCTURE`，无 Python fallback。

---

### ✅ V6.1 缺口 #5 — 离线知识库 + Knowledge Center

**现状**：`knowledge/` 为 JSON 文件 + 关键词匹配，SQLite 仅用于会话/实验持久化。

**V6.1 目标**：SQLite FTS5 全文检索 + 模板案例 + 种子知识数据；用户可在 Knowledge Center 查看，Agent 在规划前检索。

**实现文件**（已落地）：
- `topoptpilot/knowledge/base.py` — SQLite FTS5 知识库（14 篇双语文档）
- `topoptpilot/knowledge/documents/` — 种子知识数据（SIMP/OC/滤波/投影/模板/失败模式等）
- `topoptpilot/api/fastapi_app.py` — `GET /api/knowledge/search` + `GET /api/knowledge/{id}`
- `topoptpilot/tools/research_tools.py` — `knowledge_search`/`knowledge_get`/`solver_get_capabilities`
- `desktop/src/KnowledgeCenter.tsx` — 知识库界面
- WebSocket 事件：`KNOWLEDGE_REFERENCED`/`SOLVER_CAPABILITY`/`SOLVER_WARMUP`

**验收**：Agent 规划前成功检索相关知识；用户在 Knowledge Center 可查看参数/错误/实验解释。

---

### ✅ V6.1 缺口 #6 — 模板报告生成

**现状**：报告生成（`assets/report_generator.py`）为简单 Markdown，无 PDF 输出，无阶段报告。

**V6.1 目标**：每轮生成 Markdown 阶段报告（round-XXXX.md），研究终止时生成 Markdown + PDF。报告六章结构（概述/假设设计/参数配置/求解结果/对比分析/人工干预），数据全部来自 Research State。

**实现文件**（已落地）：
- `topoptpilot/reports/generator.py` — 确定性六章报告组装（round-XXXX.md + final-report.md）
- `topoptpilot/reports/generator.py`（`render_pdf`）— PyMuPDF 渲染 PDF，中文字体显式回退（Microsoft YaHei/PingFang SC/Noto Sans CJK SC）
- `topoptpilot/service/research_service.py` — 阶段报告与终止报告调度，`RULE_FALLBACK` 标记
- Report Writer Subagent 只读已确认事实

**验收**：每轮生成 Markdown，终止生成 Markdown+PDF；缺失字段不编造；PDF 中文渲染正确。

---

### ✅ V6.1 缺口 #7 — 工程模板 + 掩膜导入

**现状**：仅支持编程式几何配置，无工程模板和掩膜导入。

**V6.1 目标**：2D/3D 工程基础模板（MBB/悬臂梁/简支梁/L 型/桥式），掩膜导入（PNG/CSV/MAT/JSON）。

**实现文件**（已落地）：
- `mcp/matlab_mcp/topopt_prepare_geometry.m` — MATLAB 生成 MBB/悬臂梁/简支梁/L 型/桥式模板掩膜 + 载荷/支撑节点映射
- `mcp/matlab_mcp/gateway.py` — 几何资产校验（维度/尺寸/哈希/任务 Schema）
- `topoptpilot/api/fastapi_app.py` — `POST /api/solvers/geometry-preview`（掩膜预览）
- `desktop/src/ResearchSetup.tsx` — 模板选择与掩膜配置 UI

**验收**：模板分别通过 2D/3D MATLAB 最小求解；掩膜导入正确解析；非法输入被拒绝。

---

## 八、V6.1.0 修复优先级

```
P0 — V6.1 核心（✅ V6.1.1 全部完成）
  ✅ #1 Qwen 凭据管理 → Windows Credential Manager + 设置中心 UI
  ✅ #3 Subagent 架构 → Pi Lead + 6 Subagent + 工具白名单
  ✅ #4 MATLAB 全保真度 → F0-F3 全部 MATLAB MCP
  ✅ #5 离线知识库 → SQLite FTS5 + 种子数据 + Knowledge Center

P1 — V6.1 重要功能（✅ V6.1.1 全部完成）
  ✅ #2 AI 引导向导 → 引导式 Setup + AI 解析
  ✅ #6 模板报告 → 阶段报告 MD + 最终 MD/PDF
  ✅ #7 工程模板 + 掩膜 → 5 模板 + MATLAB 掩膜生成

P2 — 遗留项（V6.1.1 不阻塞）
  ⚠️ Paper-to-Plugin → 需 LLM 集成
  ⚠️ online_qwen → 需有效 API Key 在线验证
  ⚠️ CUDA MEX MCP → 占位未实现（能力探测已就绪）
  ⚠️ L3 FAISS → 首版使用 FTS5 替代
```

### V6.1.1 新增补齐项

| 补齐项 | 落地位置 |
|--------|---------|
| 求解变体等价性验证 | `topoptpilot/benchmarks/equivalence.py` + `matlab_equivalence` 发布门禁 |
| MATLAB 启动预热 + SOLVER_WARMUP | `mcp/matlab_mcp/matlab_mcp_server.py::warmup` + `SOLVER_WARMUP` 事件 |
| MCP 启动/求解耗时统计 | `startup_ms`、`warmup`、`last_runs`（worker health + 诊断） |
| API 命名对齐 | `PUT/DELETE /api/settings/agent-credential`、`POST /api/research/guide/parse` 别名 |
| PDF 中文字体显式回退 | `topoptpilot/reports/generator.py`（PyMuPDF + Microsoft YaHei/PingFang SC/Noto Sans CJK SC） |
| 缓存目录可配置 + 迁移 | `data.cache_dir` 设置项 + `tests/test_cache_location.py`（7 用例） |

---

## 九、V6.1.0 发布门禁（新增）

V6.1.0 在 V6.0.0 门禁基础上新增：

| Gate | 说明 |
|------|------|
| `credential_manager` | API Key 保存到 Windows Credential Manager，不出现在 SQLite/日志/报告 |
| `guided_setup` | 6 步向导完成冻结合同，AI_SUGGESTED 字段需逐项确认 |
| `matlab_all_fidelity` | F0-F3 均经 MATLAB MCP 求解 |
| `subagent_isolation` | Subagent 工具白名单隔离有效 |
| `knowledge_offline` | 离线知识库可检索，Knowledge Center 可访问 |
| `round_report` | 每轮生成 Markdown 阶段报告 |
| `final_report_pdf` | 终止时生成 Markdown + PDF |
| `human_approval_f3_only` | 仅 F3 强制人工审批 |
