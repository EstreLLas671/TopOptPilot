# TopOptPilot Package Rules

The root `AGENTS.md` is the research constitution and remains authoritative. This package implements
the deterministic research core. Agent runtimes may only interact through the registered scientific
tool contracts and must never import or mutate storage and solver internals directly.

