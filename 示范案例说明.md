# TopOptPilot 示范案例说明

> **V5.0 覆盖说明**：正式案例已冻结为 A 自主搜索、B 失败驱动 DOE、C 多保真晋级；
> 机器可读输入位于 `topoptpilot/cases/`。

> 本文档对应方案 §9 演示案例与实验设计，描述三个示范案例和一个核心实验矩阵。

---

## 案例 A：二维回归测试

**目的**：验证插件正确性与回归稳定性，不作为主要创新案例。

### 测试结构

| 结构 | 用途 | 预期 |
|------|------|------|
| MBB 梁 | 滤波/投影/优化器基线复现 | 经典对称拓扑 |
| 悬臂梁 | 梯度检查 + 优化器对比 | OC/MMA 一致 |
| L 型结构 | 应力奇异性测试 | 内角消除 |

### 必须展示的内容

1. **CPU 基准**：经典拓扑、柔度、体积分数、收敛曲线
2. **滤波对比**：无滤波 vs 灵敏度滤波 vs PDE 滤波（棋盘格抑制效果）
3. **梯度检查**：随机设计变量的解析与有限差分误差（阈值 < 1e-4）
4. **优化器对比**：OC 与 MMA 在单约束下的柔度与耗时一致性
5. **回归门槛**：代码变更后自动确认结果未异常漂移

### 代码入口

```bash
# 在 MATLAB 中运行（待实现）
>> top3d_main_2d('MBB',    'optimizer', 'OC',   'filter', 'PDE')
>> top3d_main_2d('cantilever', 'optimizer', 'MMA', 'filter', 'sensitivity')
>> gradient_check('cantilever', 20)  # 随机20个单元抽检
```

---

## 案例 B：三维设备安装支架（主案例）

**目的**：核心演示案例，展示从工程需求到拓扑优化结构的完整流程。

### 几何参数

| 字段 | 设定值 | 说明 |
|------|--------|------|
| 设计包络 | 约 120×80×50 mm | 按团队自建模型调整 |
| 连接区域 | 底部四个安装孔 + 顶部受力耳孔 | 孔周与安装面设为非设计域 |
| 材料 | AlSi10Mg 铝合金 | E=70GPa, ν=0.33, ρ=2670kg/m³ |
| 载荷工况 A | 耳孔竖向载荷 | 主工况 |
| 载荷工况 B | 耳孔横向载荷 | 测试载荷变化稳健性 |
| 质量目标 | 体积分数约 0.40 | 约 40% 原始体积 |
| 工程要求 | 单连通、最小构件尺寸、位移复核 | 应力作为后验指标 |

### 案例流程图

```
[1] 建立原始设计域，标注安装孔、受力孔和保留区域
    │
[2] 对完全实体结构做预分析，冻结材料、载荷、位移/应力评价范围
    │
[3] 运行 CPU-TOP3D_XL 中小网格基线，与 CUDA FP64 核对
    │
[4] 运行实验矩阵所有组（B0 / B1 / A1 / A2 / Ours / Ablation）
    │
[5] 在粗/中/细三档网格 + 竖直/横向载荷下测试稳健性
    │
[6] 对最终密度场阈值分割、光顺和几何重建（STL 输出）
    │
[7] 使用独立 FEM 重新划分网格，复核位移、应力、质量和安全性
```

### 评价指标

| 指标 | 定义 | 呈现方式 |
|------|------|---------|
| 质量/体积分数 | 轻量化 + 约束满足 | 数值表 + 前后对比 |
| 柔度 | 整体刚度目标 | 收敛曲线 + 相对变化 |
| 最大位移 | 使用性能 | 独立 FEM 云图 |
| 最大应力 | 后验安全性 | 应力云图 + 位置解释 |
| 灰度比例 | 0-1 清晰度 | 随迭代曲线 |
| 连通分量 | 结构完整性 | 断连警报 + 回滚记录 |
| PCG 次数/残差 | 求解可信度和难度 | 每轮曲线 |
| 运行时间 | 工程效率与迭代成本 | GPU vs CPU 对比 |

### 输入任务示例（task.json）

```json
{
  "task_id": "bracket_3d_001",
  "research_goal": "研究三维支架中自适应滤波与投影策略",
  "geometry": "models/bracket.TopVoxel",
  "material": { "E_MPa": 70000, "nu": 0.33, "density_kg_m3": 2670 },
  "load_cases": ["vertical", "lateral"],
  "volume_fraction": 0.40,
  "requirements": {
    "connected_components": 1,
    "gray_ratio_max": 0.05,
    "compliance_increase_max": 0.03
  },
  "compute_budget": { "gpu": "RTX 5090", "max_runs": 30 }
}
```

### 关键注意事项

1. **输入必须由工程负责人确认** — Agent 可以发现缺失或冲突，但不能替代载荷责任和工程签字
2. **GPU 不提高物理精度** — GPU 使更细三维网格和多轮自动实验在可接受时间内完成
3. **结果是"方法对比"而非"最优结构"** — 核心输出是假设是否成立，而非支架本身多漂亮

---

## 案例 C：Paper-to-Plugin 现场演示

**目的**：展示从论文 PDF 到可执行插件的自动化流水线。

### 选择标准

必须选择一篇**包含明确公式和参数**的滤波或投影论文：
- Heaviside 投影（β、η 明确）
- PDE/Helmholtz 滤波（半径 r 明确）
- 灵敏度滤波（半径 r_min 明确）

**禁止**现场生成 CUDA 求解器。

### 演示流程（8 步）

```
Step 1: 上传 PDF
        → 展示论文标题、DOI、摘要
Step 2: 方法拆解
        → Agent 定位方法部分，提取核心公式
Step 3: 证据定位
        → 显示公式所在页码、附图编号
Step 4: 重复插件检索
        → 查询注册表，检查是否已有相同功能插件
Step 5: 生成插件规格
        → 输出 YAML 格式的插件说明书（§5.3 字段）
Step 6: 生成实验性代码
        → 输出 MATLAB 函数骨架（~20 行核心逻辑）
Step 7: 梯度检查
        → 随机单元抽检，显示解析/有限差分误差
Step 8: 状态登记
        → 以 Experimental 状态写入注册表
```

### 成功标准

- 从上传 PDF 到插件登记 ≤ 5 分钟
- 梯度检查误差 < 1e-4
- DOI + 页码可回溯

---

## 核心实验矩阵

### 六组实验设计

| 实验组 | 优化器 | 滤波 | 投影 | 控制器 | 回答的科学问题 |
|--------|--------|------|------|--------|---------------|
| **B0** | OC | PDE 滤波 | 无 | 固定 p=3 | 无延续基线表现如何？ |
| **B1** | OC | PDE 滤波 | Heaviside | 固定轮次 β+=2/10步 | 人工固定调度能改善多少？ |
| **A1** | OC | PDE 滤波 | Heaviside | 灰度反馈 β | 单一灰度指标是否足够？ |
| **A2** | OC | PDE 滤波 | Heaviside | 收敛反馈 β/p | 柔度稳定性是否能判断时机？ |
| **Ours** | OC | PDE 滤波 | Heaviside | **联合反馈（灰度+柔度+连通+求解难度）** | **核心假设是否成立？** |
| **Ablation** | OC | PDE 滤波 | Heaviside | 逐项移除四类信号 | 每个信息源的实际贡献？ |

### 实验覆盖维度

每个实验组在以下维度上重复：

| 维度 | 取值 |
|------|------|
| 网格级别 | coarse (~50K 单元) → medium (~200K) → fine (~500K+) |
| 载荷工况 | 竖直 (vertical) / 横向 (lateral) |
| 求解器 | CPU-TOP3D_XL (FP64) / CUDA MEX (FP64 / FP32 / mixed) |
| 随机种子 | 每组至少 3 个种子（控制初始化随机性） |

### 假设判定规则（实验前冻结）

```
成功条件（联合反馈成立）:
  ✓ 灰度比例 < 5%
  ✓ 单连通分量
  ✓ 柔度相对基线增幅 ≤ 3%
  ✓ 在多数测试工况中减少达到收敛所需的迭代或总时间

失败条件（联合反馈不成立）:
  ✗ 灰度比例 > 10%
  ✗ 出现多连通分量
  ✗ 柔度相对基线增幅 > 8%
  ✗ 仅在部分网格有效 → "部分支持"并输出适用边界

允许负结果:
  若联合反馈仅在部分网格有效，或降低灰度但显著损害柔度，
  系统应输出"部分支持"或"不支持"，并分析原因。
  负结果同样是科学结果。
```

### 基线对比逻辑

```
Ours vs B0:  联合反馈 vs 无投影基线 → 判断灰度改善幅度
Ours vs B1:  联合反馈 vs 固定调度 → 判断自适应是否优于固定方案
Ours vs A1:  联合反馈 vs 单灰度反馈 → 判断多指标融合的增益
Ours vs A2:  联合反馈 vs 单收敛反馈 → 判断灰度+连通+求解难度的增量价值
Ablation:    逐项移除 → 每个指标的边际贡献
```

---

## 十分钟演示脚本

### 时间线

| 时间 | 内容 | 核心动作 | 技术看点 |
|------|------|---------|---------|
| 0:00–0:50 | 问题与差距 | 传统方法多、选择难、三维试验成本高 | — |
| 0:50–1:40 | 系统架构 | 官方 Pi/Qwen 决策 → Policy → Python/MATLAB FEM | 四层分离图 |
| 1:40–2:40 | 上传论文与任务 | 展示真实 PDF、支架模型、边界条件 | 输入真实而非 demo 数据 |
| 2:40–3:40 | Paper-to-Plugin | 提取公式、页码、条件 → 生成方法卡片 | DOI/页码可回溯 |
| 3:40–4:40 | 候选假设竞争 | 生成 3 项候选，审稿 Agent 指出风险 | 反例质量 |
| 4:40–6:10 | 真实求解 | 任务 JSON → CUDA MEX → 残差曲线实时更新 | GPU 在真正跑 |
| **6:10–7:20** | **失败与修正** | **一轮过早投影→断连→Agent 回滚→调整** | **核心亮点** |
| 7:20–8:30 | 第二轮与对比 | 展示结构、灰度、柔度、连通、时间对比表 | 多指标对比 |
| 8:30–9:20 | 独立复核 | 重建网格 → 独立 FEM → 位移/应力云图 | 可信度验证 |
| 9:20–10:00 | 研究报告 | 假设等级 + 适用边界 + 真实引用 + 复现包 | 赛题字段输出 |

### 备用素材（必须提前录制）

```
[1] 完整 GPU 求解加速录像（防止现场等待）
[2] 一次失败实验 + 一次修正的完整日志
[3] CPU/GPU 一致性验证画面
[4] 论文页码证据 + 插件状态变化 + 人工批准记录
[5] 无网络或百炼异常时的离线结果回放
```

---

## 附录：结果 JSON 示例

每次实验输出（参照方案附录 B）：

```json
{
  "run_id": "run_202607_bracket_001",
  "status": "converged",
  "objective": { "compliance": 128.4 },
  "constraints": { "volume_fraction": 0.399 },
  "quality": {
    "gray_ratio": 0.037,
    "connected_components": 1,
    "max_displacement_mm": 0.28
  },
  "solver": {
    "backend": "cuda_mex_fp64",
    "relative_residual": 8.2e-5,
    "cg_iterations": 146,
    "solve_time_seconds": 642
  },
  "artifacts": {
    "density": "density.mat",
    "history": "history.csv",
    "design": "design.nii",
    "log": "run.log"
  }
}
```
