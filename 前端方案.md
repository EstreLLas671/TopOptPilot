# TopOptPilot 前端方案：V6.0.0 三层工作台

> **V6.0.0 主入口**：`desktop/` Tauri 2 + React 19 原生桌面应用（v6.0.0），zh-CN 默认 / en-US 可选，含 **SettingsWorkspace 设置页面**。
> **开发入口**：`topoptpilot/app/streamlit_app.py` Streamlit Workspace（`python launch.py --web`）。
> **API 入口**：`topoptpilot/api/fastapi_app.py` FastAPI（供桌面端 sidecar 与脚本调用，含 Settings API 8 端点）。

---

## 一、设计原则

| 原则 | 说明 |
|------|------|
| **原生优先** | 主入口为 Windows 原生窗口，低延迟，离线可用 |
| **三栏 Codex 式布局** | Explorer / Stream / Inspector 三列并存，不切换标签页 |
| **双语开箱即用** | zh-CN 默认 + en-US，i18next 持久化到 localStorage |
| **确定性安全策略优先** | 关键参数（volfrac/rmin/beta/penal）由 SafetyGuard 硬约束，UI 不绕过 |
| **流式事件 + 审批制** | F3 实验必须显式审批；决策卡提供 Approve / Edit / Reject / Why |
| **异步不阻塞** | 求解在 Executor 子进程运行；UI 通过 WebSocket 接收事件 |
| **可复现** | `/export` 一键生成复现包，`topoptpilot/replay.py` 离线回放 |

---

## 二、技术选型（V5.1）

### 2.1 桌面端（主入口）

| 组件 | 技术 | 理由 |
|------|------|------|
| **窗口壳** | Tauri 2 | 轻量（安装包 230MB 含后端）、Rust 进程管理、NSIS 分发 |
| **前端** | React 19 + TypeScript 5.8 + Vite 7 | 组件化、类型安全、快构建 |
| **图标库** | lucide-react 0.468 | 统一线性图标集 |
| **国际化** | i18next + react-i18next | 双语开箱、语言持久化 |
| **数据可视化** | Canvas 密度场 + SVG 收敛曲线 | 轻量，无第三方图表库 |
| **Rust 侧** | tauri v2 + serde v1 + serde_json v1 | 进程管理、IPC invoke |
| **Python 后端** | FastAPI + Uvicorn + PyInstaller | sidecar 单文件分发、随机端口 + 一次性 token 鉴权 |

### 2.2 开发端（Streamlit Workspace）

| 组件 | 技术 |
|------|------|
| **主应用** | `topoptpilot/app/streamlit_app.py` |
| **布局** | Streamlit 三列（explorer 22% / main 56% / inspector 22%） |
| **主题** | `topoptpilot/app/theme/style.css` 深色 |
| **可视化** | matplotlib 密度场 + pandas 收敛曲线 |
| **视图** | CHAT / TIMELINE / COMPARE / BENCHMARKS 四态 |

### 2.3 API 层

| 组件 | 技术 |
|------|------|
| **HTTP/WS** | FastAPI + WebSocket |
| **鉴权** | `X-TopOptPilot-Token` header（桌面端 sidecar 随机 token） |
| **CORS** | 仅允许 `tauri://localhost` / `http(s)://tauri.localhost` / `http(s)://(127.0.0.1\|localhost)(:\d+)?` |

---

## 三、桌面端应用布局（V6.0.0）

```
┌──────────────────────────────────────────────────────────────────────┐
│  ◈ TopOptPilot v6.0.0    [⚙设置]  [语言 ▾ zh-CN/en-US]  [MATLAB ●] │  ← Titlebar
├─────────────┬────────────────────────────────────┬───────────────────┤
│ EXPLORER    │ STREAM / SETTINGS                  │ INSPECTOR         │
│             │                                    │                   │
│ ▸ Researches│  ┌──────────────────────────────┐  │  ┌─────────────┐  │
│   ▸ R1      │  │ PI AGENT · STREAMING         │  │  │ MATLAB MCP  │  │
│     ▸ E01 ✓ │  │   正在调用 research_get_     │  │  │ ● Ready     │  │
│     ▸ E02 ▶ │  │   context...                 │  │  │ R2024a      │  │
│     ▸ E03 ✗ │  └──────────────────────────────┘  │  │ MCP 0.12.0  │  │
│             │                                    │  └─────────────┘  │
│ [▸ Start    │  ┌──────────────────────────────┐  │                   │
│  autonomous]│  │ PROPOSED EXPERIMENT          │  │  Experiment E01   │
│             │  │ Purpose: 建立 F0 基线        │  │  Fidelity: F0     │
│ BUDGET      │  │ Risk: LOW                    │  │  Mesh: coarse     │
│  ██████ 6/12│  │ [Approve][Edit][Reject][Why] │  │  Compliance 128.4 │
│             │  └──────────────────────────────┘  │  Gray 0.037       │
│ EVIDENCE    │                                    │  Components 1     │
│  ⌁ Trends   │  ┌──────────────────────────────┐  │  Backend python   │
│  ⚠ Failures │  │ EXPERIMENT E01 · SUCCESS     │  │                   │
│  ◇ Pareto(3)│  │ F0 — 建立基线                │  │  [拓扑密度图]      │
│             │  │ [Compliance][Gray][Comp][Runtime]│                 │
│ DECISIONS   │  │ [Topology][Convergence][Metrics]│  [收敛曲线 SVG]    │
│  D01 Approved│  └──────────────────────────────┘  │                   │
│             │                                    │  Parameters       │
│ FILES       │                                    │   beta     4.0    │
│  research.db│                                    │   rmin     1.5    │
│  progress/  │                                    │   penal    3.0    │
├─────────────┴────────────────────────────────────┴───────────────────┤
│ > /run bracket_3d_001 --volume 0.4 --mesh fine                       │  ← Command Bar
└──────────────────────────────────────────────────────────────────────┘
```

### 3.0 设置页面（`desktop/src/SettingsWorkspace.tsx`，V6.0.0 新增）

```
┌──────────────────────────────────────────────────────────────────────┐
│  ← 返回工作台    TopOptPilot 设置中心    [保存设置]                    │
│                   全局默认值只影响后续新建 Research                     │
├─────────────┬────────────────────────────────────────────────────────┤
│ 导航栏       │  主内容区                                               │
│             │                                                        │
│ ● 通用      │  默认语言: [zh-CN ▾]                                   │
│ ○ Agent     │  界面密度: [standard ▾]                                 │
│ ○ MATLAB    │  启动行为: [resume_last ▾]                              │
│ ○ 默认值    │                                                        │
│ ○ 数据诊断  │  桌面 sidecar：本机受令牌保护。版本 6.0.0                │
│             │                                                        │
└─────────────┴────────────────────────────────────────────────────────┘
```

**5 个 Tab**：
- **通用**：语言、界面密度、启动行为
- **Agent 与模型**：模型名、Base URL、超时、重试、Safe Mode、测试连接、重启 Pi 会话
- **MATLAB 与计算**：MATLAB 根目录、Python 并发数、MATLAB 超时/重试、重启 MATLAB
- **新研究默认值**：模式、总预算、F0-F3 预算、材料参数
- **数据与诊断**：数据目录切换、诊断快照、缓存清理

### 3.1 主要组件

| 组件 | 文件 | 作用 |
|------|------|------|
| `TopologyCanvas` | `App.tsx` | Canvas 绘制密度场（2D 单图 / 3D 三投影） |
| `Convergence` | `App.tsx` | SVG 折线图（compliance / gray_ratio / volume_fraction / beta） |
| `EventCard` | `App.tsx` | 事件卡（agent / tool / evidence / safety-policy 等类型） |
| `DecisionCard` | `App.tsx` | 决策卡（Approve / Edit / Reject / Why 四按钮） |
| `Metric` | `App.tsx` | 指标卡（标题 + 数值） |
| `CreateResearch` | `App.tsx` | 新建研究模态框 |
| `EditDecision` | `App.tsx` | 编辑待审批实验参数模态框 |
| **`SettingsWorkspace`** | **`SettingsWorkspace.tsx`** | **设置页面（V6.0.0 新增）**：5 个 Tab（通用/Agent/MATLAB/默认值/诊断） |

### 3.2 命令系统（与 Streamlit 共享）

```
/run                         — 启动自主研究
/pause                       — 暂停当前研究
/resume                      — 恢复
/stop                        — 终止
/approve E01                 — 批准决策
/reject E01                  — 拒绝
/rollback E01                — 回滚
/compare E01 E02             — 对比两实验
/lock beta 8                 — 锁定参数
/promote E01                 — 升保真
/retry E01                   — 重试
/report                      — 生成研究报告
/export                      — 导出复现包
```

---

## 四、i18n（双语支持）

`desktop/src/i18n.ts` 定义 47 对翻译，`fallbackLng: "zh-CN"`，localStorage 持久化。

| 领域 | zh-CN | en-US |
|------|-------|-------|
| 应用名 | TopOptPilot | TopOptPilot |
| 研究 | 研究 | Research |
| 实验 | 实验 | Experiment |
| 柔度 | 柔度 | Compliance |
| 灰度 | 灰度 | Gray ratio |
| 连通分量 | 连通分量 | Connected components |
| 后端 | 后端 | Backend |
| MATLAB 状态 | matlab | MATLAB MCP |
| 严格 F3 | matlabStrict | F3 accepts only genuine MATLAB MCP results |
| 批准 | approve | Approve |
| 拒绝 | reject | Reject |
| 为什么 | why | Why? |

---

## 五、前端 → 后端数据流

### 5.1 桌面端（Tauri）

```
┌──────────────────────┐    spawn + stdio    ┌──────────────────────┐
│  Rust (lib.rs)       │ ───────────────────→ │ desktop_sidecar.py   │
│  - ChildGuard RAII   │  TOPPILOT_SIDECAR   │ - secrets.token      │
│  - CREATE_NO_WINDOW  │  = {port, token}    │ - uvicorn on 127.0.0.1│
│  - bootstrap config  │ ←───────────────────│   :random_port       │
│  - backend_info cmd  │                     │ - settings API       │
└──────────┬───────────┘                     └──────────┬───────────┘
           │ invoke                                      │ HTTP+WS
           ↓                                             ↓
┌──────────────────────┐                          ┌──────────────────────┐
│  React (App.tsx)     │                          │  fastapi_app.py      │
│  - api.ts client     │ ─── X-TopOptPilot-Token →│  - desktop_token_    │
│  - WebSocket stream  │                          │    guard middleware  │
│  - i18n              │                          │  - ResearchService   │
│  - SettingsWorkspace │ ─── Settings API ───────→│  - Settings 持久化    │
└──────────────────────┘                          └──────────────────────┘
```

### 5.2 Streamlit Workspace

```
streamlit run app.py
   ↓
topoptpilot/app/streamlit_app.py
   ↓ @st.cache_resource
ResearchService()  ─→ ResearchOrchestrator → Pi Bridge / Policy / Solver / Evaluator
   ↓
topoptpilot/memory/research_state.py (SQLite)
```

### 5.3 API 直接调用

```bash
uvicorn topoptpilot.api.fastapi_app:app --host 127.0.0.1 --port 8000
# REST: POST /api/research, GET /api/research/{id}, POST /api/.../autonomous, ...
# WS:   ws://127.0.0.1:8000/api/research/{id}/stream?token=...
```

---

## 六、核心 API 端点（`topoptpilot/api/fastapi_app.py`）

| Method | Path | 作用 |
|--------|------|------|
| `GET` | `/api/health` | 系统健康（agent/pi_rpc/matlab） |
| `POST` | `/api/research` | 创建研究 |
| `GET` | `/api/research` | 列出研究 |
| `GET` | `/api/research/{id}` | 取研究详情 |
| `POST` | `/api/research/{id}/autonomous` | 启动自主研究 |
| `POST` | `/api/research/{id}/commands` | 发送命令 |
| `PATCH` | `/api/research/{id}/locale` | 切语言（zh-CN / en-US） |
| `GET` | `/api/research/{id}/events?after=N` | 拉事件增量 |
| `WS` | `/api/research/{id}/stream?ticket=...` | 实时流（ticket 鉴权，20s 过期） |
| `POST` | `/api/experiments` | 创建实验 |
| `GET` | `/api/experiments/{id}` | 取实验详情 |
| `POST` | `/api/decision/{id}/approve` | 批准决策 |
| `POST` | `/api/decision/{id}/reject` | 拒绝决策 |
| `POST` | `/api/decision/{id}/edit` | 编辑待批参数 |
| `GET` | `/api/decision/{id}/why` | 取决策依据 |
| `GET` | `/api/matlab/health` | MATLAB MCP 健康 |
| `POST` | `/api/matlab/restart` | 重启 MATLAB |
| `GET` | `/api/report/{id}` | 生成 Markdown 报告 |
| `POST` | `/api/tools/invoke` | 调用 Pi 工具 |
| **`GET`** | **`/api/settings`** | **读取应用设置（V6.0.0）** |
| **`PATCH`** | **`/api/settings`** | **更新应用设置（深度合并，禁止 API Key）** |
| **`POST`** | **`/api/settings/test-agent`** | **测试 Agent 连接** |
| **`POST`** | **`/api/settings/restart-pi`** | **重启 Pi 会话** |
| **`POST`** | **`/api/settings/restart-matlab`** | **重启受控 MATLAB** |
| **`GET`** | **`/api/settings/diagnostics`** | **诊断快照** |
| **`POST`** | **`/api/settings/export-diagnostics`** | **导出诊断 ZIP** |
| **`POST`** | **`/api/settings/clear-cache`** | **清理可再生缓存** |

---

## 七、历史路径（已废弃）

| 阶段 | 技术 | 现状 |
|------|------|------|
| V4.0 | Textual TUI | ❌ 未实现（方案阶段废弃） |
| V4.1 | Streamlit Workspace | ✅ 保留为开发入口（`python launch.py --web`） |
| V5.0 | FastAPI+React 驾驶舱 | ❌ 方案废弃，改为 Tauri 桌面 |
| V5.1 | Tauri 2 + React + TypeScript | ✅ 三栏工作台 + i18n + 侧栏 |
| **V6.0.0** | **V5.1 + Settings 页面 + 诊断面板** | **✅ 当前主入口** |
| 旧 CLI | `frontend/cockpit.py` | ✅ 兼容保留（不再演进） |

---

## 八、打包分发

### 8.1 一键打包（Windows）

```powershell
powershell -ExecutionPolicy Bypass -File scripts/build_desktop.ps1
```

`build_desktop.ps1` 自动完成：
1. PyInstaller 打包 `topoptpilot/api/desktop_sidecar.py` → `topoptpilot-backend.exe`
2. 下载 vendor/node v24.14.0 win-x64（首次）
3. 暂存 `resources/`：MATLAB 求解器模块 / MCP server / .pi skills / mcp / 配置文件
4. `npm --prefix desktop run tauri build`
5. 输出：`desktop/src-tauri/target/release/bundle/nsis/TopOptPilot_6.0.0_x64-setup.exe`（220 MB）

### 8.2 最终用户运行要求

| 依赖 | 必需 |
|------|:----:|
| Windows 10/11 x64 | ✅ |
| WebView2 运行时（Win11 已内置） | ✅ |
| Python / Node / Rust | ❌ 不需 |
| MATLAB R2024a（仅 F3 高保真通道） | ❌ 可选 |
| DashScope API Key（无则自动进入 Safe Mode） | ❌ 可选 |
