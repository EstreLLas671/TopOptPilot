# TopOptPilot 前端方案：桌面应用程序

> **V5.0 覆盖说明**：工作台现已显示官方 Pi RPC 会话状态与流式事件，支持新建研究、
> 自主启动、审批/锁定/回滚、Pareto、基线仪表板及复现导出。

> **版本说明（V4.1）**：当前唯一主界面为 `streamlit run app.py` 启动的 Codex-like
> Local Workspace；不再开发 Gradio、React、Vue 或独立网页前端。FastAPI 只承担测试
> API，所有业务行为统一进入 `ResearchService`。下文旧 TUI 方案仅作历史参考。

> 参照 Codex CLI、WorkBuddy 的终端交互模式，采用 Python Textual 框架构建 TUI 桌面应用。

---

## 一、设计原则

| 原则 | 说明 |
|------|------|
| **终端优先** | 不依赖浏览器，原生终端渲染，低延迟 |
| **键盘驱动** | 主操作流通过快捷键完成，鼠标辅助 |
| **分屏布局** | 多面板同时可见，不切换标签页 |
| **异步非阻塞** | 求解过程在后台线程运行，UI 不卡死 |
| **离线可用** | 所有核心功能在无网络环境下可运行 |

---

## 二、技术选型

| 组件 | 技术 | 理由 |
|------|------|------|
| **TUI 框架** | **Textual** (Python) | 异步、CSS 布局、鼠标支持、60fps 渲染 |
| **图表渲染** | matplotlib → 保存 PNG → 终端内嵌显示 | 无需 GUI 后端 |
| **3D 渲染** | matplotlib 3D / trimesh → PNG 截图 | 轻量，无额外依赖 |
| **Markdown 渲染** | Rich.markdown | 研究报告、方法卡片等富文本 |
| **交互输入** | Prompt Toolkit / Textual Input | 命令补全、历史记录 |
| **进程管理** | asyncio.subprocess | 后台运行 MATLAB/CUDA，异步回调 |

---

## 三、应用布局

### 3.1 主界面

```
┌────────────────────────────────────────────────────────────────┐
│  TopOptPilot  v1.0                     模式：[演示]  状态：●运行 │
├────────────────┬───────────────────────┬───────────────────────┤
│  📋 任务面板    │   📊 监控面板          │   💡 智能体面板        │
│                │                       │                       │
│  任务: bracket  │   ┌─────────────────┐ │  研究主管: 迭代#3     │
│  几何: OK      │   │ 收敛曲线         │ │  证据Agent: 5篇论文   │
│  材料: AlSi10Mg│   │  ━━╲╱╲╱╲╱╲╱╲╱  │ │  假设Agent: 3候选     │
│  载荷: 竖向    │   │  ╱╲╱╲╱╲╱╲╱╲╱╲ │ │  审稿Agent: H1>H3>H2 │
│  体积: 0.40    │   └─────────────────┘ │  实验Agent: 18组      │
│                │                       │  审计Agent: ✅ H1     │
│  预算: 24/30   │   柔度:128.4 灰度:0.037│                       │
│                │   PCG:146  时间:642s   │   [查看决策链路]       │
├────────────────┴───────────────────────┴───────────────────────┤
│  📝 输出面板                                                     │
│                                                                  │
│  [10:15:23] ✅ Phase 2 文献挖掘完成 — 3个方法, 2个缺口           │
│  [10:15:28] 💡 Phase 3 假设生成 — H1:联合反馈优于固定调度         │
│  [10:15:32] 🔍 Phase 4 假设审稿 — 反例:粗网格可能振荡             │
│  [10:15:38] 🧪 Phase 5 实验设计 — 6组 × 3网格 × 2载荷           │
│  [10:15:40] ⚡ Phase 6 实验执行 — GPU求解中...                    │
│                                                                  │
│  > /run bracket_3d_001 —volume 0.4 —mesh fine                    │
│  └───────────────────────────────────────────────────────────     │
└────────────────────────────────────────────────────────────────┘
```

### 3.2 面板说明

| 面板 | 位置 | 宽度 | 内容 | 快捷键 |
|------|------|------|------|--------|
| **任务面板** | 左 | 25% | 当前任务配置、进度、预算 | `Ctrl+1` |
| **监控面板** | 中 | 45% | 收敛曲线、3D结构、指标卡 | `Ctrl+2` |
| **智能体面板** | 右 | 30% | Agent决策链路、状态、评分 | `Ctrl+3` |
| **输出面板** | 底 | 100% | 运行日志 + 命令输入 | `Ctrl+4` |

---

## 四、交互方式

### 4.1 命令系统

```
# 顶层命令
/run <task.json>         — 运行完整科研闭环
/quick <params>          — 快速参数扫描（演示模式）
/plot <experiment_id>    — 查看特定实验的收敛曲线
/view <experiment_id>    — 查看 3D 结构
/compare <id1> <id2>     — 对比两组实验结果

# 参数调节（演示模式）
/beta <value>            — 设置投影陡峭度
/penal <value>           — 设置 SIMP 惩罚指数
/rmin <value>            — 设置滤波半径
/volfrac <value>         — 设置体积分数
/controller <type>       — 切换控制器策略

# 系统命令
/status                  — 系统状态
/plugins                 — 插件注册表
/kb                      — 知识库统计
/export <path>           — 导出完整研究报告
/help                    — 帮助
```

### 4.2 快捷键

| 快捷键 | 功能 |
|--------|------|
| `Ctrl+S` | 启动/暂停科研闭环 |
| `Ctrl+R` | 重置当前任务 |
| `Ctrl+E` | 导出报告 |
| `Ctrl+D` | 切换演示/科研模式 |
| `Ctrl+L` | 清屏 |
| `Ctrl+[1-4]` | 切换焦点面板 |
| `Tab` | 循环切换面板 |
| `Esc` | 取消当前操作 |
| `↑/↓` | 命令历史 |
| `F5` | 刷新 3D 渲染 |
| `F10` | 全屏模式 |
| `F12` | 开发者工具 |

### 4.3 演示模式下的实时交互

```
演示模式特有交互（答辩时使用）：

参数滑块（键盘控制）：
  ← →  调整 beta (1-32)
  ↑ ↓  调整 penal (1-5)
  [ ]  调整 rmin (0.5-6.0)

控制器切换：
  F1    固定调度
  F2    灰度反馈
  F3    联合反馈（自适应）
  F4    消融模式

每 2-3 秒触发一次轻量求解 → 实时更新收敛曲线和 3D 结构
```

---

## 五、前端接口文档

### 5.1 UI 组件接口

所有前端组件通过以下接口与后端交互，不直接耦合。

```python
# frontend/interfaces.py — 前端接口定义

@dataclass
class UIUpdatePayload:
    """UI 更新的统一数据格式"""
    panel: str                    # "task" | "monitor" | "agent" | "output"
    action: str                   # "update" | "append" | "clear" | "focus"
    data: dict                    # 具体数据
    timestamp: float = 0.0


@dataclass
class PanelTaskData:
    """任务面板数据"""
    task_id: str
    geometry_status: str          # "ok" | "missing" | "error"
    material_name: str
    load_cases: list[str]
    volume_fraction: float
    budget_used: int
    budget_total: int


@dataclass
class PanelMonitorData:
    """监控面板数据"""
    convergence_curve: list[float]     # 柔度历史
    gray_ratio_curve: list[float]      # 灰度比例历史  
    cg_iterations_curve: list[int]     # PCG 迭代历史
    compliance_current: float
    gray_ratio_current: float
    cg_iterations_current: int
    solve_time_seconds: float
    density_field_3d: Optional[np.ndarray]  # 3D 密度场 (用于渲染)
    mesh_level: str


@dataclass
class PanelAgentData:
    """智能体面板数据"""
    current_phase: str             # "literature_mining" | "hypothesis" | ...
    iteration: int
    agent_trace: list[dict]        # 决策链路
    hypotheses: list[dict]         # 候选假设及评分
    review_ranks: list[dict]       # 评审排序
    evidence_count: int
    knowledge_gaps: list[str]


@dataclass
class PanelOutputData:
    """输出面板数据"""
    lines: list[dict]              # [{time, level, message}]
    command_history: list[str]
    current_input: str


class FrontendBridge:
    """
    前端桥接器 — 后端通过此接口推数据到 UI。
    解耦：后端不直接导入 Textual，只推送数据。
    """
    
    def __init__(self):
        self._subscribers: dict[str, list[callable]] = {
            "task": [],
            "monitor": [],
            "agent": [],
            "output": [],
        }
    
    def subscribe(self, panel: str, callback: callable):
        """UI 组件订阅特定面板的数据更新"""
        self._subscribers[panel].append(callback)
    
    def publish(self, payload: UIUpdatePayload):
        """后端发布数据更新"""
        for cb in self._subscribers.get(payload.panel, []):
            cb(payload)
    
    def push_log(self, level: str, message: str):
        """快捷方法：推送日志到输出面板"""
        self.publish(UIUpdatePayload(
            panel="output",
            action="append",
            data={"level": level, "message": message, "time": time.time()}
        ))
    
    def push_convergence(self, compliance: float, gray_ratio: float, 
                         cg_iterations: int):
        """快捷方法：推送收敛数据到监控面板"""
        self.publish(UIUpdatePayload(
            panel="monitor",
            action="update",
            data={
                "compliance": compliance,
                "gray_ratio": gray_ratio,
                "cg_iterations": cg_iterations,
            }
        ))
    
    def push_agent_state(self, phase: str, iteration: int, 
                         agent_trace: list[dict]):
        """快捷方法：推送 Agent 状态到智能体面板"""
        self.publish(UIUpdatePayload(
            panel="agent",
            action="update",
            data={
                "current_phase": phase,
                "iteration": iteration,
                "agent_trace": agent_trace,
            }
        ))


# frontend/app.py — 前端应用入口（占位，由具体实现填充）
class TopOptPilotApp:
    """桌面应用主类"""
    
    def __init__(self, bridge: FrontendBridge):
        self.bridge = bridge
        self.bridge.subscribe("task", self._on_task_update)
        self.bridge.subscribe("monitor", self._on_monitor_update)
        self.bridge.subscribe("agent", self._on_agent_update)
        self.bridge.subscribe("output", self._on_output_update)
    
    def _on_task_update(self, payload: UIUpdatePayload):
        """任务面板更新回调"""
        raise NotImplementedError
    
    def _on_monitor_update(self, payload: UIUpdatePayload):
        """监控面板更新回调"""
        raise NotImplementedError
    
    def _on_agent_update(self, payload: UIUpdatePayload):
        """智能体面板更新回调"""
        raise NotImplementedError
    
    def _on_output_update(self, payload: UIUpdatePayload):
        """输出面板更新回调"""
        raise NotImplementedError
    
    def run(self):
        """启动应用主循环"""
        raise NotImplementedError


# frontend/__init__.py
__all__ = [
    "FrontendBridge", "UIUpdatePayload",
    "PanelTaskData", "PanelMonitorData", 
    "PanelAgentData", "PanelOutputData",
    "TopOptPilotApp",
]
```

### 5.2 数据流

```
┌──────────────┐       publish()       ┌──────────────┐
│   后端       │ ─────────────────────→ │   前端       │
│  Orchestrator│    UIUpdatePayload     │  TopOptPilot │
│  Experiment  │                        │  App (TUI)   │
│  NaNChecker  │                        │              │
│  ...         │                        │  4 面板订阅   │
└──────────────┘                        └──────────────┘
       │                                       │
       │  command (通过 stdin / callback)        │
       │  "参数变化 → 重算 → 推新数据"           │
       └───────────────────────────────────────┘
```

### 5.3 可视化组件接口

```python
# frontend/visualizers.py — 可视化组件

class ConvergencePlot:
    """收敛曲线组件"""
    def update(self, compliance: list[float], gray_ratio: list[float],
               cg_iterations: list[int]):
        """更新曲线数据"""
        ...
    def render(self) -> str:
        """返回终端可显示的字符串（或图片路径）"""
        ...


class Structure3D:
    """3D 结构渲染组件"""
    def load(self, density_field: np.ndarray, threshold: float = 0.5):
        """加载体素密度场"""
        ...
    def rotate(self, azimuth: float, elevation: float):
        """旋转视角"""
        ...
    def render(self) -> str:
        """返回截图路径"""
        ...


class ExperimentHeatmap:
    """实验矩阵热力图"""
    def load(self, matrix: pd.DataFrame):
        """加载实验矩阵数据"""
        ...
    def render(self) -> str:
        """返回截图路径"""
        ...


class AgentTraceGraph:
    """Agent 决策链路图"""
    def load(self, trace: list[dict]):
        """加载 Agent 轨迹"""
        ...
    def render(self) -> str:
        """返回截图路径"""
        ...
```

---

## 六、文件结构

```
frontend/
├── __init__.py              # 导出所有公开接口
├── interfaces.py            # FrontendBridge + 数据模型（纯接口）
├── app.py                   # TopOptPilotApp 主类（抽象基类）
├── visualizers.py           # 可视化组件接口（ConvergencePlot, Structure3D 等）
│
├── textual/                 # Textual TUI 实现（具体实现）
│   ├── __init__.py
│   ├── app.py               # TextualApplication（继承 TopOptPilotApp）
│   ├── screens/
│   │   ├── main_screen.py   # 主界面布局
│   │   ├── help_screen.py   # 帮助界面
│   │   └── settings_screen.py # 设置界面
│   ├── widgets/
│   │   ├── task_panel.py    # 任务面板
│   │   ├── monitor_panel.py # 监控面板（含内嵌图表）
│   │   ├── agent_panel.py   # 智能体面板
│   │   ├── output_panel.py  # 输出面板
│   │   └── command_input.py # 命令输入
│   ├── css/
│   │   └── app.tcss         # Textual CSS 样式
│   └── commands.py          # 命令注册与解析
│
├── rich/                    # Rich 降级实现（Textual 不可用时）
│   ├── __init__.py
│   ├── app.py               # RichConsoleApp
│   └── layouts.py           # Rich 布局辅助
│
└── assets/                  # 静态资源
    ├── logo.txt             # ASCII logo
    └── colors.tcss          # 配色方案
```

---

## 七、输入输出交互细节

### 7.1 启动

```bash
# 标准启动（Textual TUI）
python -m frontend.textual.app

# 降级启动（纯 Rich 控制台，无鼠标/无终端支持时）
python -m frontend.rich.app

# 指定任务直接运行
python -m frontend.textual.app --task demo/sample_inputs/bracket_task.json
```

### 7.2 输入：命令补全

```
> /[Tab]
/run        /quick      /plot       /view        /compare
/beta       /penal      /rmin       /volfrac     /controller
/status     /plugins    /kb         /export      /help

> /run [Tab]
bracket_3d_001  mbb_2d_001  cantilever_2d_001

> /controller [Tab]
fixed     gray_feedback  joint_feedback  ablation
```

### 7.3 输出：异步日志流

```
# 后端推日志 → 前端实时显示
[10:15:23] [系统]    === TopOptPilot 启动 ===
[10:15:23] [任务]    ✅ 任务包验证通过 — bracket_3d_001
[10:15:24] [Agent]   📚 Phase 2: 文献挖掘...
[10:15:28] [AGENT]   💬 PiAgent / DeepSeek V4 Flash (token: 1247)
[10:15:30] [Agent]   ✅ 文献挖掘完成 — 3 个方法, 2 个缺口
[10:15:30] [Agent]   💡 Phase 3: 假设生成...
[10:15:35] [AGENT]   💬 PiAgent / DeepSeek V4 Flash (token: 2031)
[10:15:38] [Agent]   ✅ 生成 3 个候选假设
[10:15:38] [Agent]   🔍 Phase 4: 假设审稿...
[10:15:42] [AGENT]   💬 PiAgent / DeepSeek V4 Flash (token: 1543)
[10:15:45] [Agent]   ✅ 审稿完成 — H1 排名第一
[10:15:45] [实验]    🧪 Phase 5: 实验设计...
[10:15:50] [实验]    📋 实验矩阵: 6 组 × 3 网格 × 2 载荷 = 36 组
[10:15:50] [GPU]     ⚡ Phase 6: 实验执行...
[10:15:52] [GPU]     🔄 提交 run_001 (OC+PDE+Heaviside+联合反馈)
                     ━━━━━━━━━━━━━━━━░░░░░░░░░░░  43%  642s
```

---

## 八、实施计划

### Phase 1：接口层 + 纯 Rich 降级版（2天）

| 任务 | 产出 | 时间 |
|------|------|------|
| `frontend/interfaces.py` | FrontendBridge + 全部数据模型 | 4h |
| `frontend/visualizers.py` | 可视化组件接口 | 3h |
| `frontend/rich/app.py` | Rich 控制台版（降级方案） | 3h |
| 轻量 2D 求解器移植 | `solver/拓扑优化2d.py` | 2h |
| **验证** | 后端推数据 → Rich 面板显示 | 1h |

### Phase 2：Textual TUI 主应用（3天）

| 任务 | 产出 | 时间 |
|------|------|------|
| 主界面布局 | `main_screen.py` + `app.tcss` | 4h |
| 任务面板 | `task_panel.py` | 2h |
| 监控面板 | `monitor_panel.py`（内嵌 matplotlib） | 4h |
| 智能体面板 | `agent_panel.py` | 3h |
| 输出面板 + 命令输入 | `output_panel.py` + `command_input.py` | 3h |
| 命令解析 + 补全 | `commands.py` | 2h |
| **验证** | 全键盘操作流畅、4面板同步更新 | 1h |

### Phase 3：演示模式交互（2天）

| 任务 | 产出 | 时间 |
|------|------|------|
| 参数滑块（键盘驱动） | 监控面板扩展 | 3h |
| 控制器策略切换 | 命令注册 | 2h |
| 实时 2D 求解 callback | 轻量求解器集成 | 3h |
| 失败→修正对比演示 | 可视化组件扩展 | 2h |
| **验证** | 5 分钟演示脚本可完整走通 | 1h |

### 合计：7 天

---

## 九、与现有系统的集成关系

```
orchestrator.py ──→ FrontendBridge.push_log()      ──→ 输出面板日志
                 ──→ FrontendBridge.push_convergence() ──→ 监控面板曲线
                 ──→ FrontendBridge.push_agent_state() ──→ 智能体面板

experiment_queue.py ──→ FrontendBridge.publish(monitor) ──→ 实验进度条

NaNChecker ──→ FrontendBridge.push_log("warning") ──→ 输出面板告警

lightweight_2d_solver.py ←── callback ←── 演示模式参数变化
```

**关键设计**：后端完全不知道前端是 Textual、Rich 还是其他实现。FrontendBridge 是唯一耦合点，任何实现了 4 个 callback 的前端都可以接入。
