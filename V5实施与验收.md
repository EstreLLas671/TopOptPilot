# TopOptPilot V5.0 实施与验收

## 冻结架构

TopOptPilot 采用四层解耦：官方 Pi Agent 只负责科研推理；确定性 Policy 将科学意图
编译成有界实验；Python/MATLAB Solver 产生物理真值；Evaluator 与 SQLite Research
Memory 负责裁决和事实持久化。Pi 不能访问 shell、文件写入、数据库或求解器，只能调用
`.pi/extensions/topopt-tools.ts` 注册的 11 个工具。

每个 Research ID 使用一个常驻、可恢复的 Pi JSON-RPC session。六个 Skill 在 Python
Bridge 侧按任务动态注入，Pi 启动参数禁止发现用户全局 Skill、Prompt 和 Extension，避免
科研沙箱被本机配置污染。根目录 `AGENTS.md` 是不可变科研宪法。

## 功能矩阵

| 能力 | 实现 |
|---|---|
| Pi Runtime / session / stream / abort / resume | `topoptpilot/agent_runtime/` |
| 11 Tool 沙箱与鉴权网关 | `.pi/extensions/topopt-tools.ts`, `tool_gateway.py` |
| Intent → Proposal → Safety | `topoptpilot/policy/intent_compiler.py` |
| DOE 与竞争解释 | `topoptpilot/policy/doe_templates.py` |
| L0–L3 Research Memory | `topoptpilot/memory/research_memory.py` |
| 异步真实 2D FEM | `solver/topopt_engine.py` |
| 真实 Hex8 Python 3D FEM | `solver/topopt3d.py` |
| F0–F3、分层预算、缓存、暖启动 | `topoptpilot/fidelity/`, `executor/` |
| MATLAB 可选后端与 replay | `solver/matlab_backend.py`, `topoptpilot/solver/matlab_adapter/replay.py` |
| Reviewer bounded workflow | `topoptpilot/agent_runtime/reviewer.py` |
| Workspace / approval / Pareto / baseline | `topoptpilot/app/` |
| Random/Grid/TPE/Rule/Pi 与消融 | `topoptpilot/benchmarks/` |
| Case A/B/C | `topoptpilot/cases/` |
| 报告与复现包 | `ResearchService.generate_report`, `/export` |

## 自主闭环约束

主 Agent 每轮必须先读取 L3 context 与 budget，只能选择科学意图；数值参数只能由 Policy
编译器产生。FEM 完成后 Evaluator 写入结构化证据，Pi 才能决定下一轮。模型调用失败时，
Safe Mode 仍只选择意图并经过同一 Policy，不绕过 Safety。终止条件是目标达成、预算耗尽、
平台期或用户停止。F3 必须经过 Reviewer、Safety 与人工审批。

## 发布验收记录（2026-08-21）

- 经典 2D FEM 地面真值、确定性、控制器、持久化、MATLAB fallback：70/70。
- 标准 pytest：15/15；覆盖工具越权、L3、Policy、暖启动、Pi session 重启、研究物理参数链、
  F3 审批、危险人工编辑、缓存损坏、worker 崩溃、案例定义、artifact 与复现包。
- 真实 3D Hex8 冒烟：稀疏方程残差低于 `1e-8`。
- 完整模型故障闭环：`ESTABLISH_BASELINE → REDUCE_GRAYNESS → BUDGET_EXHAUSTED`，
  两轮均由 Policy proposal 产生并运行真实 FEM。
- Worker 异常：实验记为 FAILED、错误留证、Research 恢复 READY。
- Cache：首次 miss 消耗预算，第二次相同物理任务 hit，不重复消耗 FEM 预算。
- 正式 Case A：6 个 F0 实验，最佳可行柔度 `99.0723`，最佳灰度率 `0.0111`。
- 正式 Case B：6 个 F0 实验，观测断连并执行竞争解释 DOE，重复实验率为 0。
- 正式 Case C：`F0 → F1 → F2 → F3`；F2 为真实 `python3d`，F3 在 MATLAB 缺席时明确标注
  `matlab_unavailable_fallback_python3d`，未伪装成 MATLAB 或 2D 结果。
- Random/Grid/TPE/Rule 均按相同 5 次真实 FEM 预算执行；Pi 基线只接受真实在线 Pi 决策，
  模型失败时结果明确标为 `Rule-fallback`。
- Fresh Python venv：`pip check` 无破损依赖；产品测试 15/15。
- Fresh npm install：Pi `0.84.2`，0 vulnerabilities。
- Streamlit `/_stcore/health` 返回 `ok`；FastAPI `/api/health` 返回 200，并报告
  `official-pi-rpc / qwen3.7-plus / Pi ready`。
- Pi session restart 保持相同 UUID 与 session file；扩展加载无错误。

统一 `python -m topoptpilot.release_audit` 审计中，全部离线门禁为通过；在线 Qwen 工具门禁为
失败。当前提供的 DashScope 凭据在真实请求中返回 HTTP 401 `invalid_api_key`。该项属于外部
凭据状态，不影响已验证的 Pi RPC 协议、会话恢复和 Safe Mode；换入有效
`DASHSCOPE_API_KEY` 后无需改代码即可重跑同一门禁。密钥只从环境变量读取，不写入源码、
报告或复现包。
